# Rondo Club Environment Variables
#
# This file contains environment variables for local tooling (deploy scripts, etc.)
# For WordPress configuration (Slack, Google, encryption keys), see README.md
#
# Copy this file to .env and fill in your values.

# =============================================================================
# DEPLOYMENT (used by bin/deploy.sh)
# =============================================================================

# SSH connection details for production server
DEPLOY_SSH_HOST=
DEPLOY_SSH_PORT=22
DEPLOY_SSH_USER=

# Remote paths on production server
DEPLOY_REMOTE_WP_PATH=~/public_html
DEPLOY_REMOTE_THEME_PATH=${DEPLOY_REMOTE_WP_PATH}/wp-content/themes/stadion

# Production URL (for reference)
DEPLOY_PRODUCTION_URL=

# =============================================================================
# RONDO API (used by bin/get-feedback.sh)
# =============================================================================

# Your Rondo Club/WordPress site URL (no trailing slash)
RONDO_API_URL=

# WordPress username for API access
RONDO_API_USER=

# WordPress Application Password (generate in WP Admin > Users > Profile > Application Passwords)
# Format: xxxx xxxx xxxx xxxx xxxx xxxx (spaces are fine)
RONDO_API_PASSWORD=

# Path to Homebrew bin directory (required for cron/launchd scripts)
# This adds the directory to PATH so node, claude, etc. can be found
# Find with: brew --prefix
HOMEBREW_PATH=/opt/homebrew/bin

# User home directory (required for cron/launchd scripts)
# Claude needs this to find its config at ~/.claude/
# Find with: echo $HOME
USER_HOME=/Users/joostdevalk

# Path to Claude CLI binary (optional, defaults to 'claude' in PATH)
# Only needed if HOMEBREW_PATH is not set
# CLAUDE_PATH=/opt/homebrew/bin/claude
