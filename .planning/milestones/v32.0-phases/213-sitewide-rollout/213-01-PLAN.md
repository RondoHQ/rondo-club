---
phase: 213-sitewide-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Finance/FactuurDetail.jsx
  - src/pages/Finance/Facturen.jsx
  - src/pages/Finance/FinanceSettings.jsx
  - src/pages/Finance/FinanceDashboard.jsx
  - src/pages/Finance/FactuurNieuw.jsx
  - src/components/finance/InvoiceDraftForm.jsx
  - src/components/FinancesCard.jsx
  - src/components/DisciplineCaseTable.jsx
autonomous: true
requirements: [ROLL-01, ROLL-03]

must_haves:
  truths:
    - "On draft invoice detail: Send uses btn-primary, Mark Paid uses btn-secondary, PDF/Regenerate use btn-tertiary, Delete uses btn-danger"
    - "On sent/overdue invoice detail: Mark Paid uses btn-secondary, Resend uses btn-tertiary, PDF actions use btn-tertiary"
    - "On paid invoice detail: PDF actions use btn-tertiary"
    - "No inline color overrides (border-green, border-red, border-orange, bg-green, bg-deep-midnight) remain on any button in Finance files"
    - "Finance list page Create button uses btn-primary, all utility buttons use btn-tertiary"
    - "InvoiceDraftForm submit uses btn-primary, cancel uses btn-secondary, add/remove line item use btn-tertiary"
  artifacts:
    - path: "src/pages/Finance/FactuurDetail.jsx"
      provides: "Invoice detail with correct tier hierarchy per status"
    - path: "src/components/finance/InvoiceDraftForm.jsx"
      provides: "Draft form with correct tier buttons"
    - path: "src/components/DisciplineCaseTable.jsx"
      provides: "Create invoice button without inline overrides"
  key_links:
    - from: "src/pages/Finance/FactuurDetail.jsx"
      to: "src/index.css"
      via: "btn-primary, btn-secondary, btn-tertiary, btn-danger classes"
      pattern: "btn-(primary|secondary|tertiary|danger)"
---

<objective>
Apply correct button tier hierarchy to all Finance-related pages and components.

Purpose: Finance pages are the most complex button area — FactuurDetail alone has ~20 buttons with 3 inline color override patterns (green for mark-paid, red for delete, orange for reset) that must be replaced with proper tier classes. This plan eliminates all rogue inline styles and assigns correct tiers based on action weight.

Output: All 8 Finance-related files using only btn-primary/secondary/tertiary/danger with no inline color overrides.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-button-css-system/212-01-SUMMARY.md

<interfaces>
<!-- Button CSS tiers from src/index.css (Phase 212) -->
.btn-primary  — gradient fill, highest prominence (Save, Send, Create)
.btn-secondary — outlined, brand border + text (important secondary: Mark Paid, Cancel)
.btn-tertiary  — ghost, text-only, subtle hover (utility: PDF, Copy, Regenerate, filter)
.btn-danger    — red filled, destructive (Delete, Remove)

<!-- All tiers include: inline-flex items-center justify-center px-4 py-2 rounded-lg font-medium -->
<!-- So "flex items-center gap-2" in existing classes can be simplified to just "gap-2" -->
<!-- Keep "gap-2" when button has icon + text. Remove redundant flex/inline-flex/items-center. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply tier hierarchy to FactuurDetail.jsx</name>
  <files>src/pages/Finance/FactuurDetail.jsx</files>
  <action>
Reassign button tiers in FactuurDetail.jsx according to action weight. The file has buttons grouped by invoice status (draft, sent/overdue, paid). Apply these mappings:

**Draft status buttons:**
- "Verstuur factuur" (Send) — keep btn-primary (already correct)
- "Markeer als betaald (zonder versturen)" — change from btn-secondary with green inline overrides to btn-secondary (remove: border-green-300 dark:border-green-700 text-green-700 dark:text-green-400 hover:bg-green-50 dark:hover:bg-green-900/20)
- "Download PDF" / "Genereer PDF" / "PDF opnieuw genereren" — change from btn-secondary to btn-tertiary
- "Regenerate PDF" — change from btn-secondary to btn-tertiary
- "Verwijder factuur" (Delete) — change from btn-secondary with red inline overrides to btn-danger (remove: border-red-300 dark:border-red-700 text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20)

**Sent/Overdue status buttons:**
- "Markeer als betaald" — change from btn-primary with green inline overrides (bg-green-600 hover:bg-green-700) to btn-secondary
- "Opnieuw versturen" (Resend) — change from btn-secondary to btn-tertiary
- "Download PDF" — change from btn-secondary to btn-tertiary
- "PDF opnieuw genereren" — change from btn-secondary to btn-tertiary
- "Kortingen aanpassen" (Update discounts) — change from btn-secondary to btn-tertiary

**Paid status buttons:**
- "Download PDF" — change from btn-secondary to btn-tertiary
- "PDF opnieuw genereren" — change from btn-secondary to btn-tertiary
- "Genereer PDF" — change from btn-secondary to btn-tertiary

**Cross-status buttons:**
- "Betaallink aanmaken" / "Betaallink opnieuw aanmaken" (payment link) — change from btn-secondary to btn-tertiary
- "Reset factuur (test)" — change from btn-secondary with orange inline overrides to btn-danger (remove: border-orange-300 dark:border-orange-700 text-orange-600 dark:text-orange-400 hover:bg-orange-50 dark:hover:bg-orange-900/20). Test-only reset is destructive.

**Line ~733** (the btn btn-secondary on "Opslaan" save button inside edit mode) — change to btn-primary (it is a save action).

**Also around line 440** — the btn-secondary on the edit pencil button area, check context and assign btn-tertiary if it is a utility action.

For each change: replace the full className string. Since btn-* classes include inline-flex/items-center/justify-center/px-4/py-2/rounded-lg/font-medium, remove those redundant Tailwind classes. Keep only: the btn-* class, gap-2 (if icon+text), and any size overrides like text-sm.

Example: `className="btn-secondary flex items-center gap-2"` becomes `className="btn-tertiary gap-2"` (flex and items-center are in the base).
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && grep -c 'border-green-\|border-red-\|border-orange-\|bg-green-600\|bg-deep-midnight' src/pages/Finance/FactuurDetail.jsx | grep -q '^0$' && echo "PASS: No inline color overrides" || echo "FAIL: Inline overrides remain"</automated>
  </verify>
  <done>FactuurDetail.jsx uses only btn-primary (send), btn-secondary (mark paid), btn-tertiary (PDF, payment link, resend, utility), btn-danger (delete, reset) with zero inline color overrides on buttons</done>
</task>

<task type="auto">
  <name>Task 2: Apply tier hierarchy to remaining Finance files</name>
  <files>src/pages/Finance/Facturen.jsx, src/pages/Finance/FinanceSettings.jsx, src/pages/Finance/FinanceDashboard.jsx, src/pages/Finance/FactuurNieuw.jsx, src/components/finance/InvoiceDraftForm.jsx, src/components/FinancesCard.jsx, src/components/DisciplineCaseTable.jsx</files>
  <action>
Apply button tier assignments to each file:

**Facturen.jsx (invoice list):**
- "Nieuwe factuur" Link (line ~282) — keep btn-primary (correct, it is the main CTA). Remove redundant inline-flex/items-center since btn includes them; keep gap-2.

**FinanceSettings.jsx:**
- "Verstuur" test email button (line ~78) — change from btn-secondary to btn-primary (it is the primary action on this section). Remove redundant flex/items-center/justify-center/whitespace-nowrap.

**FinanceDashboard.jsx / FactuurNieuw.jsx:**
- Audit any buttons — apply primary for main CTA, tertiary for utility.

**InvoiceDraftForm.jsx:**
- Submit button (line ~480, "btn btn-primary") — keep btn-primary, remove redundant "btn" prefix (btn-primary already extends .btn).
- Cancel button (line ~482, "btn btn-secondary") — keep btn-secondary, remove "btn" prefix.
- "Add line item" button (line ~457, "btn btn-secondary") — change to btn-tertiary (utility action), remove "btn" prefix.
- "Remove line item" button (line ~452, "btn btn-secondary") — change to btn-danger (destructive), remove "btn" prefix. It deletes a line.

**FinancesCard.jsx:**
- Audit buttons — likely navigation links, apply btn-tertiary for utility or btn-primary for main CTA.

**DisciplineCaseTable.jsx:**
- "Maak factuur(en)" button (line ~272) — change from btn-secondary with heavy inline overrides (bg-deep-midnight hover:bg-obsidian text-white px-4 py-2 rounded-lg flex items-center gap-2 disabled:opacity-50) to btn-primary gap-2 (it is the main action after selection). Remove ALL inline overrides since btn-primary provides everything.

For all files: remove redundant Tailwind classes that are already in the btn-* base (inline-flex, items-center, justify-center, px-4, py-2, rounded-lg, font-medium, disabled:opacity-50, disabled:cursor-not-allowed). Keep gap-2 and size overrides.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && grep -c 'bg-deep-midnight\|hover:bg-obsidian' src/components/DisciplineCaseTable.jsx | grep -q '^0$' && grep -c '"btn btn-' src/components/finance/InvoiceDraftForm.jsx | grep -q '^0$' && echo "PASS: No rogue overrides" || echo "FAIL: Overrides remain"</automated>
  </verify>
  <done>All Finance list, settings, draft form, and discipline case files use correct tier hierarchy with no inline overrides or redundant "btn" prefixes</done>
</task>

</tasks>

<verification>
1. `grep -rn 'border-green-\|border-red-\|border-orange-\|bg-green-600\|bg-deep-midnight\|hover:bg-obsidian' src/pages/Finance/ src/components/finance/ src/components/DisciplineCaseTable.jsx src/components/FinancesCard.jsx` returns no results
2. `grep -c '"btn btn-' src/components/finance/InvoiceDraftForm.jsx` returns 0
3. `npm run build` succeeds
4. `npm run lint` passes
</verification>

<success_criteria>
- Every button in Finance-related files uses exactly one of btn-primary/btn-secondary/btn-tertiary/btn-danger
- No inline color overrides (green, red, orange, deep-midnight) remain on any button
- Send/Create actions use btn-primary, Mark Paid uses btn-secondary, PDF/utility actions use btn-tertiary, Delete/Reset use btn-danger
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/213-sitewide-rollout/213-01-SUMMARY.md`
</output>
