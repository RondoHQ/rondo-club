---
phase: 213-sitewide-rollout
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/People/PeopleList.jsx
  - src/pages/People/PersonDetail.jsx
  - src/pages/Teams/TeamDetail.jsx
  - src/pages/Teams/TeamsList.jsx
  - src/pages/Teams/Kaderlijst.jsx
  - src/pages/Commissies/CommissieDetail.jsx
  - src/pages/Commissies/CommissiesList.jsx
  - src/pages/Settings/Settings.jsx
  - src/pages/Settings/CustomFields.jsx
  - src/pages/Settings/RelationshipTypes.jsx
  - src/pages/Settings/FeedbackManagement.jsx
  - src/pages/Profile/Profile.jsx
  - src/pages/MembershipPassScanner.jsx
  - src/router.jsx
autonomous: true
requirements: [ROLL-04, ROLL-05]

must_haves:
  truths:
    - "On People, Teams, Commissies list pages, filter clear and utility buttons use btn-tertiary"
    - "On detail pages, back-navigation links use btn-tertiary, share buttons use btn-tertiary"
    - "On Settings pages, Save buttons use btn-primary, utility and add-item buttons use correct tiers"
    - "No redundant inline-flex/items-center/px-4/py-2/rounded-lg classes remain alongside btn-* classes"
  artifacts:
    - path: "src/pages/Settings/Settings.jsx"
      provides: "Settings page with correct tier hierarchy"
    - path: "src/pages/People/PersonDetail.jsx"
      provides: "Person detail with correct button tiers"
    - path: "src/pages/People/PeopleList.jsx"
      provides: "People list with correct filter/utility tiers"
  key_links:
    - from: "src/pages/Settings/Settings.jsx"
      to: "src/index.css"
      via: "btn-primary for save, btn-tertiary for utility"
      pattern: "btn-(primary|secondary|tertiary)"
    - from: "src/pages/People/PersonDetail.jsx"
      to: "src/index.css"
      via: "btn-tertiary for back/share/edit utility buttons"
      pattern: "btn-tertiary"
---

<objective>
Apply correct button tier hierarchy to People, Teams, Commissies, Settings, Profile, and remaining page files (14 files).

Purpose: These pages have back-navigation, share, filter, and utility buttons that should be tertiary. Settings has Save buttons (primary) and utility buttons (tertiary). Split from original 213-02 to keep plans within scope limits.

Output: All 14 page files using correct btn-* tiers with no rogue styles.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-button-css-system/212-01-SUMMARY.md

<interfaces>
<!-- Button CSS tiers from src/index.css (Phase 212) -->
.btn-primary  — gradient fill (Save, Send, Create, Submit)
.btn-secondary — outlined, brand border (Cancel, Close, important-but-not-primary)
.btn-tertiary  — ghost, text-only (Back, Share, Clear filters, utility)
.btn-danger    — red filled (Delete, Remove, permanent destructive)

<!-- Base includes: inline-flex items-center justify-center px-4 py-2 rounded-lg font-medium -->
<!-- Clean up pattern: "btn-secondary flex items-center gap-2" -> "btn-secondary gap-2" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply tier hierarchy to People, Teams, and Commissies pages</name>
  <files>src/pages/People/PeopleList.jsx, src/pages/People/PersonDetail.jsx, src/pages/Teams/TeamDetail.jsx, src/pages/Teams/TeamsList.jsx, src/pages/Teams/Kaderlijst.jsx, src/pages/Commissies/CommissieDetail.jsx, src/pages/Commissies/CommissiesList.jsx</files>
  <action>
Apply tier assignments to each page:

**PeopleList.jsx:**
- "Annuleren" cancel in add-person dialog (line ~527) — keep btn-secondary
- "Toevoegen" submit in add-person dialog (line ~533) — keep btn-primary
- "Wis filters" clear filters (line ~1208) — change from btn-secondary to btn-tertiary
- Export button (line ~1079) — change from btn-secondary to btn-tertiary (utility)

**PersonDetail.jsx:**
- "Terug naar leden" back link (line ~951) — change from btn-secondary to btn-tertiary (navigation)
- Share button (line ~1040) — change from btn-secondary to btn-tertiary (utility)
- Export vCard button (line ~1048) — change from btn-secondary to btn-tertiary (utility)
- All "Bewerken"/"Toevoegen" edit/add buttons throughout (~1295, ~1430, ~1595, ~1603, ~1769, ~1855) — change from btn-secondary to btn-tertiary (these are inline edit triggers, utility weight)

**TeamDetail.jsx:**
- "Terug naar teams" back link (line ~158) — change from btn-secondary to btn-tertiary
- Share button (line ~180) — change from btn-secondary to btn-tertiary

**TeamsList.jsx:**
- "Wis filters" clear filters (line ~556) — change from btn-secondary to btn-tertiary

**Kaderlijst.jsx:**
- Export button (line ~567) — change from btn-secondary to btn-tertiary

**CommissieDetail.jsx:**
- "Terug naar commissies" back link (line ~90) — change from btn-secondary to btn-tertiary
- Share button (line ~112) — change from btn-secondary to btn-tertiary

**CommissiesList.jsx:**
- "Wis filters" clear filters (line ~478) — change from btn-secondary to btn-tertiary

For all files: clean up redundant Tailwind classes that duplicate btn-* base styles. Keep gap-2, text-sm, and conditional classes.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && FAILS=""; for f in src/pages/People/PersonDetail.jsx src/pages/Teams/TeamDetail.jsx src/pages/Commissies/CommissieDetail.jsx; do if grep -q 'btn-secondary.*Terug\|Terug.*btn-secondary' "$f" 2>/dev/null; then FAILS="$FAILS $f"; fi; done; for f in src/pages/People/PeopleList.jsx src/pages/Teams/TeamsList.jsx src/pages/Commissies/CommissiesList.jsx; do if grep -q 'btn-secondary.*Wis filters\|Wis filters.*btn-secondary' "$f" 2>/dev/null; then FAILS="$FAILS $f"; fi; done; if [ -z "$FAILS" ]; then echo "PASS: Back nav and clear filter buttons are not btn-secondary"; else echo "FAIL: btn-secondary still on back/filter buttons in:$FAILS"; fi</automated>
  </verify>
  <done>People, Teams, Commissies pages use correct tiers: back links=btn-tertiary, share=btn-tertiary, clear filters=btn-tertiary, create/save=btn-primary, cancel=btn-secondary</done>
</task>

<task type="auto">
  <name>Task 2: Apply tier hierarchy to Settings, Profile, Scanner, and router pages</name>
  <files>src/pages/Settings/Settings.jsx, src/pages/Settings/CustomFields.jsx, src/pages/Settings/RelationshipTypes.jsx, src/pages/Settings/FeedbackManagement.jsx, src/pages/Profile/Profile.jsx, src/pages/MembershipPassScanner.jsx, src/router.jsx</files>
  <action>
Apply tier assignments to each page:

**Settings.jsx** (large file, many buttons):
- All Save/Submit buttons (lines ~658, ~802, ~952, ~1243, ~1831, ~2582) — keep btn-primary
- "Synchroniseer data" button (~1793) — change from btn-secondary to btn-tertiary (utility)
- "Webhook aanmaken" button (line ~1828) — keep btn-secondary (it creates something important, secondary action next to Save primary)
- "Uitnodiging versturen" invite send buttons (~1903, ~1927) — keep btn-primary (primary action in that section)
- Role management buttons (~1944) — change from btn-secondary to btn-tertiary if utility
- User management action (~2522) — change from btn-secondary to btn-tertiary if utility
- Small text-xs buttons (~1088, ~1107) — change from btn-secondary to btn-tertiary (they are small utility actions)
- Settings save (~3079) — keep btn-primary

**CustomFields.jsx:**
- "Terug" back link (line ~245) — change from btn-primary to btn-tertiary (it is a back link, navigation)
- "Nieuw veld" add button (line ~334) — keep btn-primary (main CTA on page)

**RelationshipTypes.jsx:**
- "Terug" back link (line ~322) — change from btn-primary to btn-tertiary (navigation)
- Add/save buttons (lines ~349, ~390, ~465) — keep btn-primary
- Cancel buttons (lines ~405, ~476) — keep btn-secondary
- Utility "Toevoegen" (line ~248) — keep btn-secondary (it is a meaningful add action)

**FeedbackManagement.jsx:**
- "Terug" back link (line ~69) — change from btn-primary to btn-tertiary (navigation)
- Add button (line ~165) — keep btn-secondary (add action)

**Profile.jsx:**
- Save button (line ~376) — keep btn-primary. Remove redundant "btn" prefix from "btn btn-primary".

**MembershipPassScanner.jsx:**
- Start camera (line ~182) — keep btn-primary
- Stop camera (line ~187) — keep btn-secondary
- Person link (line ~238) — change from btn-secondary to btn-tertiary (navigation)

**router.jsx:**
- Error page "Ga terug" link (line ~52) — change to btn-tertiary (go-back navigation). Remove redundant "btn" prefix if present.

For all files: clean up redundant Tailwind classes that duplicate btn-* base styles.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && FAILS=""; for f in src/pages/Settings/CustomFields.jsx src/pages/Settings/RelationshipTypes.jsx src/pages/Settings/FeedbackManagement.jsx; do if grep -q 'btn-primary.*Terug\|Terug.*btn-primary' "$f" 2>/dev/null; then FAILS="$FAILS $f"; fi; done; if grep -q '"btn btn-' src/pages/Profile/Profile.jsx 2>/dev/null; then FAILS="$FAILS Profile.jsx"; fi; if [ -z "$FAILS" ]; then echo "PASS: Back nav buttons are tertiary, no double btn prefixes"; else echo "FAIL: Issues in:$FAILS"; fi</automated>
  </verify>
  <done>Settings save=btn-primary, back links in CustomFields/RelationshipTypes/FeedbackManagement=btn-tertiary, no "btn btn-*" double prefixes, utility buttons=btn-tertiary</done>
</task>

</tasks>

<verification>
1. `grep -rn '"btn btn-' src/pages/ src/router.jsx` returns no results (no double "btn" prefixes)
2. Back navigation buttons in Settings sub-pages do not use btn-primary: `grep -rn 'btn-primary.*Terug' src/pages/Settings/` returns no matches
3. `npm run build` succeeds
4. `npm run lint` passes
</verification>

<success_criteria>
- People/Teams/Commissies: back links=btn-tertiary, share=btn-tertiary, filters=btn-tertiary
- Settings: save=btn-primary, utility=btn-tertiary, no misassigned navigation buttons
- No redundant "btn btn-*" double class prefixes
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/213-sitewide-rollout/213-04-SUMMARY.md`
</output>
