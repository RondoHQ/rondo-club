---
phase: 213-sitewide-rollout
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Feedback/FeedbackList.jsx
  - src/pages/Feedback/FeedbackDetail.jsx
  - src/pages/VOG/VOGList.jsx
  - src/pages/VOG/VOGUpcoming.jsx
  - src/pages/Contributie/ContributieList.jsx
  - src/pages/Contributie/ContributieOverzicht.jsx
  - src/pages/Contributie/NogTeFactureren.jsx
  - src/pages/Contributie/SeasonSelector.jsx
  - src/pages/Clothing/ClothingPage.jsx
  - src/pages/Todos/TodosList.jsx
  - src/components/DataTable/DataTableToolbar.jsx
  - src/components/DataTable/ColumnSettingsPanel.jsx
  - src/components/CustomFieldsSection.jsx
autonomous: true
requirements: [ROLL-06, ROLL-07]

must_haves:
  truths:
    - "On Feedback, VOG, Contributie, Clothing pages all buttons use correct tier for their action weight"
    - "DataTable toolbar filter button and column settings button use btn-tertiary"
    - "Clear filter buttons use btn-tertiary across all pages"
    - "Primary actions (Create, Send) use btn-primary; utility actions use btn-tertiary"
    - "SeasonSelector dropdown styled as btn-tertiary (no lift, no shadow)"
  artifacts:
    - path: "src/components/DataTable/DataTableToolbar.jsx"
      provides: "Toolbar with tertiary utility buttons"
    - path: "src/pages/Feedback/FeedbackList.jsx"
      provides: "Feedback list with correct tier buttons"
    - path: "src/pages/Contributie/NogTeFactureren.jsx"
      provides: "Invoice creation page with correct tiers"
  key_links:
    - from: "src/components/DataTable/DataTableToolbar.jsx"
      to: "src/index.css"
      via: "btn-tertiary for filter/column utility actions"
      pattern: "btn-tertiary"
---

<objective>
Apply correct button tier hierarchy to Feedback, VOG, Contributie, Clothing, Todos pages and DataTable toolbar components.

Purpose: These are the remaining pages that need tier assignment. The DataTable toolbar is used across many list pages and its filter/column buttons should consistently use btn-tertiary. Feedback, VOG, Contributie pages have a mix of primary CTAs and utility buttons.

Output: All 13 remaining files using correct btn-* tiers with no rogue styles.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-button-css-system/212-01-SUMMARY.md

<interfaces>
<!-- Button CSS tiers from src/index.css (Phase 212) -->
.btn-primary  — gradient fill (Save, Send, Create main CTA)
.btn-secondary — outlined, brand border (important secondary actions)
.btn-tertiary  — ghost, text-only (utility: filter, export, refresh, clear)
.btn-danger    — red filled (Delete, Remove)

<!-- Base includes: inline-flex items-center justify-center px-4 py-2 rounded-lg font-medium -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply tier hierarchy to Feedback, VOG, Contributie, Clothing, and Todos pages</name>
  <files>src/pages/Feedback/FeedbackList.jsx, src/pages/Feedback/FeedbackDetail.jsx, src/pages/VOG/VOGList.jsx, src/pages/VOG/VOGUpcoming.jsx, src/pages/Contributie/ContributieList.jsx, src/pages/Contributie/ContributieOverzicht.jsx, src/pages/Contributie/NogTeFactureren.jsx, src/pages/Contributie/SeasonSelector.jsx, src/pages/Clothing/ClothingPage.jsx, src/pages/Todos/TodosList.jsx, src/components/CustomFieldsSection.jsx</files>
  <action>
Apply tier assignments per file:

**FeedbackList.jsx:**
- "Nieuwe feedback" create button (line ~395) — keep btn-primary. Clean up redundant classes; keep gap-2 and text-sm.

**FeedbackDetail.jsx:**
- Back button (line ~184) — change from btn-secondary to btn-tertiary (navigation)
- Reply/submit button (line ~373) — keep btn-primary

**VOGList.jsx:**
- Export/download button (line ~540) — change from btn-secondary to btn-tertiary (utility)
- "Wis filters" clear filters (line ~627) — change from btn-secondary to btn-tertiary

**VOGUpcoming.jsx:**
- Export button (line ~140) — change from btn-secondary to btn-tertiary (utility)

**ContributieList.jsx:**
- Export button (line ~446) — change from btn-secondary to btn-tertiary (utility)
- Category filter button (line ~486) — change from btn-secondary to btn-tertiary
- "Wis filters" clear filters (line ~507) — change from btn-secondary to btn-tertiary. Keep text-sm.

**ContributieOverzicht.jsx:**
- "Maak facturen" create invoices button (line ~90) — keep btn-primary (main CTA)

**NogTeFactureren.jsx:**
- Individual "Factureer" buttons (line ~375) — change from btn-secondary to btn-tertiary. These are small per-row utility actions (text-xs py-1 px-2). Keep size overrides.
- "Ververs" refresh button (line ~418) — change from btn-secondary to btn-tertiary (utility)
- "Factureer geselecteerden" bulk create button (line ~476) — keep btn-primary (main CTA)

**SeasonSelector.jsx:**
- Season dropdown select (line ~16) — currently btn-secondary with explicit hover:translate-y-0 hover:shadow-none overrides. Change to btn-tertiary. Remove the hover:translate-y-0 hover:shadow-none since btn-tertiary already does not lift. Keep appearance-none, pr-12, bg-no-repeat for the dropdown arrow styling.

**ClothingPage.jsx:**
- "Toewijzen" assign buttons (line ~430, ~450) — change from btn-secondary to btn-tertiary. These are small utility actions. Keep text-sm/text-xs.
- "Toevoegen" add/create submit buttons (line ~490, ~521) — keep btn-primary. Keep text-sm.
- Return buttons (line ~557) — change from btn-secondary to btn-tertiary. Keep text-xs.
- Export button (line ~574) — change from btn-secondary to btn-tertiary

**TodosList.jsx:**
- "Nieuwe taak" create button (line ~224) — keep btn-primary. Keep text-sm, gap-2.

**CustomFieldsSection.jsx:**
- "Bewerken" edit buttons (line ~468) — change from btn-secondary to btn-tertiary (inline utility). Keep text-sm.

For all files: remove redundant Tailwind classes that duplicate btn-* base styles. Keep size overrides (text-sm, text-xs, py-1, px-2) and gap-2.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && FAILS=""; if grep -q 'btn-secondary.*Terug\|Terug.*btn-secondary' src/pages/Feedback/FeedbackDetail.jsx 2>/dev/null; then FAILS="$FAILS FeedbackDetail-back"; fi; for f in src/pages/VOG/VOGList.jsx src/pages/Contributie/ContributieList.jsx; do if grep -q 'btn-secondary.*Wis filters\|Wis filters.*btn-secondary' "$f" 2>/dev/null; then FAILS="$FAILS $f-filter"; fi; done; if grep -q 'btn-secondary' src/pages/Contributie/SeasonSelector.jsx 2>/dev/null; then FAILS="$FAILS SeasonSelector"; fi; if grep -q 'hover:translate-y-0' src/pages/Contributie/SeasonSelector.jsx 2>/dev/null; then FAILS="$FAILS SeasonSelector-lift"; fi; if [ -z "$FAILS" ]; then echo "PASS: Correct tier assignments verified"; else echo "FAIL: Issues in:$FAILS"; fi</automated>
  </verify>
  <done>Feedback, VOG, Contributie, Clothing, Todos pages all use correct tiers: create=primary, export/filter/utility=tertiary, no misassigned buttons</done>
</task>

<task type="auto">
  <name>Task 2: Apply tier hierarchy to DataTable toolbar components</name>
  <files>src/components/DataTable/DataTableToolbar.jsx, src/components/DataTable/ColumnSettingsPanel.jsx</files>
  <action>
Apply tier assignments to DataTable shared components:

**DataTableToolbar.jsx:**
- Filter button (line ~63) — change from btn-secondary to btn-tertiary. This is the filter toggle button with conditional active state. The existing className uses a template literal with conditional border/bg classes for active state. Replace btn-secondary with btn-tertiary and simplify the active state styling. When active, the filter button can use `bg-gray-100 dark:bg-gray-800` which matches btn-tertiary hover state — or just keep the active indicator as-is if it uses a different visual approach. Check the conditional classes and preserve the active/inactive distinction.
- Column settings cog button (line ~112) — change from btn-secondary to btn-tertiary (utility action).

**ColumnSettingsPanel.jsx:**
- "Sluiten" close button (line ~106) — keep btn-secondary (dismiss action on settings panel).

For both files: remove redundant Tailwind classes. The DataTableToolbar filter button likely has custom conditional classes for active state — preserve those while changing the base tier.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && grep -c 'btn-secondary' src/components/DataTable/DataTableToolbar.jsx | grep -q '^0$' && echo "PASS: Toolbar uses tertiary" || echo "FAIL: Secondary remains"</automated>
  </verify>
  <done>DataTable toolbar filter and column buttons use btn-tertiary; no btn-secondary remains on utility toolbar actions</done>
</task>

</tasks>

<verification>
1. `grep -rn 'btn-secondary' src/components/DataTable/DataTableToolbar.jsx` returns no matches
2. All "Wis filters" / clear filter buttons across the codebase use btn-tertiary: `grep -rn 'Wis filters\|clearFilter' src/pages/ | grep -v btn-tertiary` returns no matches
3. `npm run build` succeeds
4. `npm run lint` passes
</verification>

<success_criteria>
- DataTable toolbar uses btn-tertiary for all filter/column utility actions
- Feedback, VOG, Contributie, Clothing pages have correct tier assignments
- Create/Send CTAs use btn-primary, all utility/export/filter buttons use btn-tertiary
- SeasonSelector uses btn-tertiary without manual lift suppression
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/213-sitewide-rollout/213-03-SUMMARY.md`
</output>
