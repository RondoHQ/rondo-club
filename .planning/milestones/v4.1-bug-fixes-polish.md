# Milestone v4.1: Bug Fixes & Polish

**Status:** SHIPPED 2026-01-15
**Phases:** 56-57
**Total Plans:** 3 (56-01, 57-01, 57-FIX)

## Overview

Bug fix milestone addressing dark mode contrast issues, console errors from stale build artifacts, and calendar widget polish. Restructured dashboard to 3-row layout and added dynamic favicon that updates with accent color.

## Phases

### Phase 56: Dark Mode & Console Fixes

**Goal**: Fix dark mode contrast issues and console errors
**Depends on**: v4.0 Calendar Integration
**Plans**: 1 plan

Plans:
- [x] 56-01: Dark Mode & Console Fixes (3 tasks)

**Details:**
- Fixed dark mode contrast in CardDAV connection details and search modal
- Documented DOM synchronization errors as benign React 18 StrictMode artifacts
- Identified MIME type errors caused by stale build artifacts (1535 files, 89MB)
- Updated deploy procedure to two-step rsync with --delete for dist/ folder

### Phase 57: Calendar Widget Polish

**Goal**: Fix Today's meetings layout, timezone display, and update favicon
**Depends on**: Phase 56
**Plans**: 2 plans

Plans:
- [x] 57-01: Calendar Widget Polish (3 tasks)
- [x] 57-FIX: UAT Issue Fixes (2 tasks)

**Details:**
- Dashboard restructured to 3-row layout (Stats | Activity | Favorites)
- Meeting times now include timezone offset using ISO 8601 format
- Frontend correctly displays meeting times in user's local timezone
- Favicon dynamically updates to match user's selected accent color
- PHP static favicon removed to allow React full control

---

## Milestone Summary

**Key Decisions:**

- DOM errors documented as benign React 18 StrictMode artifacts - no fix needed
- MIME type errors caused by rsync without --delete accumulating stale artifacts
- Two-step rsync deploy: first dist/ with --delete, then remaining files
- Dashboard restructured to 3 rows instead of conditional 4-column layout
- React manages favicon dynamically via inline SVG data URLs

**Issues Resolved:**

- Fixed CardDAV connection details dark mode contrast
- Fixed search modal active result dark mode contrast
- Resolved MIME type console errors via deploy procedure fix
- Dashboard layout matches user's requested 3-row structure
- Favicon now updates dynamically with accent color changes

**Issues Deferred:**

- ESLint config missing (pre-existing, not caused by this milestone)

**Technical Debt Incurred:**

None

---

_For current project status, see .planning/ROADMAP.md_
