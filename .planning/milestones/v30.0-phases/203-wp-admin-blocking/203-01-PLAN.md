---
phase: 203-wp-admin-blocking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions.php
  - ../developer/src/content/docs/features/access-control.md
autonomous: true

must_haves:
  truths:
    - "A logged-in non-admin user navigating to /wp-admin/ is redirected to the app home page"
    - "Admin-ajax.php requests from non-admin users continue to work"
    - "WP-CLI commands run without the admin block applying"
    - "Cron tasks run without the admin block applying"
    - "Administrator users reach wp-admin normally"
  artifacts:
    - path: "functions.php"
      provides: "admin_init hook blocking non-admin users from wp-admin"
      contains: "rondo_block_wp_admin"
    - path: "../developer/src/content/docs/features/access-control.md"
      provides: "Documentation of WP admin blocking behavior"
      contains: "wp-admin"
  key_links:
    - from: "functions.php"
      to: "wp-admin"
      via: "admin_init hook with wp_safe_redirect"
      pattern: "rondo_block_wp_admin"
---

<objective>
Block non-admin users from accessing the WordPress admin panel by adding an `admin_init` hook that redirects them to the app home page, while exempting AJAX, WP-CLI, and cron requests.

Purpose: Prevent non-admin Rondo users from ever seeing the WordPress backend, keeping them entirely within the React SPA experience.
Output: One new function in functions.php, updated developer documentation.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@functions.php (reference: show_admin_bar(false) at line 702-707 for pattern, admin_init hook at line 464)
@includes/class-access-control.php (reference: manage_options check at line 63)
@../developer/src/content/docs/features/access-control.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin_init redirect hook for non-admin users</name>
  <files>functions.php</files>
  <action>
Add a new function `rondo_block_wp_admin()` hooked to `admin_init` that redirects non-admin users away from wp-admin.

Place it immediately after the existing `rondo_theme_remove_admin_bar()` function (after line 707), since both deal with admin UI restrictions for non-admin users.

The function must check these conditions and return early (allow access) if ANY are true:
1. `wp_doing_ajax()` -- admin-ajax.php requests must continue working
2. `defined( 'WP_CLI' ) && WP_CLI` -- CLI commands must not be affected
3. `defined( 'DOING_CRON' ) && DOING_CRON` -- cron tasks must not be affected
4. `current_user_can( 'manage_options' )` -- administrators pass through normally

If none of the exemptions apply (i.e., a regular logged-in user is hitting wp-admin), call:
```php
wp_safe_redirect( home_url( '/' ) );
exit;
```

Implementation:
```php
/**
 * Block non-admin users from accessing wp-admin.
 *
 * Redirects users without manage_options capability to the app home page.
 * Exempts AJAX requests, WP-CLI, and cron to avoid breaking functionality.
 */
function rondo_block_wp_admin() {
	if ( wp_doing_ajax() ) {
		return;
	}

	if ( defined( 'WP_CLI' ) && WP_CLI ) {
		return;
	}

	if ( defined( 'DOING_CRON' ) && DOING_CRON ) {
		return;
	}

	if ( current_user_can( 'manage_options' ) ) {
		return;
	}

	wp_safe_redirect( home_url( '/' ) );
	exit;
}
add_action( 'admin_init', 'rondo_block_wp_admin' );
```

Note: `admin_init` only fires inside wp-admin, so `is_admin()` is implicitly true and does not need checking. REST API requests do NOT go through `admin_init`, so the REST API is unaffected without an explicit check.
  </action>
  <verify>
1. Run `npm run build` to ensure no frontend regressions.
2. Run `npm run lint` to verify ESLint stays clean (PHP-only change, but verify nothing broke).
3. Grep functions.php for `rondo_block_wp_admin` to confirm the function exists.
4. Verify the function contains all five exemptions: `wp_doing_ajax`, `WP_CLI`, `DOING_CRON`, `manage_options`, `wp_safe_redirect`.
  </verify>
  <done>
functions.php contains a rondo_block_wp_admin function hooked to admin_init that redirects non-admin users to home_url('/') while exempting AJAX, WP-CLI, and cron requests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update developer documentation for WP admin blocking</name>
  <files>../developer/src/content/docs/features/access-control.md</files>
  <action>
Add a new section to the access control documentation between "User Roles" and "Security Considerations" titled "## WP Admin Blocking".

Content to add:

```markdown
## WP Admin Blocking

Non-admin users are blocked from accessing the WordPress admin panel (`/wp-admin/`). When a user without `manage_options` capability navigates to any wp-admin URL, they are immediately redirected to the app home page.

### How It Works

A function hooked to `admin_init` checks whether the current user has the `manage_options` capability. If not, the user is redirected via `wp_safe_redirect()`.

### Exemptions

The following request types are exempt from the redirect:

| Request Type | Detection | Why Exempt |
|-------------|-----------|------------|
| AJAX | `wp_doing_ajax()` | admin-ajax.php serves frontend AJAX requests and lives under /wp-admin/ |
| WP-CLI | `defined( 'WP_CLI' )` | CLI commands should never be redirected |
| Cron | `defined( 'DOING_CRON' )` | Scheduled tasks must run unimpeded |
| Administrators | `current_user_can( 'manage_options' )` | Admins need full wp-admin access |

### REST API

The WordPress REST API is **not affected** by admin blocking. REST requests do not go through `admin_init` (they use `rest_api_init` instead), so no exemption is needed.

### Implementation

The blocking function is `rondo_block_wp_admin()` in `functions.php`, hooked to `admin_init`.
```
  </action>
  <verify>
1. Read the updated file and verify the new section is present between "User Roles" and "Security Considerations".
2. Verify the markdown renders correctly (no broken tables or formatting).
  </verify>
  <done>
Developer documentation at features/access-control.md includes a "WP Admin Blocking" section explaining the redirect behavior, exemptions table, and REST API note.
  </done>
</task>

</tasks>

<verification>
1. `rondo_block_wp_admin()` function exists in functions.php and is hooked to `admin_init`
2. The function checks `wp_doing_ajax()`, `WP_CLI`, `DOING_CRON`, and `manage_options` before redirecting
3. The function calls `wp_safe_redirect( home_url( '/' ) )` and `exit` for non-exempt users
4. Developer documentation describes the WP admin blocking behavior
5. `npm run build` succeeds
6. `npm run lint` passes with 0 warnings
</verification>

<success_criteria>
- Non-admin users hitting /wp-admin/ are redirected to home page
- AJAX, WP-CLI, and cron requests continue working for all users
- Administrators reach wp-admin normally
- Developer documentation updated
- Build passes, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/203-wp-admin-blocking/203-01-SUMMARY.md`
</output>
