---
phase: 103-teams-commissies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/VisibilitySelector.jsx
autonomous: true

must_haves:
  truths:
    - "VisibilitySelector shows 'Zichtbaarheid' as label"
    - "Private option displays as 'Prive' with Dutch description"
    - "Workspace option shows Dutch description 'Deel met workspace-leden'"
    - "Workspace selection dialog shows Dutch text"
  artifacts:
    - path: "src/components/VisibilitySelector.jsx"
      provides: "Dutch visibility options"
      contains: "Zichtbaarheid"
  key_links:
    - from: "src/components/VisibilitySelector.jsx"
      to: "TeamEditModal.jsx, CommissieEditModal.jsx, PersonEditModal.jsx"
      via: "import and usage"
      pattern: "VisibilitySelector"
---

<objective>
Translate the shared VisibilitySelector component to Dutch.

Purpose: VisibilitySelector is used across Teams, Commissies, and People forms. Translating it FIRST ensures all entity forms automatically get Dutch visibility options without duplicating translation work.

Output: Fully translated VisibilitySelector.jsx with Dutch labels for visibility options, workspace selection, and summary text.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-teams-commissies/103-CONTEXT.md
@.planning/phases/103-teams-commissies/103-RESEARCH.md
@src/components/VisibilitySelector.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate VisibilitySelector.jsx to Dutch</name>
  <files>src/components/VisibilitySelector.jsx</files>
  <action>
Translate all English strings to Dutch in VisibilitySelector.jsx following these mappings from RESEARCH.md:

**Visibility label:**
- "Visibility" -> "Zichtbaarheid"

**Visibility options (per CONTEXT.md):**
- "Private" -> "Prive"
- "Only you can see this" -> "Alleen jij kunt dit zien"
- "Workspace" -> "Workspace" (keep English per CONTEXT.md)
- "Share with workspace members" -> "Deel met workspace-leden"

**Workspace selection:**
- "Select Workspaces" -> "Selecteer workspaces"
- "Loading workspaces..." -> "Workspaces laden..."
- "No workspaces available. Create one first." -> "Geen workspaces beschikbaar. Maak er eerst een aan."
- "members" -> "leden"

**Summary:**
- "Shared with {n} workspace(s)" -> "Gedeeld met {n} workspace(s)"

**Button:**
- "Done" -> "Klaar"

Maintain the existing component structure and logic - only change string literals.
  </action>
  <verify>
1. Run `npm run lint` - no errors
2. Run `npm run build` - build succeeds
3. Search file for remaining English: `grep -E "Private|Only you can|Select Workspaces|Loading workspaces|Done" src/components/VisibilitySelector.jsx` - should return nothing
  </verify>
  <done>
VisibilitySelector.jsx contains only Dutch strings for visibility options, workspace selection, and summary text. Build and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build and deploy</name>
  <files>dist/</files>
  <action>
Run production build and deploy to production server:

1. `npm run build` - generate production assets
2. `bin/deploy.sh` - deploy to production

This ensures the shared component changes are live for verification.
  </action>
  <verify>
1. Build completes without errors
2. Deploy script completes successfully
  </verify>
  <done>
Production build generated and deployed. VisibilitySelector changes are live.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run lint` - passes
2. Run `npm run build` - passes
3. No English visibility strings remain in VisibilitySelector.jsx
4. Production deployment successful
</verification>

<success_criteria>
- VisibilitySelector.jsx fully translated to Dutch
- All visibility options show Dutch labels
- Workspace selection shows Dutch text
- Build and lint pass
- Changes deployed to production
</success_criteria>

<output>
After completion, create `.planning/phases/103-teams-commissies/103-01-SUMMARY.md`
</output>
