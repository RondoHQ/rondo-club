---
phase: 185-invoice-pdf-club-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/class-finance-config.php
  - includes/class-rest-api.php
  - includes/class-invoice-pdf-generator.php
  - src/pages/Finance/FinanceSettings.jsx
  - src/api/client.js
  - style.css
  - package.json
  - CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "Invoice PDF header shows club's own uploaded logo instead of Rondo platform logo"
    - "Invoice PDF accent colors (header border, org name, h1 FACTUUR, h2 Betaalgegevens) use the club's configured accent color"
    - "Admin can upload a club logo image via Finance Settings Organisatiegegevens section"
    - "Admin can pick an accent color via Finance Settings Organisatiegegevens section"
    - "If no club logo is configured, PDF falls back to the Rondo logo; if no accent color is set, falls back to #0891b2"
  artifacts:
    - path: "includes/class-finance-config.php"
      provides: "club_logo_id and accent_color option storage and retrieval"
      contains: "OPTION_CLUB_LOGO_ID"
    - path: "includes/class-invoice-pdf-generator.php"
      provides: "Dynamic logo and accent color in PDF HTML template"
      contains: "accent_color"
    - path: "src/pages/Finance/FinanceSettings.jsx"
      provides: "Logo upload and color picker UI in Organisatiegegevens section"
      contains: "accent_color"
  key_links:
    - from: "src/pages/Finance/FinanceSettings.jsx"
      to: "/rondo/v1/finance/settings"
      via: "updateFinanceSettings sends club_logo_id and accent_color"
      pattern: "club_logo_id|accent_color"
    - from: "includes/class-invoice-pdf-generator.php"
      to: "includes/class-finance-config.php"
      via: "get_club_logo_id() and get_accent_color() calls"
      pattern: "get_club_logo_id|get_accent_color"
---

<objective>
Add club branding settings (logo upload + accent color) to Finance Settings and use them in the Invoice PDF generator, replacing hardcoded Rondo branding.

Purpose: Invoices should represent the club's identity, not the Rondo platform. Clubs need their own logo and colors on official financial documents.
Output: Club logo and accent color configurable in settings, applied to invoice PDFs.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@includes/class-finance-config.php
@includes/class-invoice-pdf-generator.php
@includes/class-rest-api.php
@src/pages/Finance/FinanceSettings.jsx
@src/hooks/useFinanceSettings.js
@src/api/client.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add club_logo_id and accent_color to FinanceConfig + REST API + PDF generator</name>
  <files>
    includes/class-finance-config.php
    includes/class-rest-api.php
    includes/class-invoice-pdf-generator.php
  </files>
  <action>
**FinanceConfig (includes/class-finance-config.php):**

1. Add two new option constants:
   - `OPTION_CLUB_LOGO_ID = 'rondo_finance_club_logo_id'` (stores WordPress attachment ID, integer)
   - `OPTION_ACCENT_COLOR = 'rondo_finance_accent_color'` (stores hex color string like `#0891b2`)

2. Add defaults: `club_logo_id => 0`, `accent_color => ''` (empty means use default #0891b2)

3. Add getter methods:
   - `get_club_logo_id(): int` — returns `(int) get_option(OPTION_CLUB_LOGO_ID, 0)`
   - `get_accent_color(): string` — returns `get_option(OPTION_ACCENT_COLOR, '')` (empty = default)

4. In `get_all_settings()` add:
   - `'club_logo_id' => $this->get_club_logo_id()`
   - `'club_logo_url' => ''` — resolve: if `club_logo_id > 0`, call `wp_get_attachment_url($id)` and return URL, else empty string. This lets the frontend display the current logo without needing a separate endpoint.
   - `'accent_color' => $this->get_accent_color()`

5. In `update_settings()` add handlers:
   - `club_logo_id`: `absint($data['club_logo_id'])` then `update_option(OPTION_CLUB_LOGO_ID, ...)`
   - `accent_color`: sanitize with `sanitize_hex_color($data['accent_color'])`, only store if valid hex or empty string (empty to reset to default). Use WordPress's `sanitize_hex_color()` function.

6. In `get_setting()` switch, add cases for `'club_logo_id'` and `'accent_color'`.

**REST API (includes/class-rest-api.php):**

No route changes needed — the existing GET/POST `/rondo/v1/finance/settings` already passes through `get_all_settings()` and `update_settings()`. The new fields flow automatically through the existing FinanceConfig methods.

**InvoicePdfGenerator (includes/class-invoice-pdf-generator.php):**

1. In `generate()` method, after the existing `$finance_config` instantiation (~line 104), add:
   - `$accent_color = $finance_config->get_accent_color();`
   - `if (empty($accent_color)) { $accent_color = '#0891b2'; }` (default fallback)
   - `$club_logo_id = $finance_config->get_club_logo_id();`

2. Replace the hardcoded logo path logic (lines 111-113):
   ```php
   if ($club_logo_id > 0) {
       $logo_path = get_attached_file($club_logo_id);
       $logo_exists = $logo_path && file_exists($logo_path);
   } else {
       $logo_path = get_template_directory() . '/public/icons/rondo-logo.png';
       $logo_exists = file_exists($logo_path);
   }
   ```

3. Pass `$accent_color` as a new parameter to `build_html()`.

4. In `build_html()`:
   - Add `$accent_color` parameter (after `$qr_code_path`)
   - Replace all 4 occurrences of `#0891b2` with `{$accent_color}`:
     - `.header` border-bottom: `2px solid #0891b2` -> `2px solid {$accent_color}`
     - `.header .org-name` color: `#0891b2` -> `{$accent_color}`
     - `h1` color: `#0891b2` -> `{$accent_color}`
     - `.payment-section h2` color: `#0891b2` -> `{$accent_color}`
   - Use `esc_attr($accent_color)` in the CSS output to prevent injection.
  </action>
  <verify>
1. Run `npm run build` to ensure no PHP syntax errors affect the build pipeline.
2. Grep for `#0891b2` in `class-invoice-pdf-generator.php` — should find ZERO occurrences (all replaced with dynamic color).
3. Grep for `OPTION_CLUB_LOGO_ID` and `OPTION_ACCENT_COLOR` in `class-finance-config.php` — should exist.
4. Grep for `accent_color` and `club_logo_id` in `class-finance-config.php` `get_all_settings` method — should exist.
  </verify>
  <done>
FinanceConfig stores and retrieves club_logo_id (attachment ID) and accent_color (hex). REST API exposes them via existing settings endpoint. InvoicePdfGenerator uses club logo (with Rondo fallback) and dynamic accent color (with #0891b2 fallback) for all branded elements.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add logo upload and color picker to Finance Settings UI + version bump</name>
  <files>
    src/pages/Finance/FinanceSettings.jsx
    src/api/client.js
    style.css
    package.json
    CHANGELOG.md
  </files>
  <action>
**FinanceSettings.jsx (src/pages/Finance/FinanceSettings.jsx):**

Add two new fields to the Organisatiegegevens section (Section 1), after the E-mailadres field:

1. **Club Logo Upload:**
   - Add `club_logo_id` and `club_logo_url` to formData state (default: `0` and `''`)
   - In the useEffect that loads settings, add: `club_logo_id: settings.club_logo_id || 0`, `club_logo_url: settings.club_logo_url || ''`
   - UI: A "Clublogo" label with:
     - If `club_logo_url` is set: show a small preview image (max-height 60px) with a "Verwijderen" (remove) button next to it
     - A file input (`<input type="file" accept="image/*">`) for uploading
   - Upload handler: When file selected, upload to WordPress media library using the standard WP REST media endpoint: `POST /wp/v2/media` with FormData containing the file. Get back the attachment ID and URL from the response. Set `club_logo_id` to the attachment ID and `club_logo_url` to the source URL.
   - Remove handler: Set `club_logo_id` to `0` and `club_logo_url` to `''`.
   - In the submit payload, include `club_logo_id: formData.club_logo_id`.
   - For the media upload, use the existing `api` instance from `@/api/client.js` (the WordPress REST API client with nonce). Import it: the file already imports `prmApi`, also import the base `api` client. The upload call: `const response = await api.post('/wp/v2/media', formData_upload, { headers: { 'Content-Type': 'multipart/form-data' } })`. The response contains `response.data.id` (attachment ID) and `response.data.source_url` (full URL).

2. **Accent Color Picker:**
   - Add `accent_color` to formData state (default: `''`)
   - In the useEffect, add: `accent_color: settings.accent_color || ''`
   - UI: A "Accentkleur" label with:
     - A native HTML color input (`<input type="color">`) next to a text input showing the hex value
     - Default value when empty: `#0891b2` (show as placeholder in text input, set as defaultValue on color input)
     - A "Reset naar standaard" (reset to default) small link/button that clears the value back to empty
   - In submit payload, include `accent_color: formData.accent_color`.
   - The color input and text input should be synced: changing one updates the other.

3. **Import the base `api` client:**
   - In `src/api/client.js`, check if the base WordPress API client is exported. It is — the file exports both `api` (WordPress /wp/v2/) and `prmApi` (/rondo/v1/). Add `api` to the import statement in FinanceSettings.jsx.

**Submit handler changes:**
   - In `handleSubmit`, add `club_logo_id` and `accent_color` to the payload object.

**API client (src/api/client.js):**
   - No changes needed — `updateFinanceSettings` already passes all data through to the POST endpoint, and the media upload uses the base `api` client directly.

**Version bump:**
   - `style.css`: Update `Version: 26.0.0` to `Version: 26.1.0`
   - `package.json`: Update `"version": "26.0.0"` to `"version": "26.1.0"`

**CHANGELOG.md:**
   - Add entry under a new `## [26.1.0]` heading (or add to existing if one exists):
     ```
     ### Added
     - Club branding settings: logo upload and accent color in Finance Settings
     - Invoice PDFs now use club's own logo and accent color instead of Rondo branding
     ```
  </action>
  <verify>
1. Run `npm run build` — must succeed with zero errors.
2. Run `npm run lint` — must pass (max-warnings: 0). Pay attention to unused imports.
3. Check that `club_logo_id`, `club_logo_url`, and `accent_color` appear in the FinanceSettings form state initialization.
4. Check that the CHANGELOG has the 26.1.0 entry.
5. Check that both `style.css` and `package.json` show version 26.1.0.
  </verify>
  <done>
Finance Settings page shows club logo upload (with preview/remove) and accent color picker (with live hex value and reset) in the Organisatiegegevens section. Version bumped to 26.1.0. CHANGELOG updated.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes — frontend compiles without errors
2. `npm run lint` passes — no lint warnings/errors
3. No hardcoded `#0891b2` remains in `class-invoice-pdf-generator.php` (all replaced with dynamic accent color)
4. FinanceConfig has `get_club_logo_id()` and `get_accent_color()` methods
5. REST API returns `club_logo_id`, `club_logo_url`, and `accent_color` in GET /rondo/v1/finance/settings
6. FinanceSettings UI shows logo upload field and color picker in Organisatiegegevens section
7. Version is 26.1.0 in both style.css and package.json
8. CHANGELOG has 26.1.0 entry
</verification>

<success_criteria>
- Invoice PDF uses club's configured logo (falling back to Rondo logo if none set)
- Invoice PDF uses club's configured accent color for all branded elements (falling back to #0891b2 if none set)
- Admin can upload and remove a club logo in Finance Settings
- Admin can set and reset an accent color in Finance Settings
- All changes build and lint cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/185-invoice-pdf-club-branding/185-01-SUMMARY.md`
</output>
