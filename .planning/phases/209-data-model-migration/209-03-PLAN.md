---
phase: 209-data-model-migration
plan: 03
type: execute
wave: 3
depends_on: ["209-02"]
files_modified:
  - acf-json/group_person_fields.json
  - src/pages/People/PersonDetail.jsx
  - src/pages/People/PeopleList.jsx
  - src/utils/formatters.js
  - src/components/ContactEditModal.jsx
  - src/components/PersonEditModal.jsx
  - src/components/AccountCard.jsx
  - src/utils/vcard.js
  - src/utils/socialIcons.js
  - src/pages/VOG/VOGList.jsx
  - src/pages/VOG/VOGUpcoming.jsx
  - src/hooks/usePeople.js
  - includes/class-rest-api.php
autonomous: true
requirements: [DATA-04]

must_haves:
  truths:
    - "Frontend reads contact fields directly from person.acf.email_1 etc. instead of contact_info array"
    - "The legacy contact_info repeater field group no longer exists in ACF JSON"
    - "REST API no longer returns the backward-compatible contact_info array"
    - "ContactEditModal is replaced with a simple fixed-field form (no dynamic repeater)"
  artifacts:
    - path: "acf-json/group_person_fields.json"
      provides: "Person fields WITHOUT contact_info repeater"
    - path: "src/pages/People/PersonDetail.jsx"
      provides: "Person detail reading from fixed contact fields"
      contains: "email_1"
    - path: "src/utils/formatters.js"
      provides: "Formatters without normalizeContactInfo and without contact_info in repeaterFields"
  key_links:
    - from: "src/pages/People/PersonDetail.jsx"
      to: "person.acf.email_1"
      via: "direct ACF field access"
      pattern: "acf\\.email_1|acf\\['email_1'\\]"
    - from: "src/utils/formatters.js"
      to: "sanitizePersonAcf"
      via: "contact_info removed from repeaterFields"
      pattern: "repeaterFields"
---

<objective>
Update all frontend code to read contact data from the new fixed ACF fields directly, remove the backward-compatible contact_info array from the REST API, remove the legacy contact_info repeater from the ACF field group, and clean up all related frontend utilities (normalizeContactInfo, socialIcons, ContactEditModal repeater form).

Purpose: Complete the migration by removing all traces of the contact_info repeater pattern.
Output: Clean frontend using fixed fields, no legacy ACF fields, no backward-compatibility layer.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-data-model-migration/209-01-SUMMARY.md
@.planning/phases/209-data-model-migration/209-02-SUMMARY.md
</context>

<interfaces>
<!-- Fixed ACF fields available on person.acf in REST response -->
person.acf.email_1      — Primary email
person.acf.email_2      — Secondary email
person.acf.mobile_1     — Primary mobile
person.acf.mobile_2     — Secondary mobile
person.acf.telephone_1  — Primary telephone
person.acf.telephone_2  — Secondary telephone

<!-- These replace the old person.acf.contact_info[] array -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend to use fixed contact fields</name>
  <files>
    src/pages/People/PersonDetail.jsx
    src/pages/People/PeopleList.jsx
    src/utils/formatters.js
    src/components/ContactEditModal.jsx
    src/components/PersonEditModal.jsx
    src/components/AccountCard.jsx
    src/utils/vcard.js
    src/utils/socialIcons.js
    src/pages/VOG/VOGList.jsx
    src/pages/VOG/VOGUpcoming.jsx
    src/hooks/usePeople.js
  </files>
  <action>
**1. src/utils/formatters.js:**
- Remove `normalizeContactInfo` function entirely
- Remove `contact_info` from the `repeaterFields` array in `sanitizePersonAcf`
- Add the 6 fixed field names to sanitizePersonAcf as regular text fields (not repeaters)

**2. src/pages/People/PersonDetail.jsx:**
- Replace `normalizeContactInfo(acf.contact_info || [])` with direct reads: `acf.email_1`, `acf.email_2`, `acf.mobile_1`, `acf.mobile_2`, `acf.telephone_1`, `acf.telephone_2`
- Update the contact info display section to show fixed fields as a simple list instead of mapping over a contact_info array
- Update the social links extraction: remove social link types entirely (LinkedIn, Bluesky, etc. are dropped per requirements). Keep WhatsApp (built from mobile_1), Sportlink, and FreeScout links.
- Update the save handler: instead of saving `contact_info: normalizedContacts`, save individual fixed fields: `email_1: value, email_2: value, ...`
- Update the ContactEditModal integration: pass individual field values instead of contact_info array
- Remove the `normalizeContactInfo` import

**3. src/components/ContactEditModal.jsx:**
- Rewrite from a dynamic repeater form (useFieldArray) to a simple form with 6 fixed fields
- Props change from `contactInfo=[]` to individual field props: `email1, email2, mobile1, mobile2, telephone1, telephone2`
- Form has 6 text inputs with appropriate labels: "Email", "Email (2e)", "Mobiel", "Mobiel (2e)", "Telefoon", "Telefoon (2e)"
- onSubmit returns an object `{ email_1, email_2, mobile_1, mobile_2, telephone_1, telephone_2 }` instead of a contacts array
- Remove all contact_type select dropdown logic
- Remove the "Add Contact Method" button and useFieldArray

**4. src/pages/People/PeopleList.jsx:**
- Replace `person.acf?.contact_info || []` reads with direct field access
- Email column: show `person.acf?.email_1`
- Phone column: show `person.acf?.mobile_1 || person.acf?.telephone_1`

**5. src/components/AccountCard.jsx:**
- Replace any contact_info reads with direct field access

**6. src/components/PersonEditModal.jsx:**
- If it references contact_info, update to use fixed fields

**7. src/utils/vcard.js:**
- Update vCard generation to read from fixed fields instead of contact_info array

**8. src/utils/socialIcons.js:**
- Remove social link type constants that are no longer used (LinkedIn, Twitter, Bluesky, Threads, Instagram, Facebook)
- Keep only WhatsApp, Sportlink, FreeScout icon/color definitions

**9. src/pages/VOG/VOGList.jsx and VOGUpcoming.jsx:**
- Replace contact_info email/phone reads with direct field access

**10. src/hooks/usePeople.js:**
- If it references contact_info, update accordingly

Run `npm run lint` and `npm run build` to verify no errors.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All frontend files read from fixed contact fields. No references to contact_info in person-related frontend code. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy ACF fields and backward-compatibility layer</name>
  <files>
    acf-json/group_person_fields.json
    includes/class-rest-api.php
  </files>
  <action>
**1. acf-json/group_person_fields.json:**
- Remove the entire `contact_info` repeater field (key: `field_contact_info`) and its sub_fields (`field_contact_type`, `field_contact_label`, `field_contact_value`)
- Keep the 6 new fixed fields in place
- Verify the JSON is valid after removal

**2. includes/class-rest-api.php:**
- Remove the `build_contact_info_from_fixed_fields()` backward-compatibility method added in Plan 02
- Remove the `contact_info` key from the REST API person response
- The REST response should now include the 6 fixed fields directly as part of the `acf` object (which ACF does automatically for registered fields)

**3. Deploy to production:**
- Run `npm run build` first
- Deploy via `bin/deploy.sh`
- Verify the site loads correctly
- Verify person detail page shows contact info from fixed fields

**4. Cleanup old repeater meta from database (optional, via WP-CLI):**
- This is NOT required for functionality — WordPress ignores orphaned meta
- If time permits, create a simple WP-CLI command or run a query to clean up old `contact_info` repeater meta rows
- This is cosmetic cleanup and should NOT block completion

**5. Update version and changelog:**
- Bump version to 31.0.0 in `style.css` and `package.json`
- Add CHANGELOG.md entry for v31.0.0 under "Changed": "Replaced contact_info repeater with 6 fixed contact fields (email_1, email_2, mobile_1, mobile_2, telephone_1, telephone_2)"
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && node -e "const f=require('./acf-json/group_person_fields.json'); const has=f.fields.some(f=>f.name==='contact_info'); console.log(has ? 'FAIL: contact_info still exists' : 'PASS: contact_info removed')"</automated>
  </verify>
  <done>Legacy contact_info repeater removed from ACF JSON. Backward-compatibility layer removed from REST API. Site deployed and working. Version bumped to 31.0.0 with changelog entry.</done>
</task>

</tasks>

<verification>
- `grep -r "contact_info" src/ | grep -v node_modules | grep -v "// Legacy"` returns zero person-related results (team/commissie contact_info is separate and untouched)
- `grep "contact_info" acf-json/group_person_fields.json` returns zero results
- `npm run build` succeeds
- `npm run lint` passes
- Production site loads, person detail shows contact fields correctly
</verification>

<success_criteria>
- No contact_info repeater in ACF field group for person
- No contact_info references in person-related frontend code
- No backward-compatibility layer in REST API
- Frontend reads directly from fixed ACF fields
- Build and lint pass
- Production deployed and working
- Version 31.0.0 with changelog
</success_criteria>

<output>
After completion, create `.planning/phases/209-data-model-migration/209-03-SUMMARY.md`
</output>
