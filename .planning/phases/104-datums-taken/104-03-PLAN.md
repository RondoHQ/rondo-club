---
phase: 104-datums-taken
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Timeline/TodoModal.jsx
  - src/components/Timeline/GlobalTodoModal.jsx
autonomous: true

must_haves:
  truths:
    - "Todo modal shows 'Taak toevoegen', 'Taak bekijken', or 'Taak bewerken' header"
    - "Todo modal shows Dutch form labels (Beschrijving, Deadline, Notities)"
    - "Todo modal shows 'Gerelateerde personen' section header"
    - "Todo modal shows 'Lid toevoegen' link for adding people"
    - "Global todo modal shows 'Leden *' label for people selector"
    - "Global todo modal shows 'Taak toevoegen' header and button"
    - "Both modals show Dutch placeholders (Wat moet er gedaan worden?)"
    - "Both modals show Dutch button labels (Annuleren, Opslaan)"
    - "Search placeholder shows 'Leden zoeken...' instead of 'Search people...'"
  artifacts:
    - path: "src/components/Timeline/TodoModal.jsx"
      provides: "Dutch todo view/edit modal"
      contains: "Taak bewerken"
    - path: "src/components/Timeline/GlobalTodoModal.jsx"
      provides: "Dutch global todo create modal"
      contains: "Taak toevoegen"
  key_links:
    - from: "src/pages/Todos/TodosList.jsx"
      to: "TodoModal.jsx"
      via: "edit modal trigger"
      pattern: "TodoModal"
    - from: "src/pages/Todos/TodosList.jsx"
      to: "GlobalTodoModal.jsx"
      via: "add modal trigger"
      pattern: "GlobalTodoModal"
---

<objective>
Translate Todo modals (view/edit and global create) to Dutch.

Purpose: Complete Dutch localization of the Todo modal components, including form labels, placeholders, buttons, and status hints.

Output: Fully translated TodoModal.jsx and GlobalTodoModal.jsx with Dutch labels, placeholders, and terminology consistent with CONTEXT.md.
</objective>

<execution_context>
@/Users/joostdevolk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevolk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-datums-taken/104-CONTEXT.md
@.planning/phases/104-datums-taken/104-RESEARCH.md
@src/components/Timeline/TodoModal.jsx
@src/components/Timeline/GlobalTodoModal.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate TodoModal.jsx to Dutch</name>
  <files>src/components/Timeline/TodoModal.jsx</files>
  <action>
Translate all English strings to Dutch in TodoModal.jsx following RESEARCH.md and CONTEXT.md mappings:

**getModalTitle function:**
- 'Add todo' -> 'Taak toevoegen'
- 'View todo' -> 'Taak bekijken'
- 'Edit todo' -> 'Taak bewerken'

**View mode labels:**
- "Description" -> "Beschrijving"
- "Due date" -> "Deadline" (per CONTEXT.md)
- "No due date" -> "Geen deadline"
- "Notes" -> "Notities"
- "Related people" -> "Gerelateerde personen"

**View mode buttons:**
- "Close" -> "Sluiten"
- "Edit" -> "Bewerken"

**Edit mode labels:**
- "Description" label -> "Beschrijving"
- "Due date (optional)" -> "Deadline (optioneel)"
- "Notes (optional)" -> "Notities (optioneel)"
- "Related people" -> "Gerelateerde personen"

**Edit mode placeholders:**
- "What needs to be done?" -> "Wat moet er gedaan worden?"
- "Add detailed notes..." -> "Voeg gedetailleerde notities toe..."
- "Search people..." -> "Leden zoeken..."

**Edit mode - Add person button:**
- "Add person" -> "Lid toevoegen"

**Edit mode - Loading/empty states in dropdown:**
- "Loading..." -> "Laden..."
- "No people found" -> "Geen leden gevonden"

**Status hint (for existing todos):**
- "Tip: Use the status buttons on the todo list to change between Open, Awaiting, and Completed."
  -> "Tip: Gebruik de statusknoppen in de takenlijst om te wisselen tussen Te doen, Openstaand en Afgerond."

**Buttons:**
- "Cancel" -> "Annuleren"
- "Saving..." -> "Opslaan..."
- "Adding..." -> "Toevoegen..."
- "Save" -> "Opslaan"
- "Add todo" -> "Taak toevoegen"
  </action>
  <verify>
1. Run `npm run lint` - no errors
2. Search for remaining English: `grep -E "Add todo|Edit todo|View todo|Description|Due date|Related people|Add person|Search people|Cancel|Saving\.\.\.|No people found" src/components/Timeline/TodoModal.jsx` - should return only translated versions
  </verify>
  <done>
TodoModal.jsx contains Dutch modal headers, view/edit mode labels, placeholders, status hint using CONTEXT.md terminology, and button labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Translate GlobalTodoModal.jsx to Dutch</name>
  <files>src/components/Timeline/GlobalTodoModal.jsx</files>
  <action>
Translate all English strings to Dutch in GlobalTodoModal.jsx following RESEARCH.md and CONTEXT.md mappings:

**Modal header:**
- "Add todo" -> "Taak toevoegen"

**Form labels:**
- "People *" (with required asterisk) -> "Leden *"
- "Description *" -> "Beschrijving *"
- "Due date (optional)" -> "Deadline (optioneel)"
- "Notes (optional)" -> "Notities (optioneel)"

**Add person button:**
- "Add person" -> "Lid toevoegen"

**Placeholders:**
- "Search people..." -> "Leden zoeken..."
- "What needs to be done?" -> "Wat moet er gedaan worden?"
- "Add detailed notes..." -> "Voeg gedetailleerde notities toe..."

**Loading/empty states in dropdown:**
- "Loading..." -> "Laden..."
- "No people found" -> "Geen leden gevonden"

**Buttons:**
- "Cancel" -> "Annuleren"
- "Adding..." -> "Toevoegen..."
- "Add todo" -> "Taak toevoegen"
  </action>
  <verify>
1. Run `npm run lint` - no errors
2. Search for remaining English: `grep -E "Add todo|People \*|Description|Due date|Add person|Search people|Cancel|Adding\.\.\.|No people found" src/components/Timeline/GlobalTodoModal.jsx` - should return only translated versions
  </verify>
  <done>
GlobalTodoModal.jsx contains Dutch modal header, form labels using Dutch terminology (Leden instead of People), placeholders, and button labels.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run lint` - passes
2. Run `npm run build` - passes
3. Click "Taak toevoegen" on todos page - GlobalTodoModal shows Dutch header "Taak toevoegen"
4. GlobalTodoModal shows "Leden *" for people selector
5. Click edit on existing todo - TodoModal shows "Taak bekijken" header in view mode
6. Click Edit button - TodoModal shows "Taak bewerken" header
7. Form labels show Dutch (Beschrijving, Deadline, Notities)
8. People selector shows "Lid toevoegen" and "Leden zoeken..."
9. Status hint shows Dutch terminology (Te doen, Openstaand, Afgerond)
</verification>

<success_criteria>
- TodoModal.jsx fully translated with Dutch view/edit mode labels, placeholders, and buttons
- GlobalTodoModal.jsx fully translated with Dutch labels (Leden instead of People)
- Both modals use consistent terminology from CONTEXT.md
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/104-datums-taken/104-03-SUMMARY.md`
</output>
