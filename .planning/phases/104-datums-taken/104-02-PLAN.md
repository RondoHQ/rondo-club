---
phase: 104-datums-taken
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Todos/TodosList.jsx
  - src/components/Timeline/CompleteTodoModal.jsx
autonomous: true

must_haves:
  truths:
    - "Todos page title shows 'Taken' instead of 'Todos'"
    - "Filter tabs show Dutch labels (Te doen, Openstaand, Afgerond, Alle)"
    - "Header text shows Dutch (Alle taken, Te doen, Openstaand, Afgeronde taken)"
    - "Empty states show Dutch messages (Geen taken gevonden)"
    - "Todo action tooltips show Dutch (Taak heropenen, Taak bewerken, Taak verwijderen)"
    - "Due date shows 'Deadline:' instead of 'Due:'"
    - "Overdue indicator shows '(te laat)' instead of '(overdue)'"
    - "Complete modal shows 'Taak afronden' header"
    - "Complete modal shows 'Openstaand' option with Dutch description"
    - "Delete confirmation shows Dutch text"
  artifacts:
    - path: "src/pages/Todos/TodosList.jsx"
      provides: "Dutch todos list page"
      contains: "Taken"
    - path: "src/components/Timeline/CompleteTodoModal.jsx"
      provides: "Dutch complete todo modal"
      contains: "Taak afronden"
  key_links:
    - from: "src/pages/Todos/TodosList.jsx"
      to: "CompleteTodoModal.jsx"
      via: "modal trigger"
      pattern: "CompleteTodoModal"
---

<objective>
Translate Todos list page and complete todo modal to Dutch.

Purpose: Complete Dutch localization of the Todos list view including filter tabs, headers, empty states, action tooltips, and the complete todo flow modal.

Output: Fully translated TodosList.jsx and CompleteTodoModal.jsx with Dutch labels, status terms per CONTEXT.md (Te doen, Openstaand, Afgerond).
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-datums-taken/104-CONTEXT.md
@.planning/phases/104-datums-taken/104-RESEARCH.md
@src/pages/Todos/TodosList.jsx
@src/components/Timeline/CompleteTodoModal.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate TodosList.jsx to Dutch</name>
  <files>src/pages/Todos/TodosList.jsx</files>
  <action>
Translate all English strings to Dutch in TodosList.jsx following RESEARCH.md and CONTEXT.md mappings:

**Document title:**
- useDocumentTitle('Todos') -> useDocumentTitle('Taken')

**Page header:**
- `<h1>Todos</h1>` -> `<h1>Taken</h1>`
- "Add todo" button -> "Taak toevoegen"

**Filter tabs (per CONTEXT.md status labels):**
- "Open" -> "Te doen"
- "Awaiting" -> "Openstaand"
- "Completed" -> "Afgerond"
- "All" -> "Alle"

**getHeaderText function - update labels object:**
```jsx
const labels = {
  all: 'Alle taken',
  open: 'Te doen',
  awaiting: 'Openstaand',
  completed: 'Afgeronde taken',
};
return labels[statusFilter] || 'Taken';
```

**getEmptyMessage function - update messages object:**
```jsx
const messages = {
  all: 'Geen taken gevonden',
  open: 'Geen open taken',
  awaiting: 'Geen openstaande taken',
  completed: 'Geen afgeronde taken',
};
return messages[statusFilter] || 'Geen taken gevonden';
```

**Empty state hint (when statusFilter === 'open'):**
- `Create todos from a person's detail page or click "Add todo"` -> `Maak taken aan vanaf een ledenpagina of klik op "Taak toevoegen"`

**Delete confirmation dialog:**
- 'Are you sure you want to delete this todo?' -> 'Weet je zeker dat je deze taak wilt verwijderen?'

**TodoItem component - getToggleTitle function:**
- 'Reopen todo' -> 'Taak heropenen'
- 'Mark as complete' -> 'Markeren als afgerond'
- 'Complete todo' -> 'Taak afronden'

**TodoItem component - button titles:**
- title="Reopen todo" -> title="Taak heropenen"
- title="Edit todo" -> title="Taak bewerken"
- title="Delete todo" -> title="Taak verwijderen"

**TodoItem component - due date display:**
- "Due: {date}" -> "Deadline: {date}"
- "(overdue)" -> "(te laat)"

**TodoItem component - awaiting indicator:**
- 'Waiting since today' -> 'Wacht sinds vandaag'
- `Waiting ${awaitingDays}d` -> `Wacht ${awaitingDays}d`

**Error handling:**
- 'Failed to create activity. Please try again.' -> 'Activiteit kon niet worden aangemaakt. Probeer het opnieuw.'
  </action>
  <verify>
1. Run `npm run lint` - no errors
2. Search for remaining English: `grep -E "Add todo|Todos|Open|Awaiting|Completed|All todos|Due:|overdue|Reopen todo|Edit todo|Delete todo" src/pages/Todos/TodosList.jsx` - should return only translated versions or code comments
  </verify>
  <done>
TodosList.jsx contains Dutch page title, filter tabs using CONTEXT.md terminology (Te doen, Openstaand, Afgerond), Dutch headers, empty states, tooltips, and due date labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Translate CompleteTodoModal.jsx to Dutch</name>
  <files>src/components/Timeline/CompleteTodoModal.jsx</files>
  <action>
Translate all English strings to Dutch in CompleteTodoModal.jsx following RESEARCH.md and CONTEXT.md mappings:

**Modal header:**
- "Complete todo" -> "Taak afronden"

**Question text:**
- "What's the status of this todo?" -> "Wat is de status van deze taak?"

**Option 1: Awaiting (per CONTEXT.md: Openstaand)**
- "Awaiting response" -> "Openstaand"
- "You did your part, waiting for their reply" -> "Je hebt je deel gedaan, wachten op hun reactie"

**Option 2: Complete**
- "Complete" -> "Afronden"
- "Mark the todo as fully done" -> "Markeer de taak als volledig afgerond"

**Option 3: Complete & log activity**
- "Complete & log activity" -> "Afronden & activiteit loggen"
- "Record this as an activity on the timeline" -> "Leg dit vast als activiteit op de tijdlijn"

**Button:**
- "Cancel" -> "Annuleren"
  </action>
  <verify>
1. Run `npm run lint` - no errors
2. Search for remaining English: `grep -E "Complete todo|Awaiting response|Complete & log|Cancel" src/components/Timeline/CompleteTodoModal.jsx` - should return only translated versions
  </verify>
  <done>
CompleteTodoModal.jsx contains Dutch modal header, status options using CONTEXT.md terminology (Openstaand instead of Awaiting response), and Dutch button labels.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run lint` - passes
2. Run `npm run build` - passes
3. Navigate to /todos - page title shows "Taken"
4. Filter tabs show "Te doen", "Openstaand", "Afgerond", "Alle"
5. Empty state shows "Geen open taken" (or appropriate Dutch message per filter)
6. Hover over todo buttons shows Dutch tooltips
7. Click complete on an open todo - modal shows "Taak afronden" header
8. Modal options show "Openstaand" (not "Awaiting response")
9. Delete confirmation shows Dutch text
</verification>

<success_criteria>
- TodosList.jsx fully translated with Dutch page title, filter tabs, headers, empty states, tooltips
- Status terminology follows CONTEXT.md (Te doen, Openstaand, Afgerond)
- CompleteTodoModal.jsx fully translated with Dutch options and descriptions
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/104-datums-taken/104-02-SUMMARY.md`
</output>
