---
phase: 207-in-app-profile-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/class-rest-api.php
  - src/api/client.js
  - src/hooks/useCurrentUser.js
  - src/pages/Profile/Profile.jsx
  - src/lazyPages.js
  - src/router.jsx
  - src/components/layout/Layout.jsx
  - ../developer/src/content/docs/api/rest-api.md
  - CHANGELOG.md
  - style.css
  - package.json
autonomous: true

must_haves:
  truths:
    - "User navigates to /profile and sees their linked Sportlink name and active Functies"
    - "User enters current + new password and submits; password changes"
    - "If current password is wrong, form shows a clear Dutch error message"
    - "After successful password change, user is redirected to the login page (session invalidated)"
    - "Demo user cannot see the password change form"
    - "Non-admin UserMenu 'Profiel bewerken' links to in-app /profile instead of wp-admin"
  artifacts:
    - path: "src/pages/Profile/Profile.jsx"
      provides: "Profile page with identity display + password form"
      min_lines: 50
    - path: "includes/class-rest-api.php"
      provides: "POST /rondo/v1/user/password endpoint + expanded GET /user/me"
      contains: "change_password"
    - path: "src/hooks/useCurrentUser.js"
      provides: "useChangePassword mutation hook"
      contains: "useChangePassword"
  key_links:
    - from: "src/pages/Profile/Profile.jsx"
      to: "src/hooks/useCurrentUser.js"
      via: "useCurrentUser() for identity display + useChangePassword() for mutation"
      pattern: "useCurrentUser|useChangePassword"
    - from: "src/hooks/useCurrentUser.js"
      to: "src/api/client.js"
      via: "prmApi.changePassword()"
      pattern: "prmApi\\.changePassword"
    - from: "src/api/client.js"
      to: "includes/class-rest-api.php"
      via: "POST /rondo/v1/user/password"
      pattern: "/rondo/v1/user/password"
    - from: "src/components/layout/Layout.jsx"
      to: "src/pages/Profile/Profile.jsx"
      via: "UserMenu Link to /profile"
      pattern: "to=\"/profile\""
---

<objective>
Add an in-app Profile page where users see their linked Sportlink identity and can change their password without visiting wp-login.php.

Purpose: Completes the SPA experience for non-admin users by removing the last reason to visit wp-admin (password management). Satisfies PROF-01 through PROF-04.

Output: POST /rondo/v1/user/password endpoint, expanded GET /user/me response, Profile.jsx page, sidebar + UserMenu integration, version 29.4.0.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/207-in-app-profile-page/207-RESEARCH.md
@.planning/phases/205-user-provisioning/205-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend password endpoint + expanded /user/me</name>
  <files>includes/class-rest-api.php, ../developer/src/content/docs/api/rest-api.md</files>
  <action>
In `includes/class-rest-api.php`:

**1. Register the POST /rondo/v1/user/password endpoint in `register_routes()`**, right after the existing `/user/me` route (line ~359). Use `\WP_REST_Server::CREATABLE`, `permission_callback => is_user_logged_in()`, with args:
- `current_password`: required, type string, `sanitize_callback` that returns `$v` unchanged (NEVER sanitize passwords)
- `new_password`: required, type string, `sanitize_callback` that returns `$v` unchanged

**2. Add the `change_password( $request )` method** near `get_current_user()` (around line 2707):
- Get `$user_id = get_current_user_id()` and `$user = get_userdata( $user_id )`
- **Demo guard:** If `$user->user_login === 'demo'`, return `new \WP_Error( 'demo_user', 'Wachtwoord wijzigen is niet beschikbaar in de demo.', [ 'status' => 403 ] )`
- Verify current password: `wp_check_password( $current_password, $user->user_pass, $user_id )`. On failure return `new \WP_Error( 'wrong_password', 'Huidig wachtwoord is onjuist.', [ 'status' => 400 ] )`
- Change password: `wp_set_password( $new_password, $user_id )`
- Destroy ALL sessions: `$sessions = \WP_Session_Tokens::get_instance( $user_id ); $sessions->destroy_all();`
- Return `rest_ensure_response( [ 'success' => true, 'message' => 'Wachtwoord succesvol gewijzigd. Log opnieuw in.' ] )`

**3. Expand the `get_current_user()` response** (line ~2693):
- Before the return statement, read `$person_id = (int) get_user_meta( $user_id, 'rondo_linked_person_id', true );`
- Initialize `$linked_person_name = null;` and `$active_functies = [];`
- If `$person_id` is truthy, get the person post. If valid person post type:
  - Build `$linked_person_name` from `get_field('first_name', $person_id)` and `get_field('last_name', $person_id)` with `trim()`, defaulting to null if empty
  - Read `$work_history = get_field( 'work_history', $person_id ) ?: [];` and loop to extract `$job['job_title']` where `$job['is_current']` is truthy
- Add `'linked_person_name' => $linked_person_name,` and `'active_functies' => $active_functies,` to the response array

**NOTE:** The get_users() method (line ~2717) already has very similar person-name-from-linked-id logic. Reuse the exact same pattern (first + ' ' + last, trim).

**4. Update developer docs** at `../developer/src/content/docs/api/rest-api.md`:
- Update the `GET /rondo/v1/user/me` response example to include `linked_person_name` and `active_functies` fields
- Add a new section for `POST /rondo/v1/user/password` with request body (`current_password`, `new_password`), success response, and error codes (400 wrong password, 403 demo user)
  </action>
  <verify>
Run `npm run build` from rondo-club to ensure no PHP syntax issues break the frontend build. Grep for `change_password` and `linked_person_name` in class-rest-api.php to confirm both additions exist. Grep for `user/password` in the developer docs to confirm documentation was added.
  </verify>
  <done>
POST /rondo/v1/user/password endpoint registered with demo guard, current password verification, wp_set_password + session destruction. GET /user/me response includes linked_person_name and active_functies. Developer docs updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend Profile page + sidebar integration + version bump</name>
  <files>src/api/client.js, src/hooks/useCurrentUser.js, src/pages/Profile/Profile.jsx, src/lazyPages.js, src/router.jsx, src/components/layout/Layout.jsx, CHANGELOG.md, style.css, package.json</files>
  <action>
**1. API client** (`src/api/client.js`):
Add to the `prmApi` object, near `getCurrentUser`:
```js
changePassword: (data) => api.post('/rondo/v1/user/password', data),
```

**2. useCurrentUser hook** (`src/hooks/useCurrentUser.js`):
Add `useMutation` to the import from `@tanstack/react-query`. Add a new exported function:
```js
export function useChangePassword() {
  return useMutation({
    mutationFn: ({ currentPassword, newPassword }) =>
      prmApi.changePassword({ current_password: currentPassword, new_password: newPassword }),
  });
}
```
Import `prmApi` is already present. Add `useMutation` to the existing import. Update the JSDoc comment listing consumers to include "Profile".

**3. Profile page** (`src/pages/Profile/Profile.jsx`):
Create `src/pages/Profile/` directory and `Profile.jsx`. This is a standalone page (no tabs). Structure:

- Import `useCurrentUser` and `useChangePassword` from `@/hooks/useCurrentUser`
- Import `User`, `Lock`, `Briefcase` from `lucide-react`
- Import `useState` from `react`
- Use `useCurrentUser()` to get `linked_person_name`, `active_functies`, `name`, `email`
- Use `useChangePassword()` for the mutation
- State: `currentPassword`, `newPassword`, `confirmPassword`, `successMessage`, `errorMessage`
- The page layout:
  - Top section: "Profiel" heading
  - Card 1 — "Account": show user name (from `user.name`), email (from `user.email`)
  - Card 2 — "Sportlink koppeling" (only if `user.linked_person_name` is truthy): show linked name and active functies as pills/badges. If no functies, show "Geen actieve functies"
  - Card 3 — "Wachtwoord wijzigen" (hidden if `window.rondoConfig?.isDemoUser`):
    - Three fields: "Huidig wachtwoord", "Nieuw wachtwoord", "Bevestig nieuw wachtwoord"
    - All fields type="password"
    - Client-side validation: new password minimum 8 characters, confirm must match new
    - Submit button "Wachtwoord wijzigen" (disabled during mutation, show spinner)
    - On error: display `error.response?.data?.message` or "Er is een fout opgetreden" under the current password field
    - On success: immediately `window.location.href = window.rondoConfig?.loginUrl || '/wp-login.php'` — NO queryClient invalidation, NO intermediate API calls. The session is dead after password change.
- Use consistent Tailwind classes: `card` for containers, `btn btn-primary` for submit, dark mode support

**4. Lazy import** (`src/lazyPages.js`):
Add:
```js
export const Profile = lazy(() => import('@/pages/Profile/Profile'));
```

**5. Router** (`src/router.jsx`):
Add `Profile` to the import from `./lazyPages`. Add route inside the ProtectedLayout children, after the Settings routes and before the fallback:
```js
// Profile route
{ path: 'profile', element: <Profile /> },
```

**6. Layout sidebar + UserMenu** (`src/components/layout/Layout.jsx`):
- **getPageTitle**: Add `if (path.startsWith('/profile')) return 'Profiel';` to the function
- **UserMenu update**: Change the "Profiel bewerken" link from an `<a href={user.profile_url} target="_blank">` to a React Router `<Link to="/profile">` (import `Link` from `react-router-dom`). Keep it hidden for demo users (existing `!window.rondoConfig?.isDemoUser` guard is already there). For admin users, keep BOTH the in-app profile link AND the existing WordPress admin link. The `User` icon is already imported.

**7. Version bump**: Update version from 29.3.0 to 29.4.0 in `package.json`, `style.css`. Add CHANGELOG.md entry under `## [29.4.0]` with:
```
### Added
- In-app Profile page with Sportlink identity display and password change form
- POST /rondo/v1/user/password endpoint for in-app password management
- GET /rondo/v1/user/me now returns linked_person_name and active_functies
- Sidebar UserMenu links to in-app profile instead of wp-admin for non-admin users
```
  </action>
  <verify>
Run `npm run build` from rondo-club — must succeed with zero errors. Run `npm run lint` — must pass with zero warnings. Verify Profile.jsx exists in `src/pages/Profile/`. Grep for `useChangePassword` in useCurrentUser.js. Grep for `changePassword` in client.js. Grep for `Profile` in lazyPages.js and router.jsx. Grep for `/profile` in Layout.jsx.
  </verify>
  <done>
Profile.jsx page renders linked Sportlink name, active functies, and password change form. Password form validates locally (8+ chars, confirm match), submits to backend, hard-redirects to login on success. UserMenu links non-admin users to in-app /profile. Router, lazy import, sidebar title all wired up. Version 29.4.0.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npm run lint` passes with zero warnings
3. `grep -r 'change_password' includes/class-rest-api.php` finds the method
4. `grep -r 'linked_person_name' includes/class-rest-api.php` finds the response field
5. `grep -r 'useChangePassword' src/hooks/useCurrentUser.js` finds the hook
6. `grep -r 'changePassword' src/api/client.js` finds the API method
7. `grep -r 'Profile' src/lazyPages.js` finds the lazy import
8. `grep -r '/profile' src/router.jsx` finds the route
9. `grep -r '/profile' src/components/layout/Layout.jsx` finds the sidebar link
10. Deploy to production via `bin/deploy.sh`, verify the profile page loads at /profile
</verification>

<success_criteria>
- POST /rondo/v1/user/password endpoint exists with demo guard + current password verification + session destruction
- GET /rondo/v1/user/me returns linked_person_name and active_functies
- Profile page at /profile displays identity and password form
- Password change redirects to login page after success
- Demo user cannot see password form
- UserMenu "Profiel bewerken" links to /profile for non-admin users
- Build passes, lint passes, version is 29.4.0
- Developer docs updated
</success_criteria>

<output>
After completion, create `.planning/phases/207-in-app-profile-page/207-01-SUMMARY.md`
</output>
