---
phase: 177-documentation-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - style.css
  - package.json
  - CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "AGENTS.md no longer references person_label, team_label, or date_type taxonomies"
    - "AGENTS.md no longer references important_date CPT"
    - "AGENTS.md correctly lists 2 main CPTs (person, team) plus supporting CPTs"
    - "AGENTS.md taxonomy list only shows relationship_type and seizoen"
    - "Version bumped to 24.1.0 in style.css and package.json"
    - "CHANGELOG.md has a v24.1.0 entry documenting all removed features"
  artifacts:
    - path: "AGENTS.md"
      provides: "Accurate project documentation"
      contains: "relationship_type"
    - path: "style.css"
      provides: "Theme version"
      contains: "24.1.0"
    - path: "package.json"
      provides: "Package version"
      contains: "24.1.0"
    - path: "CHANGELOG.md"
      provides: "Changelog entry"
      contains: "24.1.0"
  key_links:
    - from: "AGENTS.md"
      to: "includes/class-post-types.php"
      via: "CPT descriptions must match registered post types"
      pattern: "person.*team"
    - from: "AGENTS.md"
      to: "includes/class-taxonomies.php"
      via: "Taxonomy descriptions must match registered taxonomies"
      pattern: "relationship_type"
---

<objective>
Update AGENTS.md to reflect the simplified data model after v24.1 dead feature removal, bump version to 24.1.0, and add changelog entry.

Purpose: AGENTS.md is the primary developer guide and must accurately describe the current system. Stale references to removed taxonomies and CPTs mislead future development. The version bump and changelog close out the v24.1 milestone.

Output: Updated AGENTS.md, version files, and changelog.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-backend-cleanup/175-01-SUMMARY.md
@.planning/phases/175-backend-cleanup/175-02-SUMMARY.md
@.planning/phases/176-frontend-cleanup/176-01-SUMMARY.md
@.planning/phases/176-frontend-cleanup/176-02-SUMMARY.md
@AGENTS.md
@style.css
@package.json
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AGENTS.md to reflect current data model</name>
  <files>AGENTS.md</files>
  <action>
Read AGENTS.md and make the following targeted edits:

1. **Project Overview** (line 7): Change "managing people, teams (synced from Sportlink), and important dates" to "managing people, teams, and club operations". Remove all "important dates" references throughout the file.

2. **Key classes** section (around line 45-54): Update the PostTypes description:
   - Change `Rondo\Core\PostTypes` from "Registers Person, Team, Important Date CPTs" to "Registers Person, Team, Commissie, and other CPTs"
   - Change `Rondo\Core\Taxonomies` from "Registers labels and relationship types" to "Registers relationship types and seizoen taxonomy"

3. **React app structure** (around line 66): Change "pages/ - Route components for People, Teams, Dates, Settings" to "pages/ - Route components for People, Teams, Commissies, Settings"

4. **API namespaces** (around line 75-76): Change "/wp/v2/ - Standard WordPress REST (people, teams, important-dates)" to "/wp/v2/ - Standard WordPress REST (people, teams, commissies)"

5. **Data Model section** (around line 79-88): Replace the entire Custom Post Types and Taxonomies subsections:

   **Custom Post Types:**
   - `person` - Contact records with relationships, work history, photo gallery
   - `team` - Teams with logo, industry, contact info (post type slug remains `team` for backward compatibility)
   - `commissie` - Committees with staff members and team structure
   - `rondo_todo` - Task/todo items linked to people
   - `discipline_case` - Discipline/incident tracking

   **Taxonomies:**
   - `relationship_type` - Relationship classifications
   - `seizoen` - Season classification for discipline cases

6. **style.css Description** (line 6): Change "Track people, teams, and important dates" to "A React-powered club management theme for sports clubs."

Verify the edits by grepping for removed terms:
- `person_label` should return 0 hits
- `team_label` should return 0 hits
- `date_type` should return 0 hits
- `important_date` should return 0 hits in AGENTS.md
  </action>
  <verify>
Run these greps on AGENTS.md:
- `grep -c "person_label" AGENTS.md` returns 0
- `grep -c "team_label" AGENTS.md` returns 0
- `grep -c "date_type" AGENTS.md` returns 0
- `grep -c "important_date" AGENTS.md` returns 0
- `grep -c "relationship_type" AGENTS.md` returns at least 1
- `grep -c "seizoen" AGENTS.md` returns at least 1
  </verify>
  <done>AGENTS.md accurately describes the current data model with no references to removed features (person_label, team_label, date_type, important_date taxonomies/CPTs)</done>
</task>

<task type="auto">
  <name>Task 2: Bump version to 24.1.0 and add changelog entry</name>
  <files>style.css, package.json, CHANGELOG.md</files>
  <action>
1. **style.css**: Change `Version: 24.0.0` to `Version: 24.1.0`

2. **package.json**: Change `"version": "24.0.0"` to `"version": "24.1.0"`

3. **CHANGELOG.md**: Add a new entry below `## [Unreleased]` and above `## [24.0.0]`:

```markdown
## [24.1.0] - 2026-02-13

### Removed
- Removed `person_label` and `team_label` taxonomy registrations and all database data
- Removed `commissie_label` taxonomy registration and database data
- Removed Settings/Labels management page from frontend
- Removed BulkLabelsModal component and label bulk actions from all list views
- Removed label columns, badges, and filters from PeopleList, TeamsList, CommissiesList
- Removed label add/remove controls from PersonDetail
- Removed all label-related API client methods (12 methods)
- Removed `date_type` field from reminders and iCal birthday data
- Removed `CATEGORIES` line from iCal birthday events
- Removed deprecated `RONDO_Dates_CLI_Command` WP-CLI class
- Removed deprecated `migrate_birthdates` and `update_date_references` WP-CLI commands
- Removed residual `important_date` references from route map, REST API, and CLI
- Removed teams and commissies bulk-update endpoints (were labels-only)

### Changed
- Updated email notification wording from "important dates" to "birthdays"
- Updated CLI reminder messages to reflect birthday-only system
- Simplified people bulk-update endpoint (organization_id only)
- Updated AGENTS.md and developer docs to reflect simplified data model
```

Verify version consistency across files.
  </action>
  <verify>
- `grep "Version: 24.1.0" style.css` finds the version
- `grep '"version": "24.1.0"' package.json` finds the version
- `grep "24.1.0" CHANGELOG.md` finds the changelog entry
- Versions match across all three files
  </verify>
  <done>Version bumped to 24.1.0 in style.css and package.json, and CHANGELOG.md has a comprehensive v24.1.0 entry documenting all removed features and changes</done>
</task>

</tasks>

<verification>
1. AGENTS.md contains zero references to: person_label, team_label, date_type, important_date
2. AGENTS.md correctly lists current CPTs: person, team, commissie, rondo_todo, discipline_case
3. AGENTS.md correctly lists current taxonomies: relationship_type, seizoen
4. style.css shows Version: 24.1.0
5. package.json shows "version": "24.1.0"
6. CHANGELOG.md contains [24.1.0] entry with Removed section
</verification>

<success_criteria>
- AGENTS.md accurately reflects the post-v24.1 data model
- All three version files are consistent at 24.1.0
- Changelog documents all features removed in v24.1
</success_criteria>

<output>
After completion, create `.planning/phases/177-documentation-updates/177-01-SUMMARY.md`
</output>
