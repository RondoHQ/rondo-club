---
phase: 177-documentation-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ../developer/src/content/docs/data-model.md
  - ../developer/src/content/docs/architecture.md
  - ../developer/src/content/docs/api/rest-api.md
  - ../developer/src/content/docs/api/people.md
  - ../developer/src/content/docs/features/reminders.md
  - ../developer/src/content/docs/integrations/ical-feed.md
  - ../developer/src/content/docs/overview.md
autonomous: true

must_haves:
  truths:
    - "data-model.md no longer lists person_label or team_label taxonomies"
    - "data-model.md correctly shows 2 taxonomies (relationship_type, seizoen)"
    - "rest-api.md no longer lists person_label or team_label endpoints"
    - "rest-api.md no longer shows 'labels' in dashboard or search response examples"
    - "architecture.md no longer references Important Date CPT or labels"
    - "people.md no longer references labels_add/labels_remove in bulk-update"
    - "reminders.md no longer shows date_type in response examples"
    - "ical-feed.md no longer shows CATEGORIES line in iCal event example"
  artifacts:
    - path: "../developer/src/content/docs/data-model.md"
      provides: "Accurate data model reference"
      contains: "relationship_type"
    - path: "../developer/src/content/docs/api/rest-api.md"
      provides: "Accurate REST API reference"
    - path: "../developer/src/content/docs/architecture.md"
      provides: "Accurate architecture overview"
    - path: "../developer/src/content/docs/api/people.md"
      provides: "Accurate People API reference"
    - path: "../developer/src/content/docs/features/reminders.md"
      provides: "Accurate reminders documentation"
    - path: "../developer/src/content/docs/integrations/ical-feed.md"
      provides: "Accurate iCal feed documentation"
  key_links:
    - from: "../developer/src/content/docs/data-model.md"
      to: "includes/class-taxonomies.php"
      via: "Taxonomy documentation must match registered taxonomies"
      pattern: "relationship_type"
---

<objective>
Update all developer documentation on developer.rondo.club to reflect the simplified data model after v24.1 dead feature removal.

Purpose: Developer docs must match the actual codebase. Stale references to removed taxonomies, labels, date_type, and important_date mislead developers and API consumers.

Output: Updated docs across 7 files in the developer docs site.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-backend-cleanup/175-01-SUMMARY.md
@.planning/phases/176-frontend-cleanup/176-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update data-model.md and architecture.md</name>
  <files>../developer/src/content/docs/data-model.md, ../developer/src/content/docs/architecture.md, ../developer/src/content/docs/overview.md</files>
  <action>
**data-model.md** — Make these targeted edits:

1. **Taxonomies intro** (line 116-117): Change "three custom taxonomies" to "two custom taxonomies" (relationship_type and seizoen). The seizoen taxonomy exists but is not documented — add it.

2. **Remove Person Label section** (lines 119-130): Delete the entire `### Person Label` section including the example labels.

3. **Remove Team Label section** (lines 132-145): Delete the entire `### Team Label` section including the example labels.

4. **Add Seizoen taxonomy section** after the Relationship Type section:
```markdown
### Seizoen (`seizoen`)

Season classification for discipline cases.

| Property | Value |
|----------|-------|
| Hierarchical | No (tag-like) |
| Attached To | discipline_case |
| REST Enabled | Yes |

**Example values:** 2024-2025, 2025-2026
```

5. **Workspace section** (lines 188-200): This documents a `workspace` CPT but the actual implementation uses a taxonomy-based approach. Check if this section is still accurate. If the workspace CPT is no longer registered in class-post-types.php, remove the section. Based on prior code review, workspace IS still a pattern but through taxonomy, not CPT. Remove the "Workspace (`workspace`)" CPT section since it's not registered as a CPT in class-post-types.php.

**architecture.md** — Make these targeted edits:

1. **Backend classes table**: Change PostTypes description from "Registers Person, Team, Important Date CPTs" to "Registers Person, Team, Commissie, and other CPTs". Change Taxonomies description from "Registers labels and relationship types" to "Registers relationship types and seizoen taxonomy".

2. **Frontend API namespaces**: Change "/wp/v2/ — Standard WordPress REST (people, teams, important-dates)" to "/wp/v2/ — Standard WordPress REST (people, teams, commissies)"

**overview.md** — Make this targeted edit:

1. Change "managing people, teams, and important dates" to "managing people, teams, and club operations"

Verify: Grep all three files for `person_label`, `team_label`, `important_date`, `date_type` — all should return 0.
  </action>
  <verify>
- Grep data-model.md for "person_label" returns 0 hits
- Grep data-model.md for "team_label" returns 0 hits
- Grep architecture.md for "Important Date" returns 0 hits
- Grep architecture.md for "labels and relationship" returns 0 hits
- Grep overview.md for "important dates" returns 0 hits
  </verify>
  <done>data-model.md correctly lists 2 taxonomies (relationship_type, seizoen) with no label taxonomy references. architecture.md correctly describes current CPTs and taxonomies. overview.md updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update REST API docs, People API, reminders, and iCal docs</name>
  <files>../developer/src/content/docs/api/rest-api.md, ../developer/src/content/docs/api/people.md, ../developer/src/content/docs/features/reminders.md, ../developer/src/content/docs/integrations/ical-feed.md</files>
  <action>
**rest-api.md** — Make these targeted edits:

1. **Taxonomies table** (lines 57-60): Remove the `person_label` and `team_label` endpoint rows. Keep only `relationship_type`. Add a note that `seizoen` is also available via `/wp/v2/seizoen`.

2. **Dashboard response** (lines 78-106): Remove `"labels": ["Family", "Friends"]` from the `recent_people` example (line 92). Labels are no longer returned in person summaries.

3. **Search response** (lines 143-153): Remove `"labels": []` from both the people and teams search results examples (lines 148, 151).

4. **Reminders response** (lines 172-184): Remove `"date_type": ["birthday"]` from the response example (line 285 in the reminders doc section). Actually this is in reminders.md, handle there.

**people.md** — Make these targeted edits:

1. **Bulk Update** section (lines 436-476): Remove `labels_add` and `labels_remove` from the request body example and the "Available bulk updates" table. The bulk-update endpoint now only supports `visibility`, `assigned_workspaces`, and `organization_id`. Remove the `labels_add` and `labels_remove` rows from the table entirely. Update the description text to remove "add labels" mention.

**reminders.md** — Make these targeted edits:

1. **REST API response example** (around line 284): Remove `"date_type": ["birthday"]` from the JSON response example for GET `/rondo/v1/reminders`.

**ical-feed.md** — Make these targeted edits:

1. **Event Structure example** (lines 142-154): Remove the `CATEGORIES:Birthday` line from the iCal VEVENT example.

2. **Field Mapping table** (lines 156-169): Remove the `CATEGORIES` row from the field mapping table (the one mapping to date type name).

Verify: Grep all four files for stale references.
  </action>
  <verify>
- Grep rest-api.md for "person_label" returns 0 hits
- Grep rest-api.md for "team_label" returns 0 hits
- Grep rest-api.md for '"labels"' returns 0 hits in response examples
- Grep people.md for "labels_add" returns 0 hits
- Grep people.md for "labels_remove" returns 0 hits
- Grep reminders.md for "date_type" returns 0 hits
- Grep ical-feed.md for "CATEGORIES" returns 0 hits
  </verify>
  <done>REST API docs no longer reference label taxonomy endpoints or labels in responses. People API no longer shows label bulk operations. Reminders doc no longer shows date_type field. iCal doc no longer shows CATEGORIES line.</done>
</task>

</tasks>

<verification>
1. Zero references to person_label, team_label across all modified developer docs
2. Zero references to date_type in reminders.md
3. Zero references to CATEGORIES in ical-feed.md
4. Zero references to labels_add/labels_remove in people.md
5. data-model.md shows exactly 2 taxonomies: relationship_type, seizoen
6. architecture.md backend table shows correct CPT and taxonomy descriptions
7. REST API taxonomy endpoints table only shows relationship_type and seizoen
</verification>

<success_criteria>
- All developer documentation on developer.rondo.club reflects the current post-v24.1 data model
- No references to removed features (person_label, team_label, date_type, important_date, CATEGORIES, labels_add/labels_remove)
- Documentation is accurate for any developer or API consumer reading it fresh
</success_criteria>

<output>
After completion, create `.planning/phases/177-documentation-updates/177-02-SUMMARY.md`
</output>
