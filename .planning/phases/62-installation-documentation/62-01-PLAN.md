---
phase: 62-installation-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
---

<objective>
Add comprehensive wp-config.php configuration documentation to README.md.

Purpose: Enable users to properly configure Stadion with all required constants for Slack integration, Google Calendar integration, and encryption.
Output: Updated README.md with complete configuration section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source files:
@README.md
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Configuration section to README.md</name>
  <files>README.md</files>
  <action>
Add a new "## Configuration" section after the "## Installation" section in README.md. The section should include:

1. Introduction explaining that Stadion uses wp-config.php constants (not .env files)
2. Required constants table with:
   - Constant name
   - Purpose
   - Required/Optional status
   - Example or generation method

3. Subsections for each integration:

   **Encryption (Required):**
   - `STADION_ENCRYPTION_KEY` - exactly 32 bytes, used for Slack token storage
   - Include generation command: `php -r "echo bin2hex(random_bytes(16));"`

   **Slack Integration (Optional):**
   - `STADION_SLACK_CLIENT_ID`
   - `STADION_SLACK_CLIENT_SECRET`
   - `STADION_SLACK_SIGNING_SECRET`
   - Brief setup steps pointing to https://api.slack.com/apps

   **Google Calendar Integration (Optional):**
   - `GOOGLE_CALENDAR_CLIENT_ID`
   - `GOOGLE_CALENDAR_CLIENT_SECRET`
   - Brief setup steps for Google Cloud Console
   - Note about authorized redirect URI format

   **CalDAV Integration (Optional):**
   - Note that no server-side config needed
   - Users add connections via Settings UI
   - List supported providers (iCloud, Fastmail, Nextcloud)

4. Example wp-config.php snippet showing all constants

Format: Use markdown code blocks for PHP examples, tables for constant reference.

Reference `.env.example` for accurate descriptions but write in README style (more concise, user-focused).
  </action>
  <verify>
    - README.md contains new Configuration section
    - All 6 constants are documented
    - Example code block present
    - No formatting errors
  </verify>
  <done>README.md has complete Configuration section with all wp-config.php constants documented</done>
</task>

<task type="auto">
  <name>Task 2: Update Documentation section references</name>
  <files>README.md</files>
  <action>
Update the "## Documentation" section near the bottom of README.md to include a reference to configuration:

Add a new bullet point:
- **Configuration** - wp-config.php constants for integrations (in this document)

Ensure the configuration section is listed in logical order with other docs.
  </action>
  <verify>Documentation section updated with configuration reference</verify>
  <done>Documentation section references the new Configuration section</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Configuration section exists after Installation section
- [ ] All constants documented: STADION_ENCRYPTION_KEY, STADION_SLACK_CLIENT_ID, STADION_SLACK_CLIENT_SECRET, STADION_SLACK_SIGNING_SECRET, GOOGLE_CALENDAR_CLIENT_ID, GOOGLE_CALENDAR_CLIENT_SECRET
- [ ] CalDAV note included (no server config needed)
- [ ] Example wp-config.php code block present
- [ ] Documentation section updated
- [ ] README.md is valid markdown (no broken formatting)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No formatting errors in README.md
- Configuration section is comprehensive and user-friendly
</success_criteria>

<output>
After completion, create `.planning/phases/62-installation-documentation/62-01-SUMMARY.md`
</output>
