---
phase: 190-finance-settings-ui-mollie-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/class-rest-api.php
  - src/pages/Finance/FinanceSettings.jsx
  - style.css
  - package.json
  - CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "Finance Settings has a Mollie tab with a masked API key input that saves correctly"
    - "Finance Settings Betaling tab has a payment provider selector (Rabobank / Mollie) that saves correctly"
    - "Mollie tab shows a Test/Live mode badge derived from mollie_environment when a key is stored"
    - "Full Mollie API key is never returned by the REST GET endpoint — only mollie_has_api_key bool and mollie_environment string"
    - "Version is 27.0.0 in style.css and package.json"
    - "CHANGELOG.md has a v27.0.0 entry covering the full Mollie milestone"
  artifacts:
    - path: "src/pages/Finance/FinanceSettings.jsx"
      provides: "Mollie tab UI, provider selector in Betaling tab"
      contains: "mollie"
    - path: "includes/class-rest-api.php"
      provides: "Registered REST args for mollie_api_key and active_payment_provider"
      contains: "mollie_api_key"
    - path: "style.css"
      provides: "Version 27.0.0"
      contains: "27.0.0"
    - path: "package.json"
      provides: "Version 27.0.0"
      contains: "27.0.0"
    - path: "CHANGELOG.md"
      provides: "v27.0.0 changelog entry"
      contains: "27.0.0"
  key_links:
    - from: "src/pages/Finance/FinanceSettings.jsx"
      to: "/rondo/v1/finance/settings"
      via: "formData.mollie_api_key and formData.active_payment_provider sent in payload"
      pattern: "mollie_api_key"
    - from: "src/pages/Finance/FinanceSettings.jsx"
      to: "settings.mollie_has_api_key"
      via: "Conditional rendering of environment badge and existing key notice"
      pattern: "mollie_has_api_key"
---

<objective>
Add Mollie configuration UI to the Finance Settings page: a new "Mollie" tab with masked API key input and test/live environment badge, plus a payment provider selector (Rabobank / Mollie) in the existing Betaling tab. Register the missing REST args for `mollie_api_key` and `active_payment_provider`. Bump version to 27.0.0 and update CHANGELOG.md to close out the v27.0 Mollie milestone.

Purpose: This is the final phase of v27.0 Mollie — all backend infrastructure (SDK, FinanceConfig, MollieClient, MolliePayment, MollieWebhook, provider routing) was built in Phases 186–189. This phase exposes the Mollie configuration to administrators so they can enter their API key and switch between payment providers.

Output: Working Mollie configuration UI in Finance Settings, version 27.0.0 shipped.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/190-finance-settings-ui-mollie-configuration/190-RESEARCH.md
@src/pages/Finance/FinanceSettings.jsx
@includes/class-rest-api.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Mollie tab UI, provider selector, and REST args</name>
  <files>
    src/pages/Finance/FinanceSettings.jsx
    includes/class-rest-api.php
  </files>
  <action>
**Backend — `includes/class-rest-api.php`:**

Add two new args to the `update_finance_settings` POST route args array (after line 751, the `rabobank_environment` entry):

```php
'mollie_api_key'          => [ 'required' => false, 'sanitize_callback' => 'sanitize_text_field' ],
'active_payment_provider' => [
    'required'          => false,
    'sanitize_callback' => 'sanitize_text_field',
    'validate_callback' => function( $param ) {
        return in_array( $param, [ 'rabobank', 'mollie' ], true );
    },
],
```

Also add the missing args for `club_logo_id`, `accent_color`, and `bcc_email` that are currently used but not registered:

```php
'club_logo_id'  => [ 'required' => false, 'type' => 'integer' ],
'accent_color'  => [ 'required' => false, 'sanitize_callback' => 'sanitize_text_field' ],
'bcc_email'     => [ 'required' => false, 'sanitize_callback' => 'sanitize_email' ],
```

**Frontend — `src/pages/Finance/FinanceSettings.jsx`:**

1. **Add Mollie tab** to `TABS` array:
   ```jsx
   { id: 'mollie', label: 'Mollie' },
   ```

2. **Add formData fields** to initial state:
   ```jsx
   active_payment_provider: 'rabobank',
   mollie_api_key: '',
   ```

3. **Add to useEffect settings loader** (the `if (settings)` block):
   ```jsx
   active_payment_provider: settings.active_payment_provider || 'rabobank',
   // Do NOT add mollie_api_key — key is never returned by API
   ```

4. **Add to handleSubmit payload construction** (after the `rabobank_environment` line):
   ```jsx
   active_payment_provider: formData.active_payment_provider,
   ```
   And in the conditional credential section (after `rabobank_client_secret`):
   ```jsx
   if (formData.mollie_api_key.trim()) {
     payload.mollie_api_key = formData.mollie_api_key;
   }
   ```

5. **Add to post-save cleanup** (where `rabobank_client_id` and `rabobank_client_secret` are cleared):
   ```jsx
   mollie_api_key: '',
   ```

6. **Add payment provider selector to Betaling tab** — at the END of the `space-y-4` div inside the Betaling section (after the `payment_clause` textarea), add a radio group:
   - Label: "Betalingsprovider"
   - Two radio options: `rabobank` (label: "Rabobank") and `mollie` (label: "Mollie")
   - Helper text: "Kies welke provider wordt gebruikt voor betaallinks bij het versturen van facturen."
   - Follow exact styling pattern from `rabobank_environment` radio group in Rabobank tab

7. **Add Mollie tab section** — after the Rabobank tab section (`{activeTab === 'rabobank' && ...}`), add:
   - Card with heading "Mollie Koppeling" and description "API-sleutel voor betalingen via Mollie."
   - Environment badge: Render ONLY when `settings?.mollie_has_api_key` is true. Show "Live" badge (green) or "Test" badge (yellow) based on `settings.mollie_environment`. Label: "Omgeving:". Use same badge styling as existing patterns.
   - Existing key notice: Render ONLY when `settings?.mollie_has_api_key`. Green info box: "API-sleutel opgeslagen. Laat het veld leeg om de huidige waarde te behouden."
   - API key input: `type="password"`, id `mollie_api_key`, bound to `formData.mollie_api_key`. Placeholder: `settings?.mollie_has_api_key ? '••••••••' : 'live_... of test_...'`. Use standard input class from existing inputs.
   - Helper text below input: "Gebruik een `live_` sleutel voor productie of `test_` voor sandbox. De omgeving wordt automatisch afgeleid." (Use `<code>` tags for `live_` and `test_`.)
   - Follow exact card/heading/spacing pattern from Rabobank tab section.

  </action>
  <verify>
Run `npm run build` from rondo-club/ — must compile without errors. Then grep `FinanceSettings.jsx` for `mollie` to confirm the new tab, form fields, and payload are present. Grep `class-rest-api.php` for `mollie_api_key` and `active_payment_provider` to confirm REST args are registered.
  </verify>
  <done>
FinanceSettings has 5 tabs (Organisatie, Betaling, E-mail, Rabobank, Mollie). Mollie tab shows masked API key input with environment badge. Betaling tab has payment provider radio selector. REST route accepts mollie_api_key and active_payment_provider in args. Build compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Version bump and changelog</name>
  <files>
    style.css
    package.json
    CHANGELOG.md
  </files>
  <action>
1. **Version bump `style.css`:** Change `Version: 26.1.0` to `Version: 27.0.0`

2. **Version bump `package.json`:** Change `"version": "26.1.0"` to `"version": "27.0.0"`

3. **Update `CHANGELOG.md`:** Add a new `## [27.0.0] - 2026-02-18` section between `## [Unreleased]` and `## [26.1.0]`:

```markdown
## [27.0.0] - 2026-02-18

### Added
- Mollie payment provider integration for discipline case invoices
- Mollie API key storage with sodium encryption (key never exposed via REST API)
- Mollie payment link creation via Payments API with automatic checkout URL storage
- Mollie webhook endpoint for automatic invoice status updates on payment confirmation
- Payment provider routing: invoices route to Mollie or Rabobank based on configured provider
- Finance Settings: Mollie tab with masked API key input and test/live environment badge
- Finance Settings: payment provider selector (Rabobank / Mollie) in Betaling tab
```

4. Run `npm run build` to verify the build still compiles with version changes.
  </action>
  <verify>
Grep style.css for "Version: 27.0.0". Grep package.json for "27.0.0". Grep CHANGELOG.md for "27.0.0". Run `npm run build` — must pass.
  </verify>
  <done>
Version is 27.0.0 in both style.css and package.json. CHANGELOG.md has a complete v27.0.0 entry covering the entire Mollie milestone (Phases 186–190).
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. `grep -c 'mollie' src/pages/Finance/FinanceSettings.jsx` returns multiple matches (tab, form field, badge, payload)
3. `grep 'mollie_api_key' includes/class-rest-api.php` shows registered REST arg
4. `grep 'active_payment_provider' includes/class-rest-api.php` shows registered REST arg
5. `grep '27.0.0' style.css package.json CHANGELOG.md` shows version in all 3 files
6. The `mollie_api_key` field is never populated from the API response in the useEffect — only the `active_payment_provider` is loaded from settings
</verification>

<success_criteria>
- Finance Settings page has 5 tabs: Organisatie, Betaling, E-mail, Rabobank, Mollie
- Mollie tab shows masked API key input with placeholder, environment badge (Test/Live), and existing key notice
- Betaling tab includes payment provider radio selector (Rabobank / Mollie)
- REST route args include mollie_api_key and active_payment_provider
- API key is conditionally included in payload (only when non-empty) and cleared after save
- Full API key is never returned by REST GET endpoint
- Version is 27.0.0, CHANGELOG.md updated
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/190-finance-settings-ui-mollie-configuration/190-01-SUMMARY.md`
</output>
