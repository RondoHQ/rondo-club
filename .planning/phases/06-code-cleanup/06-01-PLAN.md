---
phase: 06-code-cleanup
plan: 01
type: execute
depends_on: []
files_modified:
  - src/api/client.js
  - src/main.jsx
  - src/components/layout/Layout.jsx
  - src/components/Timeline/GlobalTodoModal.jsx
  - src/pages/Dashboard.jsx
  - src/pages/People/PersonDetail.jsx
  - src/pages/People/PeopleList.jsx
  - src/pages/People/FamilyTree.jsx
  - src/pages/Companies/CompanyDetail.jsx
  - src/pages/Settings/Settings.jsx
  - src/pages/Todos/TodosList.jsx
---

<objective>
Remove all console.error() calls from production React code.

Purpose: Clean up debug logging left in production, reduce console noise, and present a more professional codebase.
Output: All 48 console.error() calls removed from 11 files, build passing.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech debt being addressed:**
- 48 console.error() calls across 11 React files
- Debug logging not cleaned up before production
- Console noise and potential info leak

**Files with console.error() counts:**
- src/pages/Settings/Settings.jsx: 17
- src/pages/People/PersonDetail.jsx: 17
- src/pages/Companies/CompanyDetail.jsx: 3
- src/components/layout/Layout.jsx: 2
- src/pages/People/PeopleList.jsx: 2
- src/main.jsx: 2
- src/pages/Todos/TodosList.jsx: 1
- src/pages/People/FamilyTree.jsx: 1
- src/api/client.js: 1
- src/pages/Dashboard.jsx: 1
- src/components/Timeline/GlobalTodoModal.jsx: 1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all console.error() calls from React components</name>
  <files>src/api/client.js, src/main.jsx, src/components/layout/Layout.jsx, src/components/Timeline/GlobalTodoModal.jsx, src/pages/Dashboard.jsx, src/pages/People/PersonDetail.jsx, src/pages/People/PeopleList.jsx, src/pages/People/FamilyTree.jsx, src/pages/Companies/CompanyDetail.jsx, src/pages/Settings/Settings.jsx, src/pages/Todos/TodosList.jsx</files>
  <action>
Remove all console.error() statements from the listed files. These are debug logging calls that should not be in production code.

For each file:
1. Search for console.error( calls
2. Remove the entire console.error() statement
3. If the console.error is the only statement in a catch block, keep the empty catch or add a comment like `// Error handled silently` only if the error is genuinely expected and safe to ignore
4. Do NOT replace with any other logging mechanism - just remove

Note: Some patterns to watch for:
- `console.error('Error:', error)` - remove entirely
- `.catch(error => console.error(error))` - replace with `.catch(() => {})` or remove the catch if error handling isn't needed
- `catch (error) { console.error(error); }` - remove the console.error line, keep the catch block structure
  </action>
  <verify>grep -r "console.error" src/ returns no matches</verify>
  <done>All 48 console.error() calls removed from all 11 files</done>
</task>

<task type="auto">
  <name>Task 2: Verify build and lint pass</name>
  <files>package.json</files>
  <action>
Run the build and lint commands to ensure removal of console.error() didn't break anything:
1. Run `npm run build` - should complete without errors
2. Run `npm run lint` - should pass with no warnings (max-warnings: 0 is configured)

If lint fails due to unused variables (e.g., `error` in catch blocks), either:
- Rename to `_error` to indicate intentionally unused
- Remove the variable entirely if catch doesn't need it: `catch { }` or `.catch(() => {})`
  </action>
  <verify>npm run build exits 0, npm run lint exits 0</verify>
  <done>Build completes successfully, lint passes with zero warnings</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -r "console.error" src/` returns no matches
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes with zero warnings
- [ ] No functionality changes - only logging removed
</verification>

<success_criteria>

- All 48 console.error() calls removed
- All 11 files updated
- Build passes
- Lint passes
- No regressions in error handling behavior
</success_criteria>

<output>
After completion, create `.planning/phases/06-code-cleanup/06-01-SUMMARY.md`
</output>
