---
phase: 105-instellingen-settings
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Settings/Settings.jsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Sync success messages display entirely in Dutch"
    - "EditConnectionModal CalDAV section displays entirely in Dutch"
    - "Notifications tab Slack description displays in Dutch"
    - "No mixed-language strings remain in Settings"
  artifacts:
    - path: "src/pages/Settings/Settings.jsx"
      provides: "Dutch translations for remaining 7 English strings"
      contains:
        - "Synchronisatie voltooid"
        - "Inloggegevens bijwerken (optioneel)"
        - "Ontvang meldingen in Slack"
  key_links:
    - from: "Settings.jsx line 411"
      to: "User-facing sync message"
      via: "setSyncSuccess()"
      pattern: "Synchronisatie voltooid.*geimporteerd"
    - from: "Settings.jsx line 1088"
      to: "User-facing calendar sync message"
      via: "setSuccessMessage()"
      pattern: "Synchronisatie voltooid.*nieuw"
---

<objective>
Translate the final 7 English strings in Settings.jsx to Dutch

Purpose: Complete the Dutch localization of Settings by closing the gaps identified in 105-VERIFICATION.md
Output: All Settings UI text displays entirely in Dutch with no mixed-language strings
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/105-instellingen-settings/105-VERIFICATION.md
@src/pages/Settings/Settings.jsx (lines 400-420, 1080-1095, 1985-2035, 2805-2835)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate sync success messages</name>
  <files>src/pages/Settings/Settings.jsx</files>
  <action>
Translate the 2 sync success messages from English to Dutch:

1. Line 411 - Contacts sync success message:
   - Current: `Sync completed: ${pullCount} imported, ${pushedCount} verzonden`
   - Change to: `Synchronisatie voltooid: ${pullCount} geimporteerd, ${pushedCount} verzonden`

2. Line 1088 - Calendar sync success message:
   - Current: `Sync completed: ${response.data.created} new, ${response.data.updated} updated events.`
   - Change to: `Synchronisatie voltooid: ${response.data.created} nieuw, ${response.data.updated} bijgewerkt.`
  </action>
  <verify>Grep for "Sync completed" returns no results; grep for "Synchronisatie voltooid" returns 2 results</verify>
  <done>Both sync success messages display entirely in Dutch</done>
</task>

<task type="auto">
  <name>Task 2: Translate EditConnectionModal CalDAV section</name>
  <files>src/pages/Settings/Settings.jsx</files>
  <action>
Translate the 5 English strings in the CalDAV credential update section (lines 1992-2027):

1. Line 1992 - Section heading:
   - Current: `Update credentials (optional)`
   - Change to: `Inloggegevens bijwerken (optioneel)`

2. Line 1994 - Help text:
   - Current: `Leave blank to keep current credentials. Fill in all fields to update.`
   - Change to: `Laat leeg om huidige inloggegevens te behouden. Vul alle velden in om bij te werken.`

3. Line 1999 - Server URL label:
   - Current: `Server URL`
   - Change to: `Server-URL`

4. Line 2021 - Password label:
   - Current: `Password / App password`
   - Change to: `Wachtwoord / App-wachtwoord`

5. Line 2027 - Password placeholder:
   - Current: `New app-specific password`
   - Change to: `Nieuw app-specifiek wachtwoord`
  </action>
  <verify>Grep for "Update credentials|Leave blank|Server URL|Password / App|app-specific" returns no results in Settings.jsx</verify>
  <done>EditConnectionModal CalDAV section displays entirely in Dutch</done>
</task>

<task type="auto">
  <name>Task 3: Translate Notifications tab Slack description</name>
  <files>src/pages/Settings/Settings.jsx</files>
  <action>
Translate the Slack notification description on line 2811:

- Current: `{slackConnected ? 'Receive notifications in Slack' : 'Slack koppelen in Connections to enable'}`
- Change to: `{slackConnected ? 'Ontvang meldingen in Slack' : 'Koppel Slack om in te schakelen'}`
  </action>
  <verify>Grep for "Receive notifications in Slack|Slack koppelen in Connections" returns no results</verify>
  <done>Slack notification description displays in Dutch for both connected and disconnected states</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run grep to confirm no English remains:
   ```bash
   grep -n "Sync completed\|Update credentials\|Leave blank\|Server URL\|Password / App\|app-specific\|Receive notifications in Slack\|Connections to enable" src/pages/Settings/Settings.jsx
   ```
   Expected: No output (no matches)

2. Run grep to confirm Dutch translations exist:
   ```bash
   grep -n "Synchronisatie voltooid\|Inloggegevens bijwerken\|Ontvang meldingen in Slack\|Koppel Slack om in te schakelen" src/pages/Settings/Settings.jsx
   ```
   Expected: 4 lines (2 sync messages + 2 Slack descriptions combined on line 2811)

3. Build and lint:
   ```bash
   npm run lint && npm run build
   ```
   Expected: No errors
</verification>

<success_criteria>
- All 7 English strings translated to Dutch
- No mixed-language strings remain in Settings.jsx
- Lint passes with no errors
- Build succeeds
- Settings page displays entirely in Dutch
</success_criteria>

<output>
After completion, create `.planning/phases/105-instellingen-settings/105-07-SUMMARY.md`
</output>
