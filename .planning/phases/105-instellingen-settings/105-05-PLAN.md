---
phase: 105-instellingen-settings
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/import/VCardImport.jsx
  - src/components/import/MonicaImport.jsx
  - src/components/import/GoogleContactsImport.jsx
  - src/hooks/useDocumentTitle.js
autonomous: true

must_haves:
  truths:
    - "vCard import component displays in Dutch"
    - "Monica import component displays in Dutch"
    - "Google Contacts import component displays in Dutch"
    - "Settings-related document titles display in Dutch"
  artifacts:
    - path: "src/components/import/VCardImport.jsx"
      provides: "Dutch translations for vCard import"
      contains: "Importeren|Selecteer bestand"
    - path: "src/components/import/MonicaImport.jsx"
      provides: "Dutch translations for Monica import"
      contains: "Importeren|SQL-bestand"
    - path: "src/components/import/GoogleContactsImport.jsx"
      provides: "Dutch translations for Google Contacts import"
      contains: "Importeren|Google Contacten"
    - path: "src/hooks/useDocumentTitle.js"
      provides: "Dutch document titles for Settings pages"
      contains: "Instellingen"
  key_links:
    - from: "DataTab"
      to: "Import components"
      via: "Component imports"
      pattern: "MonicaImport|VCardImport|GoogleContactsImport"
---

<objective>
Translate import components and Settings-related document titles from English to Dutch.

Purpose: Complete the Dutch translation of all Settings-related UI including import workflows.
Output: Import components and document title hook with Dutch translations.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/105-instellingen-settings/105-CONTEXT.md
@.planning/phases/105-instellingen-settings/105-RESEARCH.md
@src/components/import/VCardImport.jsx
@src/components/import/MonicaImport.jsx
@src/components/import/GoogleContactsImport.jsx
@src/hooks/useDocumentTitle.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate Import Components</name>
  <files>src/components/import/VCardImport.jsx, src/components/import/MonicaImport.jsx, src/components/import/GoogleContactsImport.jsx</files>
  <action>
1. VCardImport.jsx (~301 lines):
   - "Import vCard" -> "vCard importeren"
   - "Select a vCard file..." -> "Selecteer een vCard-bestand..."
   - "Drop file here or click to select" -> "Sleep bestand hierheen of klik om te selecteren"
   - "Choose File" -> "Bestand kiezen"
   - ".vcf files only" -> "alleen .vcf-bestanden"
   - "Import" -> "Importeren"
   - "Importing..." -> "Importeren..."
   - "Cancel" -> "Annuleren"
   - Success messages: "Imported X contacts" -> "X contacten geimporteerd"
   - "X updated, Y created" -> "X bijgewerkt, Y aangemaakt"
   - Error messages
   - Preview section labels if present

2. MonicaImport.jsx (~320 lines):
   - "Import from Monica" -> "Importeren van Monica"
   - "Import your contacts from Monica CRM" -> "Importeer je contacten van Monica CRM"
   - "Select SQL file" -> "Selecteer SQL-bestand"
   - "Export your Monica database..." -> "Exporteer je Monica-database..."
   - All import progress messages
   - Success/error messages
   - "Processing..." -> "Verwerken..."
   - Any help text or instructions

3. GoogleContactsImport.jsx (~450 lines):
   - "Import from Google Contacts" -> "Importeren van Google Contacten"
   - "Select CSV file" -> "Selecteer CSV-bestand"
   - "Export from Google Contacts..." -> "Exporteer van Google Contacten..."
   - All column mapping labels if present
   - Import progress messages
   - "X contacts found" -> "X contacten gevonden"
   - "Importing..." -> "Importeren..."
   - Success/error messages
   - Help text about CSV format

Search for confirm(, alert(, title= and translate those in all files.
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Grep for English strings: `grep -n "Import vCard\|Import from Monica\|Import from Google" src/components/import/*.jsx` should return no matches.
  </verify>
  <done>
All three import components fully translated to Dutch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Translate Document Titles in useDocumentTitle.js</name>
  <files>src/hooks/useDocumentTitle.js</files>
  <action>
Check useDocumentTitle.js for Settings-related title strings and translate:

1. Find how titles are set for Settings pages
2. Translate Settings-related titles:
   - "Settings" -> "Instellingen"
   - "Labels - Settings" -> "Labels - Instellingen"
   - "User Approval - Settings" -> "Gebruikers - Instellingen"
   - "Relationship Types - Settings" -> "Relatietypes - Instellingen"
   - "Custom Fields - Settings" -> "Aangepaste velden - Instellingen"
   - "Feedback Management - Settings" -> "Feedback - Instellingen"

Note: If document titles are set in each component individually rather than in useDocumentTitle.js, verify they were translated in the respective component files. If useDocumentTitle.js is just a utility hook, check if Settings pages call it with hardcoded English strings and update those calls.

Also check the main Settings.jsx for any useDocumentTitle calls and translate those strings.
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Check browser tab titles when navigating to Settings pages (during UAT).
  </verify>
  <done>
All Settings-related document titles display in Dutch in browser tab.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` completes successfully
2. `npm run lint` passes (max-warnings: 0)
3. All import components translated
4. Document titles translated
</verification>

<success_criteria>
- vCard import workflow fully in Dutch
- Monica import workflow fully in Dutch
- Google Contacts CSV import workflow fully in Dutch
- Browser tab shows Dutch titles for all Settings pages
- All import success/error messages in Dutch
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/105-instellingen-settings/105-05-SUMMARY.md`
</output>
