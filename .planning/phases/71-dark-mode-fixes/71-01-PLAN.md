---
phase: 71-dark-mode-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WorkHistoryEditModal.jsx
  - src/components/AddressEditModal.jsx
autonomous: true

must_haves:
  truths:
    - "User can view work history modal in dark mode with readable text"
    - "User can view address modal in dark mode with readable text"
    - "Modal backgrounds and borders adapt to dark mode"
    - "Form labels and text have proper contrast in dark mode"
  artifacts:
    - path: "src/components/WorkHistoryEditModal.jsx"
      provides: "Dark mode support for work history editing"
      contains: "dark:bg-gray-800"
    - path: "src/components/AddressEditModal.jsx"
      provides: "Dark mode support for address editing"
      contains: "dark:bg-gray-800"
  key_links:
    - from: "WorkHistoryEditModal.jsx"
      to: "Tailwind dark: variants"
      via: "CSS classes"
      pattern: "dark:bg-gray-800.*dark:border-gray-700"
---

<objective>
Add dark mode support to WorkHistoryEditModal and AddressEditModal components.

Purpose: These modals currently have hardcoded light mode colors (bg-white, bg-gray-50, text-gray-700) that cause poor contrast in dark mode, making them difficult to use.

Output: Both modals will have proper dark mode variants matching the established pattern used in ImportantDateModal and other components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ImportantDateModal.jsx (reference for dark mode pattern)
@src/index.css (Tailwind component definitions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark mode to WorkHistoryEditModal</name>
  <files>src/components/WorkHistoryEditModal.jsx</files>
  <action>
Update WorkHistoryEditModal.jsx with dark mode classes following the ImportantDateModal pattern:

1. Modal container (line ~105): Change `bg-white` to `bg-white dark:bg-gray-800`

2. Header border (line ~106): Change `border-b` to `border-b border-gray-200 dark:border-gray-700`

3. Heading text (line ~107): Add `text-gray-900 dark:text-gray-50` to the h2

4. Close button (line ~110): Add dark variant `dark:hover:text-gray-300`

5. Checkbox input (line ~188): Add `dark:bg-gray-700 dark:border-gray-600`

6. Checkbox label (line ~191): Change `text-gray-700` to `text-gray-700 dark:text-gray-300`

7. Footer container (line ~197): Change `border-t bg-gray-50` to `border-t border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900`

The .input and .label classes in index.css already have dark mode support, so form fields will work automatically.
  </action>
  <verify>
1. Run `npm run build` - should complete without errors
2. Visual inspection in dark mode: Modal should have dark gray background, readable text, proper borders
  </verify>
  <done>WorkHistoryEditModal displays correctly in dark mode with proper contrast for all elements</done>
</task>

<task type="auto">
  <name>Task 2: Add dark mode to AddressEditModal</name>
  <files>src/components/AddressEditModal.jsx</files>
  <action>
Update AddressEditModal.jsx with dark mode classes following the same pattern:

1. Modal container (line ~65): Change `bg-white` to `bg-white dark:bg-gray-800`

2. Header border (line ~66): Change `border-b` to `border-b border-gray-200 dark:border-gray-700`

3. Heading text (line ~67): Add `text-gray-900 dark:text-gray-50` to the h2

4. Close button (line ~70): Add dark variant `dark:hover:text-gray-300`

5. Footer container (line ~148): Change `border-t bg-gray-50` to `border-t border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900`

The .input and .label classes already have dark mode support.
  </action>
  <verify>
1. Run `npm run build` - should complete without errors
2. Visual inspection in dark mode: Modal should have dark gray background, readable text, proper borders
  </verify>
  <done>AddressEditModal displays correctly in dark mode with proper contrast for all elements</done>
</task>

</tasks>

<verification>
1. `npm run lint` passes with no errors
2. `npm run build` completes successfully
3. Both modals render correctly in dark mode on production site
</verification>

<success_criteria>
- WorkHistoryEditModal has dark mode support (bg-gray-800, border-gray-700, text-gray-50)
- AddressEditModal has dark mode support (bg-gray-800, border-gray-700, text-gray-50)
- No console errors
- Visual contrast meets accessibility standards in dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/71-dark-mode-fixes/71-01-SUMMARY.md`
</output>
