---
phase: 106-global-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CustomFieldsSection.jsx
  - src/components/CustomFieldsEditModal.jsx
  - src/components/InlineFieldInput.jsx
  - src/components/FieldFormPanel.jsx
autonomous: true

must_haves:
  truths:
    - "Custom field type labels display in Dutch (Tekst, Tekstveld, Nummer, etc.)"
    - "Custom field section displays in Dutch"
    - "Custom field edit modal displays in Dutch"
    - "Boolean values display as Ja/Nee"
  artifacts:
    - path: "src/components/CustomFieldsSection.jsx"
      provides: "Dutch custom field section text"
    - path: "src/components/CustomFieldsEditModal.jsx"
      provides: "Dutch custom field edit modal text"
    - path: "src/components/InlineFieldInput.jsx"
      provides: "Dutch inline field input text"
    - path: "src/components/FieldFormPanel.jsx"
      provides: "Dutch field type labels and form text"
  key_links:
    - from: "FieldFormPanel"
      to: "FIELD_TYPES labels"
      via: "constant array"
      pattern: "label:.*"
---

<objective>
Translate custom fields components to Dutch.

Purpose: Custom fields are a core feature - users need Dutch labels for field types, display values, and edit interface.
Output: Dutch labels for field types, boolean values, and all custom field UI elements.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-global-ui/106-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate CustomFieldsSection and InlineFieldInput</name>
  <files>src/components/CustomFieldsSection.jsx, src/components/InlineFieldInput.jsx</files>
  <action>
CustomFieldsSection.jsx:
- "Loading..." → "Laden..."
- "Not set" → "Niet ingesteld"
- "None selected" → "Niets geselecteerd"
- "Yes" / "No" (true_false defaults) → "Ja" / "Nee"
- "Download file" → "Bestand downloaden"
- "Image #N" / "File #N" → "Afbeelding #N" / "Bestand #N"
- "None linked" → "Niet gekoppeld"
- "Custom fields" → "Aangepaste velden"
- "Edit" → "Bewerken"

InlineFieldInput.jsx:
- "-- Select --" → "-- Selecteer --"
- "No" / "Yes" (true_false defaults) → "Nee" / "Ja"
  </action>
  <verify>Review files for remaining English user-facing strings</verify>
  <done>All user-facing strings in CustomFieldsSection and InlineFieldInput are Dutch</done>
</task>

<task type="auto">
  <name>Task 2: Translate CustomFieldsEditModal</name>
  <files>src/components/CustomFieldsEditModal.jsx</files>
  <action>
Look at the full file and translate:
- Modal title "Edit custom fields" → "Aangepaste velden bewerken"
- "No results found" → "Geen resultaten gevonden"
- "Search to add..." → "Zoeken om toe te voegen..."
- "Uploading..." → "Uploaden..."
- "Replace" / "Upload" → "Vervangen" / "Uploaden"
- "Cancel" → "Annuleren"
- "Saving..." → "Opslaan..."
- "Save changes" → "Wijzigingen opslaan"
- Any alert text like "Failed to upload file. Please try again." → "Bestand uploaden mislukt. Probeer het opnieuw."
  </action>
  <verify>Review file for remaining English user-facing strings</verify>
  <done>All user-facing strings in CustomFieldsEditModal.jsx are Dutch</done>
</task>

<task type="auto">
  <name>Task 3: Translate FieldFormPanel</name>
  <files>src/components/FieldFormPanel.jsx</files>
  <action>
Translate FIELD_TYPES array labels:
- Text → Tekst
- Textarea → Tekstveld
- Number → Nummer
- Email → Email (keep)
- URL → URL (keep)
- Date → Datum
- Select → Selectie
- Checkbox → Selectievakje
- True/False → Ja/Nee
- Image → Afbeelding
- File → Bestand
- Link → Link (keep)
- Color → Kleur
- Relationship → Relatie

Translate WEEK_START_OPTIONS:
- Sunday → Zondag
- Monday → Maandag

Translate form defaults:
- ui_on_text: 'Yes' → 'Ja'
- ui_off_text: 'No' → 'Nee'

Translate form labels and sections in the panel:
- "Field Label *" → "Veldnaam *"
- "Type *" → "Type *"
- "Instructions" → "Instructies"
- "Show in list view" → "Tonen in lijstweergave"
- "List view order" → "Lijstvolgorde"
- Various type-specific labels as encountered (Min, Max, Placeholder, etc. - translate to Dutch equivalents)
- "Cancel" → "Annuleren"
- "Saving..." → "Opslaan..."
- "Save" → "Opslaan"
- Error messages like "Label is required" → "Naam is verplicht"

NOTE: Read the full file first to find all strings to translate.
  </action>
  <verify>npm run build passes; Review file for remaining English user-facing strings</verify>
  <done>All user-facing strings in FieldFormPanel.jsx are Dutch</done>
</task>

</tasks>

<verification>
- [ ] FIELD_TYPES shows Dutch labels (Tekst, Tekstveld, Nummer, Datum, etc.)
- [ ] Boolean values display as Ja/Nee
- [ ] CustomFieldsSection displays Dutch text
- [ ] CustomFieldsEditModal displays Dutch text
- [ ] InlineFieldInput displays Dutch text
- [ ] FieldFormPanel displays Dutch text
</verification>

<success_criteria>
All custom field UI components display entirely in Dutch.
</success_criteria>

<output>
After completion, create `.planning/phases/106-global-ui/106-03-SUMMARY.md`
</output>
