---
phase: 199-oauth-frontend-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/class-google-oauth.php
  - includes/class-rest-google-sheets.php
  - includes/class-rest-people.php
  - functions.php
autonomous: true

must_haves:
  truths:
    - "Google OAuth class contains only Sheets-related methods (no Calendar or Contacts scopes)"
    - "GoogleOAuth namespace is Rondo\\Sheets, not Rondo\\Calendar"
    - "Gravatar REST endpoint no longer exists (returns 404)"
    - "PHP has no syntax errors after changes"
  artifacts:
    - path: "includes/class-google-oauth.php"
      provides: "Sheets-only Google OAuth class"
      contains: "namespace Rondo\\Sheets"
    - path: "functions.php"
      provides: "Updated use statement for Rondo\\Sheets\\GoogleOAuth"
      contains: "use Rondo\\Sheets\\GoogleOAuth"
    - path: "includes/class-rest-google-sheets.php"
      provides: "Updated use statement for Rondo\\Sheets\\GoogleOAuth"
      contains: "use Rondo\\Sheets\\GoogleOAuth"
  key_links:
    - from: "includes/class-rest-google-sheets.php"
      to: "includes/class-google-oauth.php"
      via: "use Rondo\\Sheets\\GoogleOAuth"
      pattern: "use Rondo.Sheets.GoogleOAuth"
    - from: "functions.php"
      to: "includes/class-google-oauth.php"
      via: "use Rondo\\Sheets\\GoogleOAuth"
      pattern: "use Rondo.Sheets.GoogleOAuth"
---

<objective>
Simplify the Google OAuth PHP class to serve only Google Sheets, remove all Calendar and Contacts scope methods, change the namespace from `Rondo\Calendar` to `Rondo\Sheets`, and remove the Gravatar REST endpoint from `class-rest-people.php`.

Purpose: After Phase 198 deleted the Calendar/Contacts sync classes, the OAuth class retains dead Calendar/Contacts methods and scope constants. This plan removes them, updates the namespace to reflect sole Sheets purpose, and removes the Gravatar endpoint (separate European service concern bundled here for efficiency since both are backend-only PHP changes).

Output: A lean `class-google-oauth.php` with only Sheets methods, updated namespace references in `functions.php` and `class-rest-google-sheets.php`, and no Gravatar endpoint in `class-rest-people.php`.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-oauth-frontend-cleanup/199-RESEARCH.md
@.planning/phases/198-backend-sync-removal/198-01-SUMMARY.md
@.planning/phases/198-backend-sync-removal/198-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip Calendar/Contacts methods from GoogleOAuth and change namespace to Rondo\Sheets</name>
  <files>includes/class-google-oauth.php, includes/class-rest-google-sheets.php, functions.php</files>
  <action>
  **In `includes/class-google-oauth.php`:**

  1. Change `namespace Rondo\Calendar;` to `namespace Rondo\Sheets;`
  2. Update the class docblock from "Handles Google OAuth2 flow for calendar and contacts integration" to "Handles Google OAuth2 for Google Sheets integration only"
  3. Remove these constants: `SCOPES` (private, calendar.readonly), `CONTACTS_SCOPE_READONLY`, `CONTACTS_SCOPE_READWRITE`
  4. Remove these Calendar-specific methods entirely: `get_client()`, `get_redirect_uri()`, `get_auth_url()`, `handle_callback()`
  5. Remove these Contacts-specific methods entirely: `get_contacts_client()`, `get_contacts_auth_url()`, `handle_contacts_callback()`, `has_contacts_scope()`, `get_contacts_access_mode()`
  6. Keep these methods: `is_configured()`, `get_access_token()`, `refresh_token()`, `get_sheets_client()`, `get_sheets_auth_url()`, `handle_sheets_callback()`, `has_sheets_scope()`, and the `SHEETS_SCOPE` constant
  7. Update the `get_access_token()` docblock: change "Calendar connection with encrypted credentials" to "OAuth connection with encrypted credentials"
  8. In `refresh_token()` method: it currently calls `self::get_client()` which is being deleted. This method creates a Google Client for token refresh. Replace the `$client = self::get_client()` call with inline client creation that mirrors `get_sheets_client()` but without scopes (scopes are not needed for token refresh). Specifically:
     ```php
     if ( ! self::is_configured() ) {
         throw new \Exception( 'Google OAuth is not configured' );
     }
     $client = new \Google\Client();
     $client->setClientId( GOOGLE_OAUTH_CLIENT_ID );
     $client->setClientSecret( GOOGLE_OAUTH_CLIENT_SECRET );
     ```
     Remove the `if ( ! $client )` null check since `is_configured()` handles that now.
  9. In `refresh_token()` return array: change `'scope' => $token['scope'] ?? implode( ' ', self::SCOPES )` to `'scope' => $token['scope'] ?? ''` since the `SCOPES` constant is being removed.

  **In `functions.php`:**

  1. Change `use Rondo\Calendar\GoogleOAuth;` to `use Rondo\Sheets\GoogleOAuth;`
  2. The `class_alias( GoogleOAuth::class, 'RONDO_Google_OAuth' )` line stays as-is (uses the imported class name which resolves correctly after the use statement change)

  **In `includes/class-rest-google-sheets.php`:**

  1. Change `use Rondo\Calendar\GoogleOAuth;` to `use Rondo\Sheets\GoogleOAuth;`

  After all edits, verify no other PHP files reference `Rondo\Calendar\GoogleOAuth` using grep.
  </action>
  <verify>
  - `grep -r "Rondo\\\\Calendar\\\\GoogleOAuth" includes/ functions.php` returns no results
  - `grep -r "CONTACTS_SCOPE" includes/class-google-oauth.php` returns no results
  - `grep -r "get_contacts_" includes/class-google-oauth.php` returns no results
  - `grep -r "get_auth_url\|get_redirect_uri\|handle_callback\b" includes/class-google-oauth.php` returns no results (only `handle_sheets_callback` and `get_sheets_auth_url` remain)
  - `grep "namespace Rondo\\\\Sheets" includes/class-google-oauth.php` returns a match
  - `php -l includes/class-google-oauth.php` passes syntax check
  </verify>
  <done>GoogleOAuth class contains only SHEETS_SCOPE constant + 7 methods (is_configured, get_access_token, refresh_token, get_sheets_client, get_sheets_auth_url, handle_sheets_callback, has_sheets_scope). Namespace is Rondo\Sheets across all 3 files.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Gravatar REST endpoint from class-rest-people.php</name>
  <files>includes/class-rest-people.php</files>
  <action>
  **In `includes/class-rest-people.php`:**

  1. Remove the `register_rest_route` block for `/people/(?P<person_id>\d+)/gravatar` (starts around line 55 with the comment "// Sideload Gravatar image"). This block includes the route registration with its callback and permission_callback.
  2. Remove the entire `sideload_gravatar()` method (starts around line 376 with the docblock "Sideload Gravatar image for a person", ends around line 447). This method generates a Gravatar URL, checks if it exists via wp_remote_head, downloads it, and sideloads it as a WordPress attachment.
  3. Verify no other methods in the class reference `sideload_gravatar`.
  </action>
  <verify>
  - `grep -r "gravatar\|sideload_gravatar" includes/class-rest-people.php` returns no results
  - `php -l includes/class-rest-people.php` passes syntax check
  </verify>
  <done>The Gravatar REST endpoint no longer exists. POST to `/rondo/v1/people/{id}/gravatar` would return 404. No Gravatar-related code remains in class-rest-people.php.</done>
</task>

</tasks>

<verification>
1. All PHP files pass syntax check: `php -l includes/class-google-oauth.php includes/class-rest-google-sheets.php includes/class-rest-people.php functions.php`
2. No references to `Rondo\Calendar\GoogleOAuth` remain anywhere in PHP files
3. No references to `gravatar` remain in `class-rest-people.php`
4. No references to `CONTACTS_SCOPE`, `get_contacts_`, `get_auth_url`, `handle_callback` (non-sheets) remain in `class-google-oauth.php`
5. `npm run build` still succeeds (frontend unchanged in this plan)
</verification>

<success_criteria>
- GoogleOAuth class has exactly 7 methods + 1 constant, namespace is `Rondo\Sheets`
- All 3 files using GoogleOAuth (`functions.php`, `class-rest-google-sheets.php`, `class-google-oauth.php`) reference `Rondo\Sheets`
- Gravatar endpoint deleted from `class-rest-people.php`
- Zero PHP syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/199-oauth-frontend-cleanup/199-01-SUMMARY.md`
</output>
