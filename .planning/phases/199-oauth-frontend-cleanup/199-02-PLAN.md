---
phase: 199-oauth-frontend-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Settings/Settings.jsx
  - src/api/client.js
  - src/hooks/usePeople.js
  - src/components/PersonEditModal.jsx
autonomous: true

must_haves:
  truths:
    - "Settings Connections tab shows only CardDAV and API-toegang subtabs (no calendars or contacts)"
    - "No frontend code references Google Contacts, Calendar connections, or Gravatar"
    - "Default connection subtab is carddav, not calendars"
    - "Frontend builds and lints cleanly"
    - "Person creation no longer attempts Gravatar sideload"
  artifacts:
    - path: "src/pages/Settings/Settings.jsx"
      provides: "Cleaned Settings page without Calendar/Contacts/Gravatar UI"
    - path: "src/api/client.js"
      provides: "API client without dead Calendar/Contacts/Gravatar methods"
    - path: "src/hooks/usePeople.js"
      provides: "Person creation hook without Gravatar sideload"
    - path: "src/components/PersonEditModal.jsx"
      provides: "Person edit modal without Gravatar helper text"
  key_links:
    - from: "src/pages/Settings/Settings.jsx"
      to: "src/api/client.js"
      via: "prmApi.getSheetsStatus (kept), no more prmApi.getGoogleContactsStatus"
      pattern: "prmApi\\.getSheetsStatus"
    - from: "src/hooks/usePeople.js"
      to: "src/api/client.js"
      via: "wpApi.createPerson (kept), no more prmApi.sideloadGravatar"
      pattern: "wpApi\\.createPerson"
---

<objective>
Remove all frontend code related to Google Contacts sync, Calendar connections management, and Gravatar from Settings.jsx, client.js, usePeople.js, and PersonEditModal.jsx.

Purpose: Phase 198 deleted the backend sync classes, and Plan 199-01 strips the OAuth class and Gravatar endpoint. This plan completes the cleanup by removing all frontend UI, hooks, state variables, and API methods that referenced the now-deleted backend functionality. The Connections tab will show only CardDAV and API-toegang subtabs.

Output: A streamlined Settings page, clean API client, and Gravatar-free person creation flow.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-oauth-frontend-cleanup/199-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead API client methods and Gravatar frontend code</name>
  <files>src/api/client.js, src/hooks/usePeople.js, src/components/PersonEditModal.jsx</files>
  <action>
  **In `src/api/client.js`:**

  1. Remove the Gravatar method (line ~102-103):
     ```js
     // Gravatar
     sideloadGravatar: (personId, email) => api.post(`/rondo/v1/people/${personId}/gravatar`, { email }),
     ```
     Also remove the `// Gravatar` comment above it.

  2. Remove all 8 Google Contacts OAuth methods (lines ~223-231):
     ```js
     // Google Contacts OAuth
     getGoogleContactsStatus: ...
     initiateGoogleContactsAuth: ...
     disconnectGoogleContacts: ...
     triggerGoogleContactsImport: ...
     getGoogleContactsUnlinkedCount: ...
     bulkExportGoogleContacts: ...
     triggerContactsSync: ...
     updateContactsSyncFrequency: ...
     ```
     Remove the `// Google Contacts OAuth` comment too.

  3. Remove all 8 Calendar connections methods (lines ~249-257):
     ```js
     // Calendar connections
     getCalendarConnections: ...
     createCalendarConnection: ...
     updateCalendarConnection: ...
     deleteCalendarConnection: ...
     triggerCalendarSync: ...
     getConnectionCalendars: ...
     getGoogleAuthUrl: ...
     testCalDAVConnection: ...
     ```
     Remove the `// Calendar connections` comment too.

     **Important:** `testCalDAVConnection` must also be removed even though the research said to keep it. The backend endpoint `/rondo/v1/calendar/auth/caldav/test` was registered in `class-rest-calendar.php` which was deleted in Phase 198. This API method is dead code.

  4. Keep all Google Sheets OAuth methods (`getSheetsStatus`, `getSheetsAuthUrl`, `disconnectSheets`, `exportPeopleToSheets`) and all meeting methods (`getPersonMeetings`, `logMeetingAsActivity`, `getTodayMeetings`, `getMeetingsForDate`, `getMeetingNotes`, `updateMeetingNotes`) and the `getIcalUrl` method.

  **In `src/hooks/usePeople.js`:**

  1. Remove the Gravatar sideload block inside `useCreatePerson` (lines ~264-270):
     ```js
     // Try to sideload Gravatar if email is provided
     if (data.email) {
       try {
         await prmApi.sideloadGravatar(personId, data.email);
       } catch {
         // Gravatar sideload failed silently - not critical
       }
     }
     ```
  2. Update the JSDoc comment for `useCreatePerson` (line ~217) from "Handles: payload building, Gravatar sideload, birthday creation" to "Handles: payload building, birthday creation"

  **In `src/components/PersonEditModal.jsx`:**

  1. Remove the Gravatar helper text (line ~373):
     ```jsx
     Gravatar wordt automatisch opgehaald indien beschikbaar
     ```
     This is likely a small `<p>` or `<span>` element. Remove just the Gravatar text/element, preserving surrounding form structure.
  </action>
  <verify>
  - `grep -r "sideloadGravatar\|gravatar\|Gravatar" src/api/client.js src/hooks/usePeople.js src/components/PersonEditModal.jsx` returns no results
  - `grep -r "getGoogleContactsStatus\|getCalendarConnections\|testCalDAVConnection\|getGoogleAuthUrl" src/api/client.js` returns no results
  - `grep -r "triggerCalendarSync\|getConnectionCalendars\|createCalendarConnection" src/api/client.js` returns no results
  - `npm run lint` passes
  </verify>
  <done>API client has no dead Google Contacts, Calendar connections, or Gravatar methods. Person creation no longer attempts Gravatar sideload. Person edit modal has no Gravatar text.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Calendar/Contacts UI from Settings.jsx</name>
  <files>src/pages/Settings/Settings.jsx</files>
  <action>
  This is a large surgical removal from Settings.jsx (3160 lines). Work systematically from top to bottom.

  **A. Imports (line 3):**
  Remove `Calendar` from the lucide-react import. Keep `Users` (still used in `ADMIN_SUBTABS` line ~35).

  **B. CONNECTION_SUBTABS constant (lines ~26-31):**
  Remove the `calendars` and `contacts` entries. Result:
  ```js
  const CONNECTION_SUBTABS = [
    { id: 'carddav', label: 'CardDAV', icon: Database },
    { id: 'api-access', label: 'API-toegang', icon: Key },
  ];
  ```

  **C. Default activeSubtab (line ~50):**
  Change `const activeSubtab = urlSubtab || 'calendars';` to `const activeSubtab = urlSubtab || 'carddav';`

  **D. Navigate default on tab change (line ~57):**
  Change `navigate(\`/settings/${tab}/calendars\`);` to `navigate(\`/settings/${tab}/carddav\`);`

  **E. Google Contacts state variables (lines ~97-115):**
  Remove ALL of these useState declarations:
  - `googleContactsStatus`, `googleContactsLoading`, `connectingGoogleContacts`, `disconnectingGoogleContacts`
  - `googleContactsMessage`, `googleContactsImporting`, `googleContactsImportResult`
  - `unlinkedCount`, `isBulkExporting`, `bulkExportResult`
  - `isSyncing`, `syncError`, `syncSuccess`

  **F. useEffect hooks (lines ~160-222):**
  1. Remove the "Fetch Google Contacts status on mount" useEffect (lines ~160-173)
  2. Remove the "Fetch unlinked count when connected with readwrite" useEffect (lines ~175-186)
  3. Remove OR simplify the "Handle OAuth callback messages" useEffect (lines ~188-213):
     - The Google Calendar callback handling (`googleConnected === 'google'`) navigates to `connections/calendars` which no longer exists
     - The Google Contacts callback handling (`googleConnected === 'google-contacts'`) references deleted state
     - The entire useEffect for googleConnected/googleError URL params can be removed since both flows are deleted
     - If this useEffect also handles Sheets OAuth callbacks, keep only that part. Check if there's a separate Sheets callback handler elsewhere.
  4. Remove the "Auto-import when pending flag is set" useEffect (lines ~216-222)

  **G. Handler functions (lines ~269-395 approx):**
  Remove these functions entirely:
  - `handleConnectGoogleContacts`
  - `handleDisconnectGoogleContacts`
  - `handleImportGoogleContacts`
  - `handleBulkExportGoogleContacts`
  - `handleContactsSync`
  - `handleFrequencyChange`

  **H. ConnectionsTab component (around line 2018-2115):**
  1. Remove all Google Contacts props from the component signature/call
  2. Remove `{activeSubtab === 'calendars' && ...}` rendering block
  3. Remove `{activeSubtab === 'contacts' && ...}` rendering block with all its Google Contacts props

  **I. Delete entire sub-components:**
  1. `CalendarsTab` function (starts ~line 952, very large — includes connection management, add modal, CalDAV modal, iCal URL section). Delete the entire function.
  2. `ConnectionsCalendarsSubtab` function (small wrapper, ~lines 2117-2121). Delete entirely.
  3. `SYNC_FREQUENCY_OPTIONS` constant (~lines 2123-2130). Delete entirely.
  4. `ConnectionsContactsSubtab` function (large component, ~lines 2131-2475). Delete entirely.

  **J. Clean up any remaining references:**
  - Search for `CalendarsTab`, `ConnectionsCalendarsSubtab`, `ConnectionsContactsSubtab`, `SYNC_FREQUENCY_OPTIONS` to ensure all references are removed
  - Search for `googleContacts` in any remaining code to catch stray references

  After all removals, verify the Settings component still renders correctly by checking the JSX structure. The ConnectionsTab should now only render CardDAV and API-toegang subtabs.

  Run `npm run lint` to catch any unused imports or variables left behind.
  Run `npm run build` to verify the frontend compiles successfully.
  </action>
  <verify>
  - `grep -r "CalendarsTab\|ConnectionsCalendarsSubtab\|ConnectionsContactsSubtab\|SYNC_FREQUENCY_OPTIONS" src/pages/Settings/Settings.jsx` returns no results
  - `grep -r "googleContacts\|GoogleContacts\|getCalendarConnections" src/pages/Settings/Settings.jsx` returns no results
  - `grep "'calendars'" src/pages/Settings/Settings.jsx` returns no results (the subtab ID is gone)
  - `grep "'contacts'" src/pages/Settings/Settings.jsx` returns no results (the subtab ID is gone)
  - `npm run lint` passes with no warnings
  - `npm run build` succeeds
  </verify>
  <done>Settings.jsx has no Calendar or Contacts subtabs, no CalendarsTab component, no ConnectionsContactsSubtab component, no Google Contacts state. CONNECTION_SUBTABS has only carddav and api-access. Default subtab is carddav. Frontend builds and lints cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run lint` passes with zero warnings
2. `npm run build` succeeds with no errors
3. No references to `getGoogleContactsStatus`, `getCalendarConnections`, `testCalDAVConnection`, `sideloadGravatar` in any src/ file
4. No references to `CalendarsTab`, `ConnectionsContactsSubtab`, `SYNC_FREQUENCY_OPTIONS` in Settings.jsx
5. `CONNECTION_SUBTABS` contains exactly 2 entries: carddav and api-access
6. Default `activeSubtab` is 'carddav'
7. No `Calendar` import from lucide-react in Settings.jsx
</verification>

<success_criteria>
- Frontend builds and lints cleanly (zero warnings, zero errors)
- Settings Connections tab renders only CardDAV and API-toegang subtabs
- No dead API client methods for Contacts, Calendar connections, or Gravatar
- Person creation flow works without Gravatar sideload attempt
- PersonEditModal has no Gravatar reference text
</success_criteria>

<output>
After completion, create `.planning/phases/199-oauth-frontend-cleanup/199-02-SUMMARY.md`
</output>
