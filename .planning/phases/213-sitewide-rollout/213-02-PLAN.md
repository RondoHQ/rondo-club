---
phase: 213-sitewide-rollout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AddressEditModal.jsx
  - src/components/ContactEditModal.jsx
  - src/components/PersonEditModal.jsx
  - src/components/RelationshipEditModal.jsx
  - src/components/WorkHistoryEditModal.jsx
  - src/components/CustomFieldsEditModal.jsx
  - src/components/CommissieEditModal.jsx
  - src/components/TeamEditModal.jsx
  - src/components/FeedbackModal.jsx
  - src/components/FeedbackEditModal.jsx
  - src/components/DashboardCustomizeModal.jsx
  - src/components/ShareModal.jsx
  - src/components/DeleteFieldDialog.jsx
  - src/components/MeetingDetailModal.jsx
  - src/components/Timeline/GlobalTodoModal.jsx
  - src/components/Timeline/TodoModal.jsx
  - src/components/Timeline/CompleteTodoModal.jsx
  - src/components/Timeline/NoteModal.jsx
  - src/components/Timeline/QuickActivityModal.jsx
  - src/components/FieldFormPanel.jsx
  - src/components/AccountCard.jsx
  - src/pages/People/PeopleList.jsx
  - src/pages/People/PersonDetail.jsx
  - src/pages/People/ColumnSettingsModal.jsx
  - src/pages/Teams/TeamDetail.jsx
  - src/pages/Teams/TeamsList.jsx
  - src/pages/Teams/Kaderlijst.jsx
  - src/pages/Commissies/CommissieDetail.jsx
  - src/pages/Commissies/CommissiesList.jsx
  - src/pages/Settings/Settings.jsx
  - src/pages/Settings/CustomFields.jsx
  - src/pages/Settings/RelationshipTypes.jsx
  - src/pages/Settings/FeedbackManagement.jsx
  - src/pages/Profile/Profile.jsx
  - src/pages/MembershipPassScanner.jsx
  - src/router.jsx
autonomous: true
requirements: [ROLL-02, ROLL-04, ROLL-05]

must_haves:
  truths:
    - "In every modal dialog, Save/Submit uses btn-primary and Cancel/Close uses btn-secondary"
    - "Delete/Remove actions in modals and dialogs use btn-danger"
    - "On People, Teams, Commissies list pages, filter clear and utility buttons use btn-tertiary"
    - "On detail pages, back-navigation links use btn-tertiary, share buttons use btn-tertiary"
    - "On Settings pages, Save buttons use btn-primary, utility and add-item buttons use correct tiers"
    - "No redundant inline-flex/items-center/px-4/py-2/rounded-lg classes remain alongside btn-* classes"
  artifacts:
    - path: "src/components/AddressEditModal.jsx"
      provides: "Modal with primary save, secondary cancel"
    - path: "src/pages/Settings/Settings.jsx"
      provides: "Settings page with correct tier hierarchy"
    - path: "src/pages/People/PersonDetail.jsx"
      provides: "Person detail with correct button tiers"
  key_links:
    - from: "src/components/*Modal.jsx"
      to: "src/index.css"
      via: "btn-primary for submit, btn-secondary for cancel"
      pattern: "btn-(primary|secondary)"
    - from: "src/pages/Settings/Settings.jsx"
      to: "src/index.css"
      via: "btn-primary for save, btn-tertiary for utility"
      pattern: "btn-(primary|secondary|tertiary)"
---

<objective>
Apply correct button tier hierarchy to all modal dialogs, People/Teams/Commissies pages, and Settings pages.

Purpose: Modal dialogs are the most consistent pattern — Save=primary, Cancel=secondary. Most modals already follow this but need cleanup of redundant Tailwind classes. People/Teams/Commissies pages have back-navigation, share, and filter buttons that should be tertiary. Settings has many Save buttons (primary) and utility buttons (tertiary).

Output: All 36 files using clean btn-* classes with no redundant inline styles.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-button-css-system/212-01-SUMMARY.md

<interfaces>
<!-- Button CSS tiers from src/index.css (Phase 212) -->
.btn-primary  — gradient fill (Save, Send, Create, Submit)
.btn-secondary — outlined, brand border (Cancel, Close, important-but-not-primary)
.btn-tertiary  — ghost, text-only (Back, Share, Clear filters, utility)
.btn-danger    — red filled (Delete, Remove, permanent destructive)

<!-- Base includes: inline-flex items-center justify-center px-4 py-2 rounded-lg font-medium -->
<!-- Clean up pattern: "btn-secondary flex items-center gap-2" -> "btn-secondary gap-2" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply tier hierarchy to all modal dialogs</name>
  <files>src/components/AddressEditModal.jsx, src/components/ContactEditModal.jsx, src/components/PersonEditModal.jsx, src/components/RelationshipEditModal.jsx, src/components/WorkHistoryEditModal.jsx, src/components/CustomFieldsEditModal.jsx, src/components/CommissieEditModal.jsx, src/components/TeamEditModal.jsx, src/components/FeedbackModal.jsx, src/components/FeedbackEditModal.jsx, src/components/DashboardCustomizeModal.jsx, src/components/ShareModal.jsx, src/components/DeleteFieldDialog.jsx, src/components/MeetingDetailModal.jsx, src/components/Timeline/GlobalTodoModal.jsx, src/components/Timeline/TodoModal.jsx, src/components/Timeline/CompleteTodoModal.jsx, src/components/Timeline/NoteModal.jsx, src/components/Timeline/QuickActivityModal.jsx, src/components/FieldFormPanel.jsx, src/components/AccountCard.jsx, src/pages/People/ColumnSettingsModal.jsx</files>
  <action>
For ALL modal/dialog files, apply this consistent pattern:

**Standard modal footer pattern:**
- Submit/Save/Create button — btn-primary (already correct in most files)
- Cancel/Close/Annuleren button — btn-secondary (already correct in most files)

**Specific cleanup needed per file:**

**All *EditModal.jsx files** (Address, Contact, Person, Relationship, WorkHistory, CustomFields, Commissie, Team, Feedback, FeedbackEdit):
- These all follow the pattern: Cancel=btn-secondary, Save=btn-primary with conditional opacity. Already correct tier assignments.
- Clean up: remove redundant Tailwind classes. The `${!isOnline ? 'opacity-50 cursor-not-allowed' : ''}` template literal on btn-primary should stay (it supplements disabled state for non-button elements, or can stay for clarity).
- Keep the existing btn-primary and btn-secondary assignments.

**CustomFieldsEditModal.jsx** line ~170:
- "Voeg veld toe" (Add field) button uses btn-secondary — change to btn-tertiary (utility add action within a form).

**DeleteFieldDialog.jsx:**
- Archive button (line ~101) — keep btn-secondary (non-destructive alternative)
- "Delete Permanently" button (line ~155-158) — currently has inline styles (bg-red-600 hover:bg-red-700 text-white rounded-lg font-medium disabled:opacity-50 disabled:cursor-not-allowed inline-flex items-center gap-2 transition-colors). Replace entire className with "btn-danger gap-2".
- Cancel button (line ~191) — keep btn-secondary

**DashboardCustomizeModal.jsx:**
- Cancel=btn-secondary, Save=btn-primary — already correct, clean up redundant classes.

**ShareModal.jsx:**
- Close=btn-secondary — already correct.

**MeetingDetailModal.jsx:**
- Close=btn-secondary — keep as is.

**Timeline modals (GlobalTodoModal, TodoModal, NoteModal, QuickActivityModal):**
- All follow Cancel=btn-secondary, Save=btn-primary — already correct.
- TodoModal has TWO footer sections (create mode and edit mode) — both correct.

**CompleteTodoModal.jsx:**
- Cancel button=btn-secondary — correct. The two action buttons (quick complete, complete with activity) are NOT btn-* buttons — they are custom styled option cards. Leave those untouched.

**FieldFormPanel.jsx:**
- Cancel=btn-secondary (line ~1321), Save=btn-primary (line ~1329) — correct.

**AccountCard.jsx:**
- Action button (line ~162) uses btn-primary — correct, keep it.

**ColumnSettingsModal.jsx (People):**
- Done/Close button (line ~312) uses btn-secondary — change to btn-primary (it is the confirming action, the only button).

For all files: remove redundant Tailwind utilities that duplicate what btn-* already provides (inline-flex, items-center, justify-center, px-4, py-2, rounded-lg, font-medium). Keep gap-2, text-sm, and conditional classes.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && grep -c 'bg-red-600 hover:bg-red-700 text-white rounded-lg' src/components/DeleteFieldDialog.jsx | grep -q '^0$' && echo "PASS: No inline danger styles" || echo "FAIL: Inline styles remain"</automated>
  </verify>
  <done>Every modal has Save/Submit=btn-primary, Cancel=btn-secondary, Delete=btn-danger; no inline color/layout overrides on modal buttons; redundant Tailwind classes cleaned up</done>
</task>

<task type="auto">
  <name>Task 2: Apply tier hierarchy to People, Teams, Commissies, Settings, and remaining pages</name>
  <files>src/pages/People/PeopleList.jsx, src/pages/People/PersonDetail.jsx, src/pages/Teams/TeamDetail.jsx, src/pages/Teams/TeamsList.jsx, src/pages/Teams/Kaderlijst.jsx, src/pages/Commissies/CommissieDetail.jsx, src/pages/Commissies/CommissiesList.jsx, src/pages/Settings/Settings.jsx, src/pages/Settings/CustomFields.jsx, src/pages/Settings/RelationshipTypes.jsx, src/pages/Settings/FeedbackManagement.jsx, src/pages/Profile/Profile.jsx, src/pages/MembershipPassScanner.jsx, src/router.jsx</files>
  <action>
Apply tier assignments to each page:

**PeopleList.jsx:**
- "Annuleren" cancel in add-person dialog (line ~527) — keep btn-secondary
- "Toevoegen" submit in add-person dialog (line ~533) — keep btn-primary
- "Wis filters" clear filters (line ~1208) — change from btn-secondary to btn-tertiary
- Export button (line ~1079) — change from btn-secondary to btn-tertiary (utility)

**PersonDetail.jsx:**
- "Terug naar leden" back link (line ~951) — change from btn-secondary to btn-tertiary (navigation)
- Share button (line ~1040) — change from btn-secondary to btn-tertiary (utility)
- Export vCard button (line ~1048) — change from btn-secondary to btn-tertiary (utility)
- All "Bewerken"/"Toevoegen" edit/add buttons throughout (~1295, ~1430, ~1595, ~1603, ~1769, ~1855) — change from btn-secondary to btn-tertiary (these are inline edit triggers, utility weight)

**TeamDetail.jsx:**
- "Terug naar teams" back link (line ~158) — change from btn-secondary to btn-tertiary
- Share button (line ~180) — change from btn-secondary to btn-tertiary

**TeamsList.jsx:**
- "Wis filters" clear filters (line ~556) — change from btn-secondary to btn-tertiary

**Kaderlijst.jsx:**
- Export button (line ~567) — change from btn-secondary to btn-tertiary

**CommissieDetail.jsx:**
- "Terug naar commissies" back link (line ~90) — change from btn-secondary to btn-tertiary
- Share button (line ~112) — change from btn-secondary to btn-tertiary

**CommissiesList.jsx:**
- "Wis filters" clear filters (line ~478) — change from btn-secondary to btn-tertiary

**Settings.jsx** (large file, many buttons):
- All Save/Submit buttons (lines ~658, ~802, ~952, ~1243, ~1831, ~2582) — keep btn-primary
- "Synchroniseer data" and "Webhook" buttons (~1793, ~1828) — change from btn-secondary to btn-tertiary (utility)
- "Webhook aanmaken" (line ~1828) — keep btn-secondary (it creates something important)
- Actually, re-evaluate: webhook create is a secondary action next to the Save primary. Keep btn-secondary.
- "Uitnodiging versturen" invite send buttons (~1903, ~1927) — keep btn-primary (primary action in that section)
- Role management buttons (~1944) — change from btn-secondary to btn-tertiary if utility
- User management action (~2522) — change from btn-secondary to btn-tertiary if utility
- Small text-xs buttons (~1088, ~1107) — change from btn-secondary to btn-tertiary (they are small utility actions)
- Settings save (~3079) — keep btn-primary

**CustomFields.jsx:**
- "Terug" back link (line ~245) — keep btn-primary? No — change to btn-tertiary (it is a back link). The existing btn-primary is incorrect for navigation.
- "Nieuw veld" add button (line ~334) — keep btn-primary (main CTA on page)

**RelationshipTypes.jsx:**
- "Terug" back link (line ~322) — change from btn-primary to btn-tertiary (navigation)
- Add/save buttons (lines ~349, ~390, ~465) — keep btn-primary
- Cancel buttons (lines ~405, ~476) — keep btn-secondary
- Utility "Toevoegen" (line ~248) — keep btn-secondary (it is a meaningful add action)

**FeedbackManagement.jsx:**
- "Terug" back link (line ~69) — change from btn-primary to btn-tertiary (navigation)
- Add button (line ~165) — keep btn-secondary (add action)

**Profile.jsx:**
- Save button (line ~376) — keep btn-primary. Remove redundant "btn" prefix from "btn btn-primary".

**MembershipPassScanner.jsx:**
- Start camera (line ~182) — keep btn-primary
- Stop camera (line ~187) — keep btn-secondary
- Person link (line ~238) — change from btn-secondary to btn-tertiary (navigation)

**router.jsx:**
- Error page "Ga terug" link (line ~52) — change from "btn btn-secondary" to btn-secondary (remove redundant "btn" prefix). Actually this is a go-back navigation — change to btn-tertiary.

For all files: clean up redundant Tailwind classes that duplicate btn-* base styles.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>All People, Teams, Commissies, Settings pages use correct tiers: Save=primary, navigation/back=tertiary, clear filters=tertiary, utility=tertiary, cancel=secondary; no "btn btn-*" double prefixes remain</done>
</task>

</tasks>

<verification>
1. `grep -rn '"btn btn-' src/` returns no results (no double "btn" prefixes)
2. `grep -rn 'bg-red-600 hover:bg-red-700 text-white rounded-lg' src/components/` returns no results
3. `npm run build` succeeds
4. `npm run lint` passes
</verification>

<success_criteria>
- Every modal: Save/Submit=btn-primary, Cancel=btn-secondary, Delete=btn-danger
- People/Teams/Commissies: back links=btn-tertiary, share=btn-tertiary, filters=btn-tertiary
- Settings: save=btn-primary, utility=btn-tertiary, no misassigned navigation buttons
- No inline color overrides remain on any button in these files
- No redundant "btn btn-*" double class prefixes
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/213-sitewide-rollout/213-02-SUMMARY.md`
</output>
