---
phase: 45-accent-colors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Dashboard.jsx
  - src/pages/People/PeopleList.jsx
  - src/pages/Companies/CompaniesList.jsx
  - src/pages/Dates/DatesList.jsx
  - src/pages/Todos/TodosList.jsx
  - src/pages/Login.jsx
  - src/App.jsx
autonomous: true
---

<objective>
Replace hardcoded primary-* and orange-* color classes with accent-* in core list pages.

Purpose: Make the main navigation and list pages respect the user's accent color preference.
Output: Dashboard, People, Companies, Dates, Todos, and Login pages use accent-* colors.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dashboard page</name>
  <files>src/pages/Dashboard.jsx</files>
  <action>
Replace all primary-* and orange-* color classes with accent-* equivalents:

Search patterns to replace:
- `primary-\d{2,3}` -> `accent-{same shade}`
- `orange-\d{2,3}` -> `accent-{same shade}`

Common replacements:
- text-primary-600 -> text-accent-600
- text-primary-400 -> text-accent-400 (dark mode)
- bg-primary-50 -> bg-accent-50
- bg-primary-100 -> bg-accent-100
- bg-primary-500 -> bg-accent-500
- border-primary-* -> border-accent-*
- ring-primary-* -> ring-accent-*
- hover:text-primary-* -> hover:text-accent-*
- dark:text-primary-* -> dark:text-accent-*
- dark:bg-primary-900/30 -> dark:bg-accent-900/30

For orange-* (used in urgency indicators, keep those as-is since they represent semantic meaning, not accent):
- Only replace orange-* when it's used for branding/accent purposes, NOT for urgency colors (yellow/orange/red aging indicators should stay)
  </action>
  <verify>npm run dev, navigate to Dashboard, verify accent colors appear correctly</verify>
  <done>Dashboard uses accent-* colors for interactive elements, urgency indicators remain semantic</done>
</task>

<task type="auto">
  <name>Task 2: Update People and Companies list pages</name>
  <files>src/pages/People/PeopleList.jsx, src/pages/Companies/CompaniesList.jsx</files>
  <action>
Replace all primary-* color classes with accent-* equivalents in both files:

Focus areas (both files have similar patterns):
- Selection toolbar background: bg-primary-50 -> bg-accent-50
- Selected state indicators: border-primary-500 -> border-accent-500
- Checkbox icons: text-primary-600 -> text-accent-600
- Filter pill backgrounds: bg-primary-100 -> bg-accent-100
- Active tab indicators: text-primary-700 -> text-accent-700
- Links/buttons: text-primary-600 hover:text-primary-700 -> text-accent-600 hover:text-accent-700
- Dark mode variants: dark:text-primary-* -> dark:text-accent-*, dark:bg-primary-900/* -> dark:bg-accent-900/*

PeopleList.jsx has ~38 primary-* occurrences
CompaniesList.jsx has ~26 primary-* occurrences

Use find/replace with regex awareness for the shade numbers.
  </action>
  <verify>npm run dev, navigate to People and Companies, verify selection states, filter pills, and links use accent color</verify>
  <done>People and Companies list pages use accent-* colors throughout</done>
</task>

<task type="auto">
  <name>Task 3: Update remaining list pages and App.jsx</name>
  <files>src/pages/Dates/DatesList.jsx, src/pages/Todos/TodosList.jsx, src/pages/Login.jsx, src/App.jsx</files>
  <action>
Replace primary-* and orange-* color classes with accent-* in remaining core pages:

DatesList.jsx (~1 primary-* occurrence):
- Update any primary-* classes

TodosList.jsx (~7 primary-* occurrences, ~4 orange-* occurrences):
- Replace primary-* with accent-*
- For orange-* in TodosList: These are likely for urgency indicators - CHECK CONTEXT before replacing
  - If used for "awaiting" aging colors, keep as orange-*
  - If used for accent/interactive elements, replace with accent-*

Login.jsx (~1 primary-* occurrence):
- Update any primary-* classes (likely button or link styling)

App.jsx (~5 primary-* occurrences):
- Update any primary-* classes (likely loading states, error states)
  </action>
  <verify>npm run dev, navigate to Dates, Todos, Login pages, verify accent colors appear correctly</verify>
  <done>All core list pages and App.jsx use accent-* colors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Dashboard page uses accent colors
- [ ] People list selection toolbar, checkboxes, and filters use accent colors
- [ ] Companies list selection toolbar, checkboxes, and filters use accent colors
- [ ] Todos list filter tabs use accent colors
- [ ] Changing accent color in Settings immediately updates all these pages
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No hardcoded primary-* or accent-intended orange-* classes remain in these files
- Urgency indicators (yellow/orange/red for aging) are preserved
</success_criteria>

<output>
After completion, create `.planning/phases/45-accent-colors/45-02-SUMMARY.md`
</output>
