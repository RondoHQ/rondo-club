---
phase: 212-button-css-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
autonomous: false
requirements:
  - BTN-01
  - BTN-02
  - BTN-03
  - BTN-04
  - BTN-05

must_haves:
  truths:
    - "btn-primary renders as a filled gradient button identical to the current style"
    - "btn-secondary renders as an outlined button with brand border and text, no fill"
    - "btn-tertiary renders as a ghost button with no border, text-only, subtle hover"
    - "btn-danger renders as a red filled button with white text"
    - "All four tiers are visually distinct and legible in both light and dark mode"
  artifacts:
    - path: "src/index.css"
      provides: "All four button tier CSS classes"
      contains: "btn-tertiary"
  key_links:
    - from: "src/index.css"
      to: "Tailwind theme tokens"
      via: "@apply directives using brand color tokens"
      pattern: "btn-(primary|secondary|tertiary|danger)"
---

<objective>
Define the four-tier button CSS system in src/index.css: keep btn-primary as-is, restyle btn-secondary to outlined, create btn-tertiary as ghost, verify btn-danger dark mode, and remove unused btn-danger-outline and btn-glass.

Purpose: Establish a clear visual hierarchy where button prominence signals action weight, before the sitewide rollout in Phase 213.
Output: Updated src/index.css with all four button tiers and proper dark mode variants.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/index.css

<interfaces>
<!-- Current button classes in src/index.css (lines 252-278) -->

The base .btn class (line 252):
```css
.btn {
  @apply inline-flex items-center justify-center px-4 py-2 rounded-lg font-medium transition-all duration-200 ease-in-out hover:-translate-y-0.5 focus:outline-hidden focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed dark:focus:ring-offset-gray-900;
}
```

Brand color tokens available (from Tailwind v4 @theme):
- electric-cyan (#0891b2) — primary brand color
- bright-cobalt (#2563EB) — secondary brand color
- deep-midnight (#1E3A8A) — dark accent
- bg-brand-gradient — gradient from electric-cyan to bright-cobalt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle btn-secondary to outlined, create btn-tertiary ghost, clean up unused classes</name>
  <files>src/index.css</files>
  <action>
In src/index.css, make these changes to the button class block (lines ~252-278):

1. **btn-primary** (BTN-01): DO NOT CHANGE. Keep the current definition exactly as-is (lines 256-259).

2. **btn-secondary** (BTN-02): Restyle from filled to outlined. Replace the current definition with:
   - Use the base .btn shared styles
   - Light mode: transparent background, border border-electric-cyan, text-electric-cyan, hover:bg-electric-cyan/10 for a subtle brand tint on hover, focus:ring-electric-cyan
   - Dark mode: dark:border-electric-cyan-light, dark:text-electric-cyan-light, dark:hover:bg-electric-cyan/15
   - Keep hover:-translate-y-0.5 for consistency with primary

3. **btn-tertiary** (BTN-03): Create NEW ghost class after btn-secondary:
   - Use the base .btn shared styles but WITHOUT border and WITHOUT hover:-translate-y-0.5 (ghost buttons should not lift)
   - Light mode: bg-transparent, text-gray-700, hover:bg-gray-100, focus:ring-gray-400, no border
   - Dark mode: dark:text-gray-300, dark:hover:bg-gray-800
   - No shadow, no border — purely text with a subtle background on hover

4. **btn-danger** (BTN-04, BTN-05): Review and adjust dark mode. The current light mode is correct (red-600 bg, white text, hover red-700). For dark mode ensure:
   - dark:bg-red-600 (not red-500 — keep consistent brightness)
   - dark:hover:bg-red-700 (not red-400 — darker on hover, not lighter)
   - dark:text-white
   - Keep focus:ring-red-500
   - Remove hover:-translate-y-0.5 from btn-danger (destructive buttons should NOT lift — this was already the case, just verify)

5. **Remove** btn-danger-outline (lines 271-273) and btn-glass (lines 276-278) — confirmed unused in the codebase.

6. **DRY cleanup**: Each btn-* class currently repeats the full inline-flex/px-4/py-2/... base styles. Refactor so btn-primary, btn-secondary, btn-tertiary, and btn-danger each use `@apply btn ...` to extend the shared .btn base class, instead of duplicating those styles. This means each variant only adds its unique color/border/shadow styles on top of `.btn`.

Important: The `.btn` base class already has hover:-translate-y-0.5. For btn-tertiary and btn-danger which should NOT lift on hover, override with hover:translate-y-0 to cancel the base behavior.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/rondo/rondo-club && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - btn-primary unchanged from current (filled gradient)
    - btn-secondary is outlined: brand border, brand text, no fill, subtle hover tint
    - btn-tertiary exists: ghost style, no border, text-only, subtle hover bg
    - btn-danger is red filled with corrected dark mode (red-600/red-700, not red-500/red-400)
    - btn-danger-outline and btn-glass removed
    - All four classes use @apply btn to extend the shared base (DRY)
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of button tiers on production</name>
  <files>n/a</files>
  <action>Deploy and verify visually.</action>
  <what-built>Four-tier button CSS system: primary (gradient filled), secondary (outlined), tertiary (ghost), danger (red filled). Removed unused btn-danger-outline and btn-glass. DRY refactor to use shared .btn base.</what-built>
  <how-to-verify>
    1. Deploy to production: `cd /Users/joostdevalk/Code/rondo/rondo-club && git pull && bin/deploy.sh`
    2. Visit production site, navigate to any page with buttons
    3. Check btn-primary: should look identical to before (gradient filled, cyan-to-cobalt)
    4. Check btn-secondary: should be outlined with brand-colored border and text, transparent background, light tint on hover
    5. Check btn-danger: should be red filled with white text
    6. Toggle dark mode and verify all three variants remain legible and distinct
    7. Note: btn-tertiary will not be visible yet since no JSX uses it — that happens in Phase 213
  </how-to-verify>
  <verify>Visual confirmation by user</verify>
  <done>User approves button appearance in both light and dark mode</done>
  <resume-signal>Type "approved" or describe visual issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `grep -c 'btn-tertiary' src/index.css` returns 1 (class exists)
- `grep -c 'btn-danger-outline' src/index.css` returns 0 (removed)
- `grep -c 'btn-glass' src/index.css` returns 0 (removed)
- Visual verification on production confirms all tiers look correct in light + dark mode
</verification>

<success_criteria>
- Four button tier classes (btn-primary, btn-secondary, btn-tertiary, btn-danger) are defined in src/index.css
- btn-primary is visually identical to the pre-change version
- btn-secondary is outlined (no fill, brand border, brand text)
- btn-tertiary is ghost (no border, text-only, subtle hover)
- btn-danger is red filled with proper dark mode
- Unused classes removed
- Build passes
- Production visual check confirms correctness
</success_criteria>

<output>
After completion, create `.planning/phases/212-button-css-system/212-01-SUMMARY.md`
</output>
