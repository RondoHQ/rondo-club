---
phase: 44-dark-mode
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - src/pages/Dashboard.jsx
  - src/pages/People/PeopleList.jsx
  - src/pages/People/PersonDetail.jsx
  - src/pages/Companies/CompaniesList.jsx
  - src/pages/Companies/CompanyDetail.jsx
  - src/pages/Dates/DatesList.jsx
  - src/pages/Todos/TodosList.jsx
  - src/pages/Login.jsx
autonomous: true
---

<objective>
Add dark mode variants to all main page components.

Purpose: Complete dark mode support for all user-facing pages so the entire application renders correctly in dark mode.
Output: All main pages (Dashboard, People, Companies, Dates, Todos, Login) display correctly in both light and dark modes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-dark-mode/44-01-SUMMARY.md

# Color mapping reference (from PRD Multi-Color.md):
# Light → Dark mappings:
# - bg-white → dark:bg-gray-800
# - bg-gray-50 → dark:bg-gray-900
# - bg-gray-100 → dark:bg-gray-700
# - bg-gray-200 → dark:bg-gray-600
# - border-gray-200 → dark:border-gray-700
# - border-gray-300 → dark:border-gray-600
# - text-gray-900 → dark:text-gray-50
# - text-gray-700 → dark:text-gray-200
# - text-gray-600 → dark:text-gray-300
# - text-gray-500 → dark:text-gray-400
# - text-gray-400 → dark:text-gray-500
# - hover:bg-gray-50 → dark:hover:bg-gray-700
# - hover:bg-gray-100 → dark:hover:bg-gray-600
# - divide-gray-100 → dark:divide-gray-700
# - ring-gray-200 → dark:ring-gray-700

# Semantic colors (stay consistent):
# - green (success): dark:text-green-400, dark:bg-green-500/20
# - amber/yellow (warning): dark:text-amber-400, dark:bg-amber-500/20
# - red (error/danger): dark:text-red-400, dark:bg-red-500/20
# - blue (info): dark:text-blue-400, dark:bg-blue-500/20

@src/pages/Dashboard.jsx
@src/pages/People/PeopleList.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark mode to Dashboard and Login pages</name>
  <files>src/pages/Dashboard.jsx, src/pages/Login.jsx</files>
  <action>
Add dark: variants to Dashboard.jsx and Login.jsx following the color mapping patterns.

Dashboard.jsx focus areas:
- Stats cards: bg-white → dark:bg-gray-800, border-gray-200 → dark:border-gray-700
- Card headers and titles: text-gray-900 → dark:text-gray-50
- Secondary text: text-gray-500/600 → dark:text-gray-400/300
- Lists within cards: divide-gray-100 → dark:divide-gray-700
- Hover states on list items
- Badge/pill components
- Empty state messages

Login.jsx focus areas:
- Form container: bg-white → dark:bg-gray-800
- Input fields (should use .input class, but verify)
- Labels and helper text
- Error messages
- Logo/branding area

Preserve all existing classes - only add dark: variants.
  </action>
  <verify>npm run build succeeds; Dashboard renders correctly in dark mode; Login page readable in dark mode</verify>
  <done>Dashboard and Login pages display correctly in both light and dark modes</done>
</task>

<task type="auto">
  <name>Task 2: Add dark mode to People pages</name>
  <files>src/pages/People/PeopleList.jsx, src/pages/People/PersonDetail.jsx</files>
  <action>
Add dark: variants to PeopleList.jsx and PersonDetail.jsx.

PeopleList.jsx focus areas:
- Table header: bg-gray-50 → dark:bg-gray-800/50
- Table rows: alternating bg-white/bg-gray-50 → dark:bg-gray-800/dark:bg-gray-800/50
- Table borders and dividers
- Sticky header styling
- Selection toolbar: bg-white → dark:bg-gray-800
- Empty state
- Pagination area
- Filter chips/buttons

PersonDetail.jsx focus areas (this is the largest file ~101KB):
- Page header with person info
- Tabs and tab panels
- Timeline section
- Sidebar (todos panel)
- Work history cards
- Contact information sections
- Relationship displays
- Mobile slide-up panel
- All card components within detail

This file is large - work through it systematically section by section.
Preserve all existing classes - only add dark: variants.
  </action>
  <verify>npm run build succeeds; PeopleList table readable in dark mode; PersonDetail all sections visible in dark mode</verify>
  <done>PeopleList and PersonDetail pages display correctly in both light and dark modes</done>
</task>

<task type="auto">
  <name>Task 3: Add dark mode to Companies, Dates, and Todos pages</name>
  <files>src/pages/Companies/CompaniesList.jsx, src/pages/Companies/CompanyDetail.jsx, src/pages/Dates/DatesList.jsx, src/pages/Todos/TodosList.jsx</files>
  <action>
Add dark: variants to remaining list and detail pages.

CompaniesList.jsx - similar to PeopleList:
- Table header, rows, borders
- Selection toolbar
- Empty state

CompanyDetail.jsx (if exists):
- Page header
- Company information sections
- Associated people list
- Notes/timeline

DatesList.jsx:
- Date cards/list items
- Calendar/month groupings if present
- Date type badges
- Upcoming/past date styling

TodosList.jsx:
- Todo items with status indicators
- Filter tabs (Open/Awaiting/Completed)
- Due date and urgency colors (preserve semantic colors)
- Person avatars and links
- Empty state

Preserve all existing classes - only add dark: variants.
  </action>
  <verify>npm run build succeeds; all list pages render correctly in dark mode; detail pages readable</verify>
  <done>Companies, Dates, and Todos pages display correctly in both light and dark modes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Dashboard stats cards and lists visible in dark mode
- [ ] Login page form readable in dark mode
- [ ] PeopleList table headers and rows have proper contrast
- [ ] PersonDetail all tabs and sections visible in dark mode
- [ ] CompaniesList table displays correctly
- [ ] DatesList items readable
- [ ] TodosList with status colors visible (urgency colors preserved)
- [ ] No white/light backgrounds bleeding through in dark mode
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- All main pages render correctly in both light and dark modes
- Text remains readable with sufficient contrast
- Semantic colors (success/warning/error) remain distinguishable
  </success_criteria>

<output>
After completion, create `.planning/phases/44-dark-mode/44-02-SUMMARY.md`
</output>
