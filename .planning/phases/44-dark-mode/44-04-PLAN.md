---
phase: 44-dark-mode
plan: 04
type: execute
wave: 3
depends_on: ["44-02"]
files_modified:
  - src/pages/Workspaces/WorkspacesList.jsx
  - src/pages/Workspaces/WorkspaceDetail.jsx
  - src/pages/Workspaces/WorkspaceSettings.jsx
  - src/pages/Workspaces/WorkspaceInviteAccept.jsx
  - src/pages/Settings/Labels.jsx
  - src/pages/Settings/RelationshipTypes.jsx
  - src/pages/Settings/UserApproval.jsx
  - src/components/import/GoogleContactsImport.jsx
  - src/components/import/MonicaImport.jsx
  - src/components/import/VCardImport.jsx
  - src/components/BulkVisibilityModal.jsx
  - src/components/BulkWorkspaceModal.jsx
  - src/components/BulkLabelsModal.jsx
  - src/components/BulkTeamModal.jsx
autonomous: true
---

<objective>
Add dark mode variants to Workspace pages, Settings sub-pages, Import wizards, and Bulk action modals.

Purpose: Complete dark mode coverage for all remaining components to achieve full application dark mode support.
Output: All workspace pages, settings pages, import wizards, and bulk modals display correctly in both light and dark modes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-dark-mode/44-02-SUMMARY.md

# Color mapping reference:
# - bg-white → dark:bg-gray-800
# - bg-gray-50 → dark:bg-gray-900
# - bg-gray-100 → dark:bg-gray-700
# - border-gray-200 → dark:border-gray-700
# - border-gray-300 → dark:border-gray-600
# - text-gray-900 → dark:text-gray-50
# - text-gray-700 → dark:text-gray-200
# - text-gray-600 → dark:text-gray-300
# - text-gray-500 → dark:text-gray-400
# - hover:bg-gray-50 → dark:hover:bg-gray-700
# - divide-gray-100 → dark:divide-gray-700
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark mode to Workspace pages</name>
  <files>src/pages/Workspaces/WorkspacesList.jsx, src/pages/Workspaces/WorkspaceDetail.jsx, src/pages/Workspaces/WorkspaceSettings.jsx, src/pages/Workspaces/WorkspaceInviteAccept.jsx</files>
  <action>
Add dark: variants to all Workspace pages.

WorkspacesList.jsx:
- Workspace cards
- Create workspace button area
- Empty state

WorkspaceDetail.jsx:
- Workspace header/title area
- Member list
- Shared contacts list
- Activity feed

WorkspaceSettings.jsx:
- Settings form sections
- Member management table
- Role selectors
- Danger zone (delete workspace)

WorkspaceInviteAccept.jsx:
- Invitation card
- Accept/decline buttons
- Workspace preview info

Preserve all existing classes - only add dark: variants.
  </action>
  <verify>npm run build succeeds; workspace pages display correctly in dark mode</verify>
  <done>All Workspace pages display correctly in dark mode</done>
</task>

<task type="auto">
  <name>Task 2: Add dark mode to Settings sub-pages</name>
  <files>src/pages/Settings/Labels.jsx, src/pages/Settings/RelationshipTypes.jsx, src/pages/Settings/UserApproval.jsx</files>
  <action>
Add dark: variants to Settings sub-pages.

Labels.jsx:
- Tab switcher (Person labels / Team labels)
- Label list with colored pills
- Add/edit/delete buttons
- Empty state

RelationshipTypes.jsx:
- Relationship type list
- Add/edit forms
- Delete confirmation

UserApproval.jsx:
- Pending users list
- Approval/rejection buttons
- User info cards

Preserve all existing classes - only add dark: variants.
  </action>
  <verify>npm run build succeeds; settings sub-pages readable in dark mode</verify>
  <done>Labels, RelationshipTypes, UserApproval pages display correctly in dark mode</done>
</task>

<task type="auto">
  <name>Task 3: Add dark mode to Import wizards and Bulk modals</name>
  <files>src/components/import/GoogleContactsImport.jsx, src/components/import/MonicaImport.jsx, src/components/import/VCardImport.jsx, src/components/BulkVisibilityModal.jsx, src/components/BulkWorkspaceModal.jsx, src/components/BulkLabelsModal.jsx, src/components/BulkTeamModal.jsx</files>
  <action>
Add dark: variants to Import wizards and Bulk action modals.

Import wizards (GoogleContactsImport, MonicaImport, VCardImport):
- Step indicators
- File upload areas
- Field mapping interface
- Preview tables
- Import progress indicators
- Success/error messages

Bulk modals:
- Modal container and header
- Selection lists
- Search/filter inputs
- Action buttons
- Current selection indicators

Preserve all existing classes - only add dark: variants.
  </action>
  <verify>npm run build succeeds; import wizards and bulk modals functional in dark mode</verify>
  <done>All import wizards and bulk modals display correctly in dark mode</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] WorkspacesList cards visible in dark mode
- [ ] WorkspaceSettings forms usable in dark mode
- [ ] Labels page with colored pills visible in dark mode
- [ ] Import wizard steps readable in dark mode
- [ ] Bulk modals (visibility, workspace, labels, team) display correctly
- [ ] No remaining white/light backgrounds bleeding through
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- All workspace, settings, import, and bulk modal components render correctly in both light and dark modes
  </success_criteria>

<output>
After completion, create `.planning/phases/44-dark-mode/44-04-SUMMARY.md`
</output>
