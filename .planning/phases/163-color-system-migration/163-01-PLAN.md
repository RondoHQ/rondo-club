---
phase: 163-color-system-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/pages/Dashboard.jsx
  - src/pages/Login.jsx
  - src/pages/People/PeopleList.jsx
  - src/pages/People/PersonDetail.jsx
  - src/pages/People/ColumnSettingsModal.jsx
  - src/pages/Teams/TeamsList.jsx
  - src/pages/Teams/TeamDetail.jsx
  - src/pages/Commissies/CommissiesList.jsx
  - src/pages/Commissies/CommissieDetail.jsx
  - src/pages/Todos/TodosList.jsx
  - src/pages/VOG/VOGList.jsx
  - src/pages/Contributie/ContributieList.jsx
  - src/pages/DisciplineCases/DisciplineCasesList.jsx
  - src/pages/Feedback/FeedbackList.jsx
  - src/pages/Feedback/FeedbackDetail.jsx
  - src/pages/Settings/Settings.jsx
  - src/pages/Settings/FeeCategorySettings.jsx
  - src/pages/Settings/FeedbackManagement.jsx
  - src/pages/Settings/CustomFields.jsx
  - src/pages/Settings/Labels.jsx
  - src/pages/Settings/RelationshipTypes.jsx
  - src/components/layout/Layout.jsx
  - src/components/InlineFieldInput.jsx
  - src/components/PersonEditModal.jsx
  - src/components/TeamEditModal.jsx
  - src/components/CommissieEditModal.jsx
  - src/components/ContactEditModal.jsx
  - src/components/AddressEditModal.jsx
  - src/components/WorkHistoryEditModal.jsx
  - src/components/RelationshipEditModal.jsx
  - src/components/CustomFieldsSection.jsx
  - src/components/CustomFieldsEditModal.jsx
  - src/components/CustomFieldColumn.jsx
  - src/components/FieldFormPanel.jsx
  - src/components/ShareModal.jsx
  - src/components/BulkLabelsModal.jsx
  - src/components/FeedbackModal.jsx
  - src/components/FeedbackEditModal.jsx
  - src/components/MeetingDetailModal.jsx
  - src/components/DashboardCard.jsx
  - src/components/DashboardCustomizeModal.jsx
  - src/components/FinancesCard.jsx
  - src/components/DisciplineCaseTable.jsx
  - src/components/DeleteFieldDialog.jsx
  - src/components/TabButton.jsx
  - src/components/Pagination.jsx
  - src/components/LoadingSpinner.jsx
  - src/components/TodoItem.jsx
  - src/components/IOSInstallModal.jsx
  - src/components/InstallPrompt.jsx
  - src/components/ReloadPrompt.jsx
  - src/components/PullToRefreshWrapper.jsx
  - src/components/RichTextEditor.jsx
  - src/components/Timeline/TimelineView.jsx
  - src/components/Timeline/QuickActivityModal.jsx
  - src/components/Timeline/TodoModal.jsx
  - src/components/Timeline/GlobalTodoModal.jsx
  - src/components/Timeline/NoteModal.jsx
  - src/components/Timeline/CompleteTodoModal.jsx
autonomous: true

must_haves:
  truths:
    - "No accent-* CSS class references remain in any JSX/JS component file"
    - "No accent-* CSS variable definitions remain in src/index.css (data-accent system deleted)"
    - "Component classes (.btn-primary, .input) use brand color tokens instead of accent-*"
    - "npm run build succeeds with zero accent-* references in generated CSS output"
  artifacts:
    - path: "src/index.css"
      provides: "Clean CSS with brand tokens only, no data-accent system"
      contains: "electric-cyan"
    - path: "src/pages/Dashboard.jsx"
      provides: "Dashboard with brand colors"
      contains: "electric-cyan"
  key_links:
    - from: "src/index.css @theme block"
      to: "component accent-* class references"
      via: "Tailwind utility class generation"
      pattern: "electric-cyan|bright-cobalt"
---

<objective>
Replace all accent-* CSS class references across 61 React component files with fixed brand color equivalents (electric-cyan, bright-cobalt, cyan-50/100), and delete the entire data-accent CSS variable system (~240 lines) from src/index.css.

Purpose: This is the bulk mechanical migration that eliminates the dynamic accent color system from the frontend, replacing it with the fixed brand colors defined in Phase 162.

Output: All React components use brand color tokens; src/index.css contains only brand tokens (no accent-* definitions); production build contains zero accent-* classes.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-foundation-tailwind-v4-tokens/162-01-SUMMARY.md
@.planning/phases/163-color-system-migration/163-RESEARCH.md
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all accent-* class references in React component files with brand color equivalents</name>
  <files>All 59 JSX files listed in files_modified (everything except src/index.css and src/hooks/useTheme.js)</files>
  <action>
Perform systematic find-and-replace across all React component files. Use the following mapping for CSS class replacements:

**Primary color mappings (apply in this order to avoid double-replacements):**
1. `accent-800` -> `deep-midnight` (dark backgrounds/text)
2. `accent-700` -> `bright-cobalt` (hover states, darker interactive)
3. `accent-600` -> `electric-cyan` (primary interactive color - most common)
4. `accent-500` -> `electric-cyan` (default primary)
5. `accent-400` -> `electric-cyan` (dark mode primary - used in `dark:text-accent-400` patterns)
6. `accent-300` -> `electric-cyan-light` (lighter variant)
7. `accent-200` -> `cyan-200` (light highlights)
8. `accent-100` -> `cyan-100` (subtle highlights)
9. `accent-50` -> `cyan-50` (light backgrounds)
10. `accent-900` -> `obsidian` (if any, very dark)

**Important nuances:**
- For `dark:text-accent-400` patterns, replace with `dark:text-electric-cyan` (the brand primary works well on dark backgrounds at its native OKLCH lightness)
- For `dark:text-accent-300` patterns, replace with `dark:text-electric-cyan-light`
- For `dark:bg-accent-800` patterns, replace with `dark:bg-deep-midnight`
- For `dark:bg-accent-900/30` or similar opacity patterns, replace accent part: `dark:bg-deep-midnight/30`
- For `dark:border-accent-500` patterns, replace with `dark:border-electric-cyan`
- For `dark:hover:text-accent-300` patterns, replace with `dark:hover:text-electric-cyan-light`
- For `ring-accent-500` patterns, replace with `ring-electric-cyan`
- For `border-accent-*` patterns, follow same scale mapping

**Do NOT touch:**
- `src/hooks/useTheme.js` (handled in plan 02)
- `src/index.css` (handled in Task 2 of this plan)
- Any variable names or comments containing "accent" (only CSS class names)

**Process:**
1. For each file in the list, read current content
2. Apply replacements using the mapping above
3. Verify no `accent-[0-9]` class references remain (check with regex `accent-\d`)
4. Pay special attention to template literals and conditional classNames

**Verification after all files:**
Run `grep -r "accent-[0-9]" src/ --include="*.jsx" --include="*.js" | grep -v useTheme | grep -v index.css` — should return zero results.
  </action>
  <verify>
Run: `cd /Users/joostdevalk/Code/rondo/rondo-club && grep -rn "accent-[0-9]" src/ --include="*.jsx" | wc -l` — must return 0.
Also: `cd /Users/joostdevalk/Code/rondo/rondo-club && grep -rn "accent-[0-9]" src/ --include="*.jsx" | head -20` — must return nothing.
  </verify>
  <done>Zero accent-* CSS class references remain in any React component file (JSX). All replaced with electric-cyan, bright-cobalt, cyan-50, cyan-100, electric-cyan-light, deep-midnight, or obsidian equivalents.</done>
</task>

<task type="auto">
  <name>Task 2: Delete data-accent CSS system and update component classes in src/index.css</name>
  <files>src/index.css</files>
  <action>
Make the following changes to `src/index.css`:

**1. Remove accent-* tokens from @theme block (lines 11-21):**
Delete the 10 `--color-accent-*` lines from the `@theme` block. Keep the brand color tokens (lines 23-28) and typography (line 31).

**2. Delete the entire data-accent CSS system (lines 155-393):**
Delete everything from `/* Theme custom properties for accent colors */` through the closing `}` of the last `.dark[data-accent="rose"]` block. This is approximately 240 lines of CSS that defines 9 color variants with dark mode inversions.

**3. Update `.timeline-content a` (around line 117):**
Replace:
```css
@apply text-accent-600 hover:text-accent-700 underline dark:text-accent-400 dark:hover:text-accent-300;
```
With:
```css
@apply text-electric-cyan hover:text-bright-cobalt underline dark:text-electric-cyan dark:hover:text-electric-cyan-light;
```

**4. Update `.btn-primary` class:**
Replace accent-* references:
- `bg-accent-600` -> `bg-electric-cyan`
- `hover:bg-accent-700` -> `hover:bg-bright-cobalt`
- `focus:ring-accent-500` -> `focus:ring-electric-cyan`
- `dark:text-accent-400` -> `dark:text-electric-cyan`
- `dark:border-accent-500` -> `dark:border-electric-cyan`
- `dark:hover:text-accent-300` -> `dark:hover:text-electric-cyan-light`

**5. Update `.btn-secondary` class:**
Replace `focus:ring-accent-500` -> `focus:ring-electric-cyan`

**6. Update `.input` class:**
Replace:
- `focus:ring-accent-500` -> `focus:ring-electric-cyan`
- `focus:border-accent-500` -> `focus:border-electric-cyan`

**After changes, the file should:**
- Start with `@import "tailwindcss"`
- Have `@theme` block with only brand colors and typography
- Have `@utility bg-brand-gradient`
- Have component classes using brand colors
- Have NO `accent-` references anywhere
  </action>
  <verify>
Run: `cd /Users/joostdevalk/Code/rondo/rondo-club && grep -c "accent-" src/index.css` — must return 0.
Run: `cd /Users/joostdevalk/Code/rondo/rondo-club && grep -c "data-accent" src/index.css` — must return 0.
Run: `cd /Users/joostdevalk/Code/rondo/rondo-club && npm run build` — must succeed (exit code 0).
Run: `cd /Users/joostdevalk/Code/rondo/rondo-club && grep -c "color-accent" dist/assets/*.css` — must return 0.
  </verify>
  <done>src/index.css contains zero accent-* references. Data-accent system (240 lines) deleted. Component classes use brand tokens. Production build generates zero accent-* CSS output.</done>
</task>

</tasks>

<verification>
1. `grep -rn "accent-[0-9]" src/ --include="*.jsx" --include="*.js" | grep -v useTheme.js` returns zero results
2. `grep -c "accent-" src/index.css` returns 0
3. `grep -c "data-accent" src/index.css` returns 0
4. `npm run build` succeeds
5. `grep "color-accent" dist/assets/*.css` returns zero matches
6. `grep "electric-cyan" src/index.css` returns matches (brand tokens present)
</verification>

<success_criteria>
- All 61 component files migrated from accent-* to brand color classes
- src/index.css data-accent system completely removed (~240 lines deleted)
- @theme block contains only brand tokens (no accent-* definitions)
- Component classes (.btn-primary, .input, etc.) use brand colors
- Production build contains zero accent-* CSS classes
- Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/163-color-system-migration/163-01-SUMMARY.md`
</output>
