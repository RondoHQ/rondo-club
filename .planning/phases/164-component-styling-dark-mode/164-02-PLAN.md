---
phase: 164-component-styling-dark-mode
plan: 02
type: execute
wave: 2
depends_on: ["164-01"]
files_modified:
  - src/pages/Dashboard.jsx
  - src/pages/People/PersonDetail.jsx
  - src/pages/Teams/TeamDetail.jsx
  - src/pages/Commissies/CommissieDetail.jsx
  - src/pages/Todos/TodosList.jsx
  - src/pages/DisciplineCases/DisciplineCasesList.jsx
  - src/pages/Feedback/FeedbackList.jsx
  - src/pages/Feedback/FeedbackDetail.jsx
  - src/pages/Settings/Settings.jsx
  - src/pages/Settings/CustomFields.jsx
  - src/pages/Settings/Labels.jsx
  - src/pages/Settings/RelationshipTypes.jsx
  - src/pages/Settings/FeedbackManagement.jsx
  - src/pages/Settings/FeeCategorySettings.jsx
  - src/components/DashboardCard.jsx
  - src/components/FinancesCard.jsx
  - src/components/VOGCard.jsx
  - src/components/CustomFieldsSection.jsx
autonomous: true

must_haves:
  truths:
    - "Page-level h1 headings display gradient text treatment"
    - "Section h2 headings on detail and list pages display gradient text treatment"
    - "Dashboard welcome heading displays gradient text treatment"
    - "DashboardCard section headings display gradient text treatment"
    - "Gradient headings remain readable in dark mode"
    - "Modal titles, filter labels, and error headings do NOT use gradient text"
  artifacts:
    - path: "src/pages/Dashboard.jsx"
      provides: "Gradient welcome heading"
      contains: "text-brand-gradient"
    - path: "src/pages/People/PersonDetail.jsx"
      provides: "Gradient page title and section headings"
      contains: "text-brand-gradient"
    - path: "src/components/DashboardCard.jsx"
      provides: "Gradient card section heading"
      contains: "text-brand-gradient"
  key_links:
    - from: "JSX heading className"
      to: "src/index.css @utility text-brand-gradient"
      via: "Tailwind class reference"
      pattern: "text-brand-gradient"
---

<objective>
Apply the text-brand-gradient utility class to heading elements across page and section components for visual brand identity.

Purpose: Make heading elements display the cyan-to-cobalt gradient text treatment as specified in COMP-06, applied selectively to prominent headings while preserving readability for small utility headings.

Output: Updated JSX components with text-brand-gradient class on page titles (h1) and section headings (h2) throughout the application.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-component-styling-dark-mode/164-01-SUMMARY.md
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply gradient text to page-level h1 headings</name>
  <files>
    src/pages/Dashboard.jsx
    src/pages/People/PersonDetail.jsx
    src/pages/Teams/TeamDetail.jsx
    src/pages/Todos/TodosList.jsx
    src/pages/DisciplineCases/DisciplineCasesList.jsx
    src/pages/Feedback/FeedbackList.jsx
    src/pages/Feedback/FeedbackDetail.jsx
    src/pages/Commissies/CommissieDetail.jsx
    src/pages/Settings/CustomFields.jsx
    src/pages/Settings/Labels.jsx
    src/pages/Settings/RelationshipTypes.jsx
    src/pages/Settings/FeedbackManagement.jsx
  </files>
  <action>
Add `text-brand-gradient` class to page-level h1 headings and the Dashboard welcome h2. These are the prominent page titles that benefit from the gradient brand treatment.

**Rules for applying gradient text:**
- ADD `text-brand-gradient` to the className
- REMOVE `text-gray-900` and `dark:text-gray-100` / `dark:text-gray-50` from the same element (the gradient utility includes a cyan fallback color and the gradient is visible in both light and dark modes; explicit text colors would conflict with `-webkit-text-fill-color: transparent`)
- KEEP all other classes (text-2xl, font-bold, flex, items-center, gap-2, mb-*, etc.)
- Do NOT apply to "Toegang geweigerd" (access denied) error headings - those should stay as-is
- Do NOT apply to the Layout.jsx navbar h1 (too small, different context)

**Specific changes by file:**

1. **Dashboard.jsx** - Welcome heading h2 (line ~314):
   - Add `text-brand-gradient`, remove `text-gray-900 dark:text-gray-50`

2. **PersonDetail.jsx** - Person name h1 (line ~997):
   - Add `text-brand-gradient`, remove `dark:text-gray-50`

3. **TeamDetail.jsx** - Team name h1 (line ~272):
   - Add `text-brand-gradient`

4. **TodosList.jsx** - "Taken" h1 (line ~207):
   - Add `text-brand-gradient`

5. **DisciplineCasesList.jsx** - Page title h1 (line ~116):
   - Add `text-brand-gradient`, remove `text-gray-900 dark:text-gray-100`

6. **FeedbackList.jsx** - "Feedback" h1 (line ~90):
   - Add `text-brand-gradient`

7. **FeedbackDetail.jsx** - Feedback title h1 (line ~120):
   - Add `text-brand-gradient`, remove `text-gray-900 dark:text-gray-100`

8. **CommissieDetail.jsx** - Commissie name h1 (line ~268):
   - Add `text-brand-gradient`

9. **CustomFields.jsx** - Page title h1 (line ~328, NOT the "Toegang geweigerd" at line ~153):
   - Add `text-brand-gradient`

10. **Labels.jsx** - Page title h1 (line ~164, NOT the "Toegang geweigerd" at line ~33):
    - Add `text-brand-gradient`

11. **RelationshipTypes.jsx** - "Relatietypes" h1 (line ~342, NOT the "Toegang geweigerd" at line ~160):
    - Add `text-brand-gradient`, remove `dark:text-gray-50`

12. **FeedbackManagement.jsx** - "Feedbackbeheer" h1 (line ~161, NOT the "Toegang geweigerd" at line ~49):
    - Add `text-brand-gradient`, remove `dark:text-gray-50`
  </action>
  <verify>
Run `npm run build` - build must succeed. Then grep for `text-brand-gradient` in the modified files to confirm the class was applied. Verify that "Toegang geweigerd" headings do NOT contain `text-brand-gradient`.
  </verify>
  <done>
All page-level h1 titles (excluding error headings and navbar) and the Dashboard welcome h2 display the gradient text treatment via the text-brand-gradient class. Conflicting text color classes have been removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply gradient text to section h2 headings on detail and card pages</name>
  <files>
    src/pages/People/PersonDetail.jsx
    src/pages/Teams/TeamDetail.jsx
    src/pages/Commissies/CommissieDetail.jsx
    src/pages/Settings/Settings.jsx
    src/components/DashboardCard.jsx
    src/components/FinancesCard.jsx
    src/components/VOGCard.jsx
    src/components/CustomFieldsSection.jsx
  </files>
  <action>
Apply `text-brand-gradient` to section h2 headings within detail pages, dashboard cards, and settings sections. These are prominent section titles that structure content areas.

**Rules (same as Task 1):**
- ADD `text-brand-gradient` to className
- REMOVE `text-gray-900` and `dark:text-gray-100` / `dark:text-gray-50` from the same element
- KEEP all other classes
- Do NOT apply to modal h2 titles (those in edit modals stay as-is for consistency with modal design)
- Do NOT apply to h3 elements used as filter category labels (text-xs font-semibold uppercase tracking-wide pattern)
- Do NOT apply to h3 elements used as metadata labels (text-sm font-medium text-gray-500 pattern)

**Specific changes by file:**

1. **PersonDetail.jsx** - Section h2s:
   - "Contactgegevens" h2 (line ~1194): add `text-brand-gradient`
   - "Adressen" h2 (line ~1299): add `text-brand-gradient`
   - "Relaties" h2 (line ~1344): add `text-brand-gradient`
   - "Tijdlijn" h2 (line ~1443): add `text-brand-gradient`
   - "Functiegeschiedenis" h2 (line ~1480): add `text-brand-gradient`
   - Custom fields section h2 (line ~1533): add `text-brand-gradient`
   - "Tuchtzaken" h2 (line ~1581): add `text-brand-gradient`
   - "Taken" h2 (line ~1607 and ~1682): add `text-brand-gradient`

2. **TeamDetail.jsx** - Section h2s:
   - All section h2s with `font-semibold mb-4 flex items-center` pattern (lines ~297, ~350, ~384, ~434, ~486): add `text-brand-gradient`
   - "Contactgegevens" h2 (line ~530): add `text-brand-gradient`

3. **CommissieDetail.jsx** - Section h2s:
   - All section h2s with `font-semibold mb-4 flex items-center` pattern (lines ~287, ~324, ~358, ~395, ~447): add `text-brand-gradient`
   - "Contactgegevens" h2 (line ~491): add `text-brand-gradient`

4. **Settings.jsx** - Settings section h2s (the ones with `text-lg font-semibold mb-4` pattern):
   - "Clubconfiguratie" h2 (line ~814): add `text-brand-gradient`, remove `dark:text-gray-100`
   - "Kleurenschema" h2 (line ~872): add `text-brand-gradient`, remove `dark:text-gray-100`
   - "Agendakoppelingen" h2 and related section headings (lines ~909, ~1217, ~1301, ~1343): add `text-brand-gradient`, remove `dark:text-gray-100`
   - "Google Contacten" h2 (line ~2214): add `text-brand-gradient`, remove `dark:text-gray-100`
   - "CardDAV-synchronisatie" h2 (line ~2556): add `text-brand-gradient`, remove `dark:text-gray-100`
   - "Meldingen" h2 (line ~2633): add `text-brand-gradient`, remove `dark:text-gray-100`
   - "Applicatiewachtwoorden" h2 (line ~2737): add `text-brand-gradient`, remove `dark:text-gray-100`
   - "API-informatie" h2 (line ~2849): add `text-brand-gradient`, remove `dark:text-gray-100`
   - "Gegevens exporteren" h2 (line ~2881): add `text-brand-gradient`
   - "Configuratie" h2 (line ~3012): add `text-brand-gradient`
   - "Systeemacties" h2 (line ~3046): add `text-brand-gradient`
   - "Over" h2 (line ~3397): add `text-brand-gradient`
   - Do NOT apply to h3 elements inside Settings (modal titles, subsection labels)

5. **DashboardCard.jsx** - Card title h2 (line ~35):
   - Add `text-brand-gradient`, remove `dark:text-gray-50`

6. **FinancesCard.jsx** - "Financieel" h2s (lines ~52 and ~77):
   - Add `text-brand-gradient`

7. **VOGCard.jsx** - "VOG Status" h2 (line ~83):
   - Add `text-brand-gradient`

8. **CustomFieldsSection.jsx** - "Aangepaste velden" h2 (line ~462):
   - Add `text-brand-gradient`
  </action>
  <verify>
Run `npm run build` and `npm run lint` - both must pass. Count occurrences of `text-brand-gradient` in all JSX files using grep - should be approximately 40-50 total instances across the modified files. Verify no modal h2 titles contain `text-brand-gradient` by checking edit modal files.
  </verify>
  <done>
Section h2 headings on detail pages, settings sections, and dashboard cards display gradient text treatment. Modal titles, filter labels, and metadata headings remain unchanged with their original styling.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run lint` passes with 0 warnings
3. `text-brand-gradient` appears on all page-level h1 titles (excluding error pages and navbar)
4. `text-brand-gradient` appears on section h2 headings in detail, settings, and card components
5. No `text-brand-gradient` on modal h2 titles (edit modals, confirm modals)
6. No `text-brand-gradient` on filter h3 labels (text-xs uppercase tracking-wide pattern)
7. No `text-brand-gradient` on "Toegang geweigerd" error headings
8. Gradient headings do not have conflicting text-gray-900/dark:text-gray-100 classes
</verification>

<success_criteria>
- Build succeeds with gradient heading classes applied
- COMP-06 requirement satisfied: heading elements display gradient text treatment
- Selective application preserves accessibility (modal titles, small labels, error states unaffected)
- Dark mode headings remain visible via the gradient or cyan fallback color
</success_criteria>

<output>
After completion, create `.planning/phases/164-component-styling-dark-mode/164-02-SUMMARY.md`
</output>
