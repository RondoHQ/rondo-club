---
phase: 198-backend-sync-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/class-google-contacts-sync.php
  - includes/class-google-contacts-export.php
  - includes/class-google-contacts-api-import.php
  - includes/class-google-contacts-connection.php
  - includes/class-rest-google-contacts.php
  - functions.php
  - includes/class-wp-cli.php
autonomous: true

must_haves:
  truths:
    - "No Google Contacts sync classes exist in the codebase"
    - "Google Contacts REST endpoints return 404 (routes no longer registered)"
    - "functions.php no longer loads any Google Contacts sync classes"
    - "Google Contacts cron hook (rondo_google_contacts_sync) is deregistered"
    - "PHP produces no errors after removal"
  artifacts:
    - path: "functions.php"
      provides: "Theme init without Google Contacts sync references"
    - path: "includes/class-wp-cli.php"
      provides: "WP-CLI without google-contacts command"
  key_links:
    - from: "functions.php"
      to: "includes/"
      via: "use statements and class instantiation"
      pattern: "GoogleContacts"
---

<objective>
Remove all Google Contacts sync backend code: delete 5 class files, remove all references from functions.php (imports, class aliases, init calls, cron scheduling), remove the google-contacts WP-CLI command, and deregister the cron hook.

Purpose: First step of v29.0 Made in Europe -- removing non-European service dependencies.
Output: Google Contacts sync is completely gone from the PHP backend.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/198-backend-sync-removal/198-CONTEXT.md
@functions.php
@includes/class-wp-cli.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Google Contacts sync class files and remove from functions.php</name>
  <files>
    includes/class-google-contacts-sync.php
    includes/class-google-contacts-export.php
    includes/class-google-contacts-api-import.php
    includes/class-google-contacts-connection.php
    includes/class-rest-google-contacts.php
    functions.php
  </files>
  <action>
    1. Delete these 5 files:
       - `includes/class-google-contacts-sync.php` (Rondo\Contacts\GoogleContactsSync)
       - `includes/class-google-contacts-export.php` (Rondo\Export\GoogleContactsExport)
       - `includes/class-google-contacts-api-import.php` (Rondo\Import\GoogleContactsAPI)
       - `includes/class-google-contacts-connection.php` (Rondo\Contacts\GoogleContactsConnection)
       - `includes/class-rest-google-contacts.php` (Rondo\REST\GoogleContacts)

    2. In `functions.php`, remove these `use` statements:
       - `use Rondo\REST\GoogleContacts as RESTGoogleContacts;`
       - `use Rondo\Import\GoogleContactsAPI;`
       - `use Rondo\Export\GoogleContactsExport;`
       - `use Rondo\Contacts\GoogleContactsSync;`

    3. Remove these backward compatibility class aliases from functions.php:
       - The `RONDO_Google_Contacts_API_Import` alias block (GoogleContactsAPI)
       - The `RONDO_Google_Contacts_Export` alias block (GoogleContactsExport)

    4. In `rondo_init()`, remove:
       - `GoogleContactsExport::init();` call (inside the `$is_admin || $is_rest || $is_cron` block)
       - `new RESTGoogleContacts();` call (inside the `$is_rest` block)
       - `new GoogleContactsSync();` call (near bottom, after "Google Contacts sync" comment)

    5. In `rondo_theme_activation()`, remove:
       - The `$contacts_sync = new GoogleContactsSync(); $contacts_sync->schedule_sync();` block

    6. In `rondo_theme_deactivation()`, remove:
       - The `$contacts_sync = new GoogleContactsSync(); $contacts_sync->unschedule_sync();` block

    7. Add cron hook deregistration: At the bottom of `functions.php` (or in rondo_init), add a one-time call to clear the scheduled hook:
       ```php
       // Clear orphaned Google Contacts sync cron hook (removed in v29.0)
       if ( wp_next_scheduled( 'rondo_google_contacts_sync' ) ) {
           wp_clear_scheduled_hook( 'rondo_google_contacts_sync' );
       }
       ```
       Place this after the `add_action( 'plugins_loaded', 'rondo_init', 5 );` line, as a standalone block.
  </action>
  <verify>
    - Run `php -l functions.php` to verify no syntax errors
    - Confirm the 5 deleted files no longer exist: `ls includes/class-google-contacts-sync.php includes/class-google-contacts-export.php includes/class-google-contacts-api-import.php includes/class-google-contacts-connection.php includes/class-rest-google-contacts.php` should show "No such file"
    - Grep for any remaining references: `grep -r "GoogleContactsSync\|GoogleContactsExport\|GoogleContactsAPI\|GoogleContactsConnection\|RESTGoogleContacts" functions.php` should return nothing
  </verify>
  <done>5 Google Contacts class files deleted, all references removed from functions.php, cron hook deregistered</done>
</task>

<task type="auto">
  <name>Task 2: Remove google-contacts WP-CLI command</name>
  <files>includes/class-wp-cli.php</files>
  <action>
    In `includes/class-wp-cli.php`:

    1. Remove these `use` statements at the top:
       - `use Rondo\Contacts\GoogleContactsSync;`
       - `use Rondo\Contacts\GoogleContactsConnection;`
       - `use Rondo\Import\GoogleContactsAPI;`

    2. Delete the entire `RONDO_Google_Contacts_CLI_Command` class (search for `class RONDO_Google_Contacts_CLI_Command` and remove the entire class including its closing brace).

    3. Remove the WP_CLI::add_command registration line:
       - `WP_CLI::add_command( 'rondo google-contacts', 'RONDO_Google_Contacts_CLI_Command' );`

    4. Run `npm run lint` and `npm run build` to verify the frontend still builds cleanly (the PHP changes should not affect the frontend, but verify as a safety check).
  </action>
  <verify>
    - `php -l includes/class-wp-cli.php` shows no syntax errors
    - `grep -r "Google_Contacts_CLI\|google-contacts.*CLI\|GoogleContactsSync\|GoogleContactsConnection\|GoogleContactsAPI" includes/class-wp-cli.php` returns nothing
    - `npm run build` completes successfully
  </verify>
  <done>Google Contacts WP-CLI command removed, no PHP syntax errors, frontend builds cleanly</done>
</task>

</tasks>

<verification>
1. `php -l functions.php` -- no syntax errors
2. `php -l includes/class-wp-cli.php` -- no syntax errors
3. None of the 5 deleted files exist
4. `grep -rn "GoogleContactsSync\|GoogleContactsExport\|GoogleContactsAPI\|GoogleContactsConnection\|RESTGoogleContacts\|RONDO_Google_Contacts" functions.php includes/class-wp-cli.php` returns nothing
5. `npm run build` succeeds
</verification>

<success_criteria>
- 5 Google Contacts sync class files deleted from `includes/`
- All Google Contacts references removed from `functions.php` (imports, aliases, init, cron scheduling)
- Google Contacts WP-CLI command removed from `class-wp-cli.php`
- `rondo_google_contacts_sync` cron hook deregistered
- PHP syntax checks pass on all modified files
- Frontend builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/198-backend-sync-removal/198-01-SUMMARY.md`
</output>
