---
phase: 198-backend-sync-removal
plan: 02
type: execute
wave: 2
depends_on: ["198-01"]
files_modified:
  - includes/class-calendar-sync.php
  - includes/class-google-calendar-provider.php
  - includes/class-calendar-connections.php
  - includes/class-rest-calendar.php
  - functions.php
  - includes/class-wp-cli.php
  - includes/class-auto-title.php
autonomous: true

must_haves:
  truths:
    - "No Calendar sync classes exist in the codebase (Sync, GoogleProvider, Connections, REST Calendar)"
    - "Calendar sync REST endpoints return 404 (routes no longer registered)"
    - "functions.php no longer loads any Calendar sync classes"
    - "Calendar sync cron hook (rondo_calendar_sync) is deregistered"
    - "Calendar Matcher and CalDAV Provider still function (not deleted)"
    - "PHP produces no errors after removal"
    - "ESLint and build pass cleanly"
  artifacts:
    - path: "functions.php"
      provides: "Theme init without Calendar sync references"
    - path: "includes/class-wp-cli.php"
      provides: "WP-CLI without calendar sync commands"
    - path: "includes/class-calendar-matcher.php"
      provides: "Calendar Matcher still intact (shared utility)"
    - path: "includes/class-caldav-provider.php"
      provides: "CalDAV Provider still intact"
  key_links:
    - from: "functions.php"
      to: "includes/"
      via: "use statements and class instantiation"
      pattern: "Calendar\\\\(Sync|Connections|GoogleProvider)|RESTCalendar"
    - from: "includes/class-auto-title.php"
      to: "includes/class-calendar-matcher.php"
      via: "Matcher::on_person_saved call"
      pattern: "on_person_saved"
---

<objective>
Remove all Calendar sync backend code: delete 4 class files (Sync, GoogleProvider, Connections, REST Calendar), remove all references from functions.php, clean up WP-CLI calendar sync commands, and deregister the cron hook. Keep Calendar Matcher (used by CardDAV and AutoTitle) and CalDAV Provider intact.

Purpose: Complete the backend sync removal for v29.0 Made in Europe.
Output: Calendar sync is completely gone from the PHP backend; CalDAV and Matcher remain functional.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/198-backend-sync-removal/198-CONTEXT.md
@.planning/phases/198-backend-sync-removal/198-01-SUMMARY.md
@functions.php
@includes/class-wp-cli.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Calendar sync class files and remove from functions.php</name>
  <files>
    includes/class-calendar-sync.php
    includes/class-google-calendar-provider.php
    includes/class-calendar-connections.php
    includes/class-rest-calendar.php
    functions.php
  </files>
  <action>
    1. Delete these 4 files:
       - `includes/class-calendar-sync.php` (Rondo\Calendar\Sync)
       - `includes/class-google-calendar-provider.php` (Rondo\Calendar\GoogleProvider)
       - `includes/class-calendar-connections.php` (Rondo\Calendar\Connections)
       - `includes/class-rest-calendar.php` (Rondo\REST\Calendar)

    2. In `functions.php`, remove these `use` statements:
       - `use Rondo\REST\Calendar as RESTCalendar;`
       - `use Rondo\Calendar\Connections;`
       - `use Rondo\Calendar\Sync;`
       - `use Rondo\Calendar\GoogleProvider;`

    3. Remove these backward compatibility class aliases from functions.php:
       - The `RONDO_REST_Calendar` alias block (RESTCalendar)
       - The `RONDO_Calendar_Connections` alias block (Connections)
       - The `RONDO_Calendar_Sync` alias block (Sync)
       - The `RONDO_Google_Calendar_Provider` alias block (GoogleProvider)

    4. In `rondo_init()`, remove:
       - `new RESTCalendar();` call (inside the `$is_rest` block)
       - The entire `new Sync();` block near the bottom (with its comment "Calendar sync - needs hooks registered for cron schedule filter")

    5. In `rondo_theme_activation()`, remove:
       - The `$calendar_sync = new Sync(); $calendar_sync->schedule_sync();` block (with its comment "Schedule calendar background sync")

    6. In `rondo_theme_deactivation()`, remove:
       - The `$calendar_sync = new Sync(); $calendar_sync->unschedule_sync();` block (with its comment "Clear calendar sync cron job")

    7. In `functions.php`, remove the `rondo_invalidate_email_lookup_on_person_save` function and its hook entirely (lines ~997-1005). This function calls `Matcher::invalidate_cache()` when a person is saved, but this cache invalidation is already handled by `Matcher::on_person_saved()` which is called from `class-auto-title.php`. The duplicate invalidation via `acf/save_post` hook is redundant.

       WAIT -- verify this is truly redundant before removing. Check if `on_person_saved` in AutoTitle covers the same trigger path. If `acf/save_post` fires at a different time than AutoTitle's hook, keep the invalidation. In that case, update the function to directly call `\Rondo\Calendar\Matcher::invalidate_cache()` instead of using the `Matcher` alias (the alias still works since Matcher class is kept, but remove the dependency on the `RONDO_Calendar_Connections` alias that may be referenced).

       Actually, looking more carefully: `rondo_invalidate_email_lookup_on_person_save` uses `Matcher::invalidate_cache()` which is fine -- Matcher is NOT being deleted. The function only depends on the Matcher class, not on any sync classes. KEEP this function as-is. Do NOT remove it.

    8. Add cron hook deregistration in `functions.php` alongside the Google Contacts one (added in 198-01):
       ```php
       // Clear orphaned Calendar sync cron hook (removed in v29.0)
       if ( wp_next_scheduled( 'rondo_calendar_sync' ) ) {
           wp_clear_scheduled_hook( 'rondo_calendar_sync' );
       }
       ```
       Place this next to the Google Contacts cron cleanup added in 198-01.
  </action>
  <verify>
    - Run `php -l functions.php` to verify no syntax errors
    - Confirm the 4 deleted files no longer exist
    - Grep for remaining references: `grep -rn "RONDO_Calendar_Connections\|RONDO_Calendar_Sync\|RONDO_Google_Calendar_Provider\|RONDO_REST_Calendar\|RESTCalendar\|Calendar\\\\Sync\|Calendar\\\\Connections\|Calendar\\\\GoogleProvider" functions.php` should return nothing (except the Matcher-related code which is kept)
    - Confirm `rondo_invalidate_email_lookup_on_person_save` still exists and uses Matcher (kept)
  </verify>
  <done>4 Calendar sync class files deleted, all sync references removed from functions.php, Matcher and CalDAV Provider preserved, cron hook deregistered</done>
</task>

<task type="auto">
  <name>Task 2: Remove calendar sync WP-CLI commands and verify clean build</name>
  <files>
    includes/class-wp-cli.php
    includes/class-auto-title.php
  </files>
  <action>
    In `includes/class-wp-cli.php`:

    1. Remove the `use Rondo\Calendar\Connections;` import (if still present after 198-01).

    2. Find and remove any calendar sync WP-CLI subcommands or methods that reference the deleted classes. Specifically look for:
       - Any methods that call `Sync::` or `RONDO_Calendar_Sync::`
       - Any methods that call `Connections::` or `RONDO_Calendar_Connections::`
       - Any methods that reference `CalDAVProvider::sync()` via the connection management flow
       - The `calendar sync` or `calendar-sync` subcommand if it exists

       However, be CAREFUL: the WP-CLI file also has a `rematch-events` command that uses `Matcher` -- this must be KEPT because Matcher is not being deleted.

    3. If the `CalDAVProvider::sync()` call in wp-cli.php (line ~1024) is within a calendar sync command that depends on the deleted Connections class, remove that entire command. But if it's a standalone CalDAV command, keep it.

    4. Verify `includes/class-auto-title.php` still works -- it calls `\Rondo\Calendar\Matcher::on_person_saved()` which is kept. No changes needed to this file unless it also references deleted classes.

    5. Run final verification:
       - `php -l functions.php`
       - `php -l includes/class-wp-cli.php`
       - `npm run lint`
       - `npm run build`
  </action>
  <verify>
    - `php -l includes/class-wp-cli.php` shows no syntax errors
    - `php -l includes/class-auto-title.php` shows no syntax errors
    - `grep -rn "Calendar\\\\Sync\|Calendar\\\\Connections\|Calendar\\\\GoogleProvider\|RONDO_Calendar_Sync\|RONDO_Calendar_Connections\|RONDO_Google_Calendar_Provider" includes/class-wp-cli.php` returns nothing
    - `npm run lint` passes with 0 warnings
    - `npm run build` succeeds
  </verify>
  <done>Calendar sync WP-CLI commands removed, Matcher commands preserved, all PHP syntax checks pass, ESLint clean, build succeeds</done>
</task>

</tasks>

<verification>
1. `php -l functions.php` -- no syntax errors
2. `php -l includes/class-wp-cli.php` -- no syntax errors
3. None of the 4 deleted calendar sync files exist
4. Calendar Matcher (`includes/class-calendar-matcher.php`) still exists
5. CalDAV Provider (`includes/class-caldav-provider.php`) still exists
6. `grep -rn "RONDO_Calendar_Sync\|RONDO_Calendar_Connections\|RONDO_Google_Calendar_Provider\|RESTCalendar" functions.php includes/class-wp-cli.php` returns nothing
7. `npm run lint` passes with 0 warnings
8. `npm run build` succeeds
</verification>

<success_criteria>
- 4 Calendar sync class files deleted from `includes/`
- All Calendar sync references removed from `functions.php` (imports, aliases, init, cron scheduling)
- Calendar sync WP-CLI commands removed from `class-wp-cli.php`
- `rondo_calendar_sync` cron hook deregistered
- Calendar Matcher and CalDAV Provider are PRESERVED and functional
- PHP syntax checks pass on all modified files
- ESLint clean, frontend builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/198-backend-sync-removal/198-02-SUMMARY.md`
</output>
