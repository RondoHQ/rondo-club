---
phase: 100-navigation-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Layout.jsx
autonomous: true

must_haves:
  truths:
    - "Sidebar shows Dutch labels: Dashboard, Leden, Teams, Commissies, Datums, Taken, Workspaces, Feedback, Instellingen"
    - "Sidebar logout button shows Uitloggen"
    - "User menu shows Profiel bewerken and WordPress beheer"
    - "Quick actions menu shows Nieuw lid, Nieuw team, Nieuwe taak, Nieuwe datum"
    - "Search modal shows Dutch placeholder and labels"
    - "Header page titles show Dutch labels"
  artifacts:
    - path: "src/components/layout/Layout.jsx"
      provides: "Dutch navigation and layout strings"
      contains: "Leden"
  key_links:
    - from: "navigation array names"
      to: "getCounts switch cases"
      via: "name matching"
      pattern: "case 'Leden'"
---

<objective>
Translate all navigation and layout UI elements from English to Dutch in Layout.jsx.

Purpose: Users see Dutch labels throughout the navigation, completing the first UI area of the Dutch localization milestone.
Output: Layout.jsx with all strings translated per CONTEXT.md decisions.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/100-navigation-layout/100-CONTEXT.md
@.planning/phases/100-navigation-layout/100-RESEARCH.md
@src/components/layout/Layout.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate navigation array and getCounts</name>
  <files>src/components/layout/Layout.jsx</files>
  <action>
Update the navigation array (lines 39-49) with Dutch labels:
- 'Dashboard' -> 'Dashboard' (keep English - loan word)
- 'People' -> 'Leden'
- 'Teams' -> 'Teams' (already correct)
- 'Commissies' -> 'Commissies' (already correct)
- 'Dates' -> 'Datums'
- 'Todos' -> 'Taken'
- 'Workspaces' -> 'Workspaces' (keep English per CONTEXT.md)
- 'Feedback' -> 'Feedback' (keep English - loan word)
- 'Settings' -> 'Instellingen'

Update getCounts switch cases (lines 57-62) to match new Dutch names:
- 'People' -> 'Leden'
- 'Organizations' -> 'Teams' (note: was already Teams in nav, but Organizations in switch)
- 'Dates' -> 'Datums'

Update sidebar logout text (line 114):
- 'Log Out' -> 'Uitloggen'
  </action>
  <verify>
Run `npm run lint` - no errors.
Grep for 'People' in Layout.jsx - should only appear in search results section header translation (handled in Task 2).
  </verify>
  <done>
Navigation sidebar shows: Dashboard, Leden, Teams, Commissies, Datums, Taken, Workspaces, Feedback, Instellingen.
Logout button shows "Uitloggen".
Navigation item counts still work correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Translate menus, search modal, and header</name>
  <files>src/components/layout/Layout.jsx</files>
  <action>
Update UserMenu component (lines 196, 207):
- 'Edit profile' -> 'Profiel bewerken'
- 'WordPress admin' -> 'WordPress beheer'
- Update aria-label="User menu" -> aria-label="Gebruikersmenu"

Update QuickAddMenu component (lines 492-519):
- 'New Person' -> 'Nieuw lid'
- 'New Organization' -> 'Nieuw team'
- 'New Todo' -> 'Nieuwe taak'
- 'New Date' -> 'Nieuwe datum'
- Update aria-label="Quick add" -> aria-label="Snelle actie"
- Update title="Quick add" -> title="Snelle actie"

Update SearchModal component:
- placeholder="Search people & organizations..." -> placeholder="Zoek leden en teams..."
- "Type at least 2 characters to search" -> "Typ minimaal 2 tekens om te zoeken"
- "Searching..." -> "Zoeken..."
- Section header "People" -> "Leden"
- Section header "Organizations" -> "Teams"
- "No results found for" -> "Geen resultaten gevonden voor"

Update Header component:
- title="Search (Cmd+K)" -> title="Zoeken (Cmd+K)"
- "Search..." -> "Zoek..."
- "Customize" button text -> "Aanpassen"

Update getPageTitle function (lines 532-543):
- 'Dashboard' -> 'Dashboard' (keep English)
- 'People' -> 'Leden'
- 'Teams' -> 'Teams' (already correct)
- 'Commissies' -> 'Commissies' (already correct)
- 'Important Dates' -> 'Datums'
- 'Todos' -> 'Taken'
- 'Workspaces' -> 'Workspaces' (keep English)
- 'Settings' -> 'Instellingen'
  </action>
  <verify>
Run `npm run lint` - no errors.
Run `npm run build` - builds successfully.
Grep for remaining English strings that should be Dutch - should find none except: Dashboard, Teams, Commissies, Workspaces, Feedback.
  </verify>
  <done>
User menu shows Dutch labels.
Quick add menu shows Dutch labels with correct gender agreement (Nieuw/Nieuwe).
Search modal fully translated.
Header page titles show Dutch.
All aria-labels and titles translated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deploy and verify</name>
  <files>src/components/layout/Layout.jsx</files>
  <action>
1. Run `npm run build` to create production build
2. Deploy to production using `bin/deploy.sh`
3. Verify the translations are live
  </action>
  <verify>
Production site shows Dutch navigation labels.
  </verify>
  <done>
Dutch navigation is live on production.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run lint` passes with no errors
2. `npm run build` succeeds
3. Production site shows:
   - Dutch sidebar labels
   - Dutch quick actions menu
   - Dutch user menu labels
   - Dutch search modal
   - Dutch header page titles
</verification>

<success_criteria>
- All navigation labels are in Dutch (except approved loan words: Dashboard, Feedback, Workspaces)
- Quick actions use correct Dutch gender: "Nieuw lid", "Nieuw team", "Nieuwe taak", "Nieuwe datum"
- User menu shows "Profiel bewerken" and "WordPress beheer"
- Search modal fully translated
- Header titles match sidebar labels
- All aria-labels and titles translated for accessibility
- No lint errors, build succeeds
- Changes deployed to production
</success_criteria>

<output>
After completion, create `.planning/phases/100-navigation-layout/100-01-SUMMARY.md`
</output>
