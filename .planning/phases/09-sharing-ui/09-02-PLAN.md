---
phase: 09-sharing-ui
plan: 02
type: execute
depends_on: ["09-01"]
files_modified: [src/components/VisibilitySelector.jsx, src/components/PersonEditModal.jsx, src/components/CompanyEditModal.jsx, src/pages/People/PeopleList.jsx, src/pages/Companies/CompaniesList.jsx]
---

<objective>
Create VisibilitySelector component and integrate it into Person and Company edit flows.

Purpose: Allow users to set visibility (private/workspace) when creating or editing contacts and companies.
Output: Working visibility controls in both PersonEditModal and CompanyEditModal for add and edit operations.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-sharing-ui/09-01-SUMMARY.md
@.planning/phases/07-data-model-visibility/07-02-SUMMARY.md
@src/components/PersonEditModal.jsx
@src/components/CompanyEditModal.jsx
@src/pages/People/PeopleList.jsx
@src/hooks/useWorkspaces.js

**ACF Fields Available (from Phase 7):**
- `_visibility`: 'private' | 'workspace' (default: 'private')
- `_assigned_workspaces`: taxonomy field for workspace_access terms (shows when visibility='workspace')

**Patterns:**
- Modals use react-hook-form with `useForm()` and `Controller`
- ACF fields sent in `acf` object on create/update
- Workspace list available via `useWorkspaces()` hook from 09-01
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VisibilitySelector component</name>
  <files>src/components/VisibilitySelector.jsx</files>
  <action>
Create a reusable component for visibility selection:

```javascript
import { useState, useEffect } from 'react';
import { Lock, Users, ChevronDown, Check } from 'lucide-react';
import { useWorkspaces } from '@/hooks/useWorkspaces';

export default function VisibilitySelector({
  value = 'private',           // Current visibility value
  workspaces = [],             // Selected workspace IDs
  onChange,                    // Called with { visibility, workspaces }
  disabled = false
}) {
  const [isOpen, setIsOpen] = useState(false);
  const { data: availableWorkspaces = [], isLoading } = useWorkspaces();

  const visibilityOptions = [
    {
      value: 'private',
      label: 'Private',
      description: 'Only you can see this',
      icon: Lock
    },
    {
      value: 'workspace',
      label: 'Workspace',
      description: 'Share with workspace members',
      icon: Users
    },
  ];

  const currentOption = visibilityOptions.find(opt => opt.value === value) || visibilityOptions[0];
  const CurrentIcon = currentOption.icon;

  const handleVisibilityChange = (newVisibility) => {
    onChange?.({
      visibility: newVisibility,
      workspaces: newVisibility === 'private' ? [] : workspaces
    });
    if (newVisibility === 'private') {
      setIsOpen(false);
    }
  };

  const handleWorkspaceToggle = (workspaceId) => {
    const newWorkspaces = workspaces.includes(workspaceId)
      ? workspaces.filter(id => id !== workspaceId)
      : [...workspaces, workspaceId];
    onChange?.({ visibility: value, workspaces: newWorkspaces });
  };

  return (
    <div className="relative">
      <label className="block text-sm font-medium text-gray-700 mb-1">
        Visibility
      </label>

      {/* Dropdown trigger */}
      <button
        type="button"
        onClick={() => !disabled && setIsOpen(!isOpen)}
        disabled={disabled}
        className="w-full flex items-center justify-between px-3 py-2 border border-gray-300 rounded-lg bg-white text-left focus:ring-2 focus:ring-primary-500 focus:border-primary-500 disabled:bg-gray-100 disabled:cursor-not-allowed"
      >
        <div className="flex items-center gap-2">
          <CurrentIcon className="w-4 h-4 text-gray-500" />
          <span className="text-sm text-gray-900">{currentOption.label}</span>
        </div>
        <ChevronDown className={`w-4 h-4 text-gray-400 transition-transform ${isOpen ? 'rotate-180' : ''}`} />
      </button>

      {/* Dropdown menu */}
      {isOpen && (
        <div className="absolute z-50 mt-1 w-full bg-white border border-gray-200 rounded-lg shadow-lg">
          {/* Visibility options */}
          <div className="p-2 border-b border-gray-100">
            {visibilityOptions.map((option) => {
              const Icon = option.icon;
              const isSelected = value === option.value;
              return (
                <button
                  key={option.value}
                  type="button"
                  onClick={() => handleVisibilityChange(option.value)}
                  className={`w-full flex items-start gap-3 p-2 rounded-md text-left hover:bg-gray-50 ${isSelected ? 'bg-primary-50' : ''}`}
                >
                  <Icon className={`w-4 h-4 mt-0.5 ${isSelected ? 'text-primary-600' : 'text-gray-400'}`} />
                  <div className="flex-1">
                    <div className={`text-sm font-medium ${isSelected ? 'text-primary-900' : 'text-gray-900'}`}>
                      {option.label}
                    </div>
                    <div className="text-xs text-gray-500">{option.description}</div>
                  </div>
                  {isSelected && <Check className="w-4 h-4 text-primary-600 mt-0.5" />}
                </button>
              );
            })}
          </div>

          {/* Workspace selection (only when visibility is 'workspace') */}
          {value === 'workspace' && (
            <div className="p-2">
              <div className="text-xs font-medium text-gray-500 uppercase tracking-wide mb-2 px-2">
                Select Workspaces
              </div>
              {isLoading ? (
                <div className="px-2 py-3 text-sm text-gray-500">Loading workspaces...</div>
              ) : availableWorkspaces.length === 0 ? (
                <div className="px-2 py-3 text-sm text-gray-500">
                  No workspaces available. Create one first.
                </div>
              ) : (
                <div className="max-h-48 overflow-y-auto">
                  {availableWorkspaces.map((workspace) => {
                    const isChecked = workspaces.includes(workspace.id);
                    return (
                      <button
                        key={workspace.id}
                        type="button"
                        onClick={() => handleWorkspaceToggle(workspace.id)}
                        className="w-full flex items-center gap-3 p-2 rounded-md text-left hover:bg-gray-50"
                      >
                        <div className={`flex items-center justify-center w-4 h-4 border-2 rounded ${isChecked ? 'bg-primary-600 border-primary-600' : 'border-gray-300'}`}>
                          {isChecked && <Check className="w-3 h-3 text-white" />}
                        </div>
                        <div className="flex-1">
                          <div className="text-sm text-gray-900">{workspace.title}</div>
                          <div className="text-xs text-gray-500">{workspace.member_count} members</div>
                        </div>
                      </button>
                    );
                  })}
                </div>
              )}
            </div>
          )}

          {/* Close button for workspace selection */}
          {value === 'workspace' && (
            <div className="p-2 border-t border-gray-100">
              <button
                type="button"
                onClick={() => setIsOpen(false)}
                className="w-full px-3 py-2 text-sm font-medium text-primary-600 hover:bg-primary-50 rounded-md"
              >
                Done
              </button>
            </div>
          )}
        </div>
      )}

      {/* Selected workspaces summary */}
      {value === 'workspace' && workspaces.length > 0 && !isOpen && (
        <div className="mt-1 text-xs text-gray-500">
          Shared with {workspaces.length} workspace{workspaces.length !== 1 ? 's' : ''}
        </div>
      )}
    </div>
  );
}
```

Key features:
- Shows Private/Workspace options with icons and descriptions
- When "Workspace" selected, shows checkbox list of available workspaces
- Calls onChange with both visibility and selected workspaces
- Handles loading and empty states for workspaces
  </action>
  <verify>`npm run lint` passes</verify>
  <done>VisibilitySelector.jsx component created with dropdown and workspace selection</done>
</task>

<task type="auto">
  <name>Task 2: Integrate VisibilitySelector into PersonEditModal</name>
  <files>src/components/PersonEditModal.jsx</files>
  <action>
Modify PersonEditModal to include visibility controls:

1. Import the component:
```javascript
import VisibilitySelector from '@/components/VisibilitySelector';
```

2. Add state for visibility (since it's not part of react-hook-form):
```javascript
const [visibility, setVisibility] = useState('private');
const [selectedWorkspaces, setSelectedWorkspaces] = useState([]);
```

3. In the useEffect that resets form on open, also reset visibility:
```javascript
// When editing, load existing visibility
if (person) {
  setVisibility(person.acf?._visibility || 'private');
  // Convert workspace term IDs to workspace post IDs
  // The _assigned_workspaces field stores term IDs, but we need workspace IDs
  // For simplicity, we'll store workspace IDs directly
  setSelectedWorkspaces(person.acf?._assigned_workspaces || []);
} else {
  setVisibility('private');
  setSelectedWorkspaces([]);
}
```

4. Add VisibilitySelector to the form JSX (after the "How we met" field, before the favorite checkbox):
```jsx
{/* Visibility */}
<div className="col-span-2">
  <VisibilitySelector
    value={visibility}
    workspaces={selectedWorkspaces}
    onChange={({ visibility: v, workspaces: w }) => {
      setVisibility(v);
      setSelectedWorkspaces(w);
    }}
  />
</div>
```

5. Update the onSubmit handler to include visibility in the data:
```javascript
const handleFormSubmit = (data) => {
  onSubmit({
    ...data,
    visibility,
    assigned_workspaces: selectedWorkspaces,
  });
};
```

6. Also update PeopleList.jsx where createPersonMutation builds the payload - add visibility fields to the acf object:
```javascript
acf: {
  // ... existing fields ...
  _visibility: data.visibility || 'private',
  _assigned_workspaces: data.assigned_workspaces || [],
}
```
  </action>
  <verify>Open PersonEditModal in browser, verify VisibilitySelector appears and functions</verify>
  <done>PersonEditModal includes VisibilitySelector for both add and edit modes</done>
</task>

<task type="auto">
  <name>Task 3: Integrate VisibilitySelector into CompanyEditModal</name>
  <files>src/components/CompanyEditModal.jsx, src/pages/Companies/CompaniesList.jsx</files>
  <action>
Apply the same pattern to CompanyEditModal:

1. Import the component:
```javascript
import VisibilitySelector from '@/components/VisibilitySelector';
```

2. Add state for visibility:
```javascript
const [visibility, setVisibility] = useState('private');
const [selectedWorkspaces, setSelectedWorkspaces] = useState([]);
```

3. In useEffect on modal open, reset/load visibility:
```javascript
if (company) {
  setVisibility(company.acf?._visibility || 'private');
  setSelectedWorkspaces(company.acf?._assigned_workspaces || []);
} else {
  setVisibility('private');
  setSelectedWorkspaces([]);
}
```

4. Add VisibilitySelector to form JSX (in an appropriate location, likely after main company fields):
```jsx
{/* Visibility */}
<div className="col-span-2">
  <VisibilitySelector
    value={visibility}
    workspaces={selectedWorkspaces}
    onChange={({ visibility: v, workspaces: w }) => {
      setVisibility(v);
      setSelectedWorkspaces(w);
    }}
  />
</div>
```

5. Include visibility in submit data:
```javascript
const handleFormSubmit = (data) => {
  onSubmit({
    ...data,
    visibility,
    assigned_workspaces: selectedWorkspaces,
  });
};
```

6. Update CompaniesList.jsx (if it has a create mutation similar to PeopleList) to include visibility fields in the ACF payload:
```javascript
acf: {
  // ... existing fields ...
  _visibility: data.visibility || 'private',
  _assigned_workspaces: data.assigned_workspaces || [],
}
```

Note: CompanyEditModal is a larger file (~700 lines). Look for the existing form structure and add VisibilitySelector in a logical location (likely in the "Basic Info" section or at the end before submit button).
  </action>
  <verify>Open CompanyEditModal in browser, verify VisibilitySelector appears and functions</verify>
  <done>CompanyEditModal includes VisibilitySelector for both add and edit modes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>VisibilitySelector component integrated into both PersonEditModal and CompanyEditModal for add and edit operations</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Go to People page, click "Add person"
    3. Verify: VisibilitySelector appears with "Private" as default
    4. Click the dropdown, select "Workspace"
    5. Verify: Workspace list appears (or "No workspaces available" if none exist)
    6. Fill in required fields (first name, last name) and save
    7. Edit the person you just created
    8. Verify: The visibility setting you chose is preserved
    9. Repeat steps 2-8 for Companies page
    10. Check browser console for any errors
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` succeeds
- [ ] VisibilitySelector works in PersonEditModal (add + edit)
- [ ] VisibilitySelector works in CompanyEditModal (add + edit)
- [ ] Visibility setting persists after save
</verification>

<success_criteria>

- VisibilitySelector component created and working
- PersonEditModal includes visibility controls
- CompanyEditModal includes visibility controls
- Default visibility is "private" for new contacts/companies
- Existing visibility loads correctly when editing
- Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/09-sharing-ui/09-02-SUMMARY.md`
</output>
